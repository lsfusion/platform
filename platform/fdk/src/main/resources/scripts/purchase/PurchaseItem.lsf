MODULE PurchaseItem;

REQUIRE Item, PurchasePack;

NAMESPACE Purchase;

EXTEND FORM item
    PROPERTIES(i) purchaseAmount = amountPackSku ON CHANGE changePackSku(i)
    PROPERTIES(b) packBarcode ON CHANGE changePackBarcodeSku(b,i) BEFORE delete(b)
;
EXTEND DESIGN item {
//    itemHeaderColumn1 {
        PROPERTY(purchaseAmount) {caption = 'Количество в упаковке (закупка)';}
//    }
}
