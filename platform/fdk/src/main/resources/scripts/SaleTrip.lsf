MODULE SaleTrip;

REQUIRE SaleOrder,
        Trip;

NAMESPACE Sale;

EXTEND CLASS order : orderTrip;
EXTEND CLASS orderDetail : orderTripDetail;

numberOrderTrip(order) += numberOrder(order);
seriesOrderTrip(order) += seriesOrder(order);
dateOrderTrip(order) += dateOrder(order);
grossWeightOrderTrip(order) += grossWeightOrder(order);

nodeToOrderTrip(order) += customerOrder(order);
nodeFromOrderTrip(order) += stockOrder(order);

quantityOrderTripDetail(orderDetail) += quantityOrderDetail(orderDetail);

//fillQuantityTripOrderTrip 'Заполнить из заказа' (trip, orderTrip) = ACTION (trip, orderTrip) {
//    FOR quantityOrderDetail(orderTripDetail) AND orderOrderDetail(orderTripDetail)== orderTrip AND inTripOrderTrip(trip, orderTrip) DO {
//
//        SET quantityTripOrderTripDetail(trip, orderTripDetail) <- quantityOrderDetail(orderTripDetail);
//
//    }
//} EDITKEY 'F5' CONFIRM;


