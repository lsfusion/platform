package platform.client.descriptor.editor;

import platform.client.descriptor.PropertyDrawDescriptor;

import javax.swing.*;

public class PropertyDrawEditor extends JPanel {

    public PropertyDrawEditor(PropertyDrawDescriptor descriptor) {
        add(new JLabel("Property"));
    }
}
