MODULE PriceRoundItem;

REQUIRE PriceRound, Item;

EXTEND CLASS ItemGroup : RoundGroup;
roundGroupSku(sku) += itemGroupItem(sku);

nameRoundGroup(group) += nameItemGroup(group) IF group IS ItemGroup;