package lsfusion.gwt.client.navigator.window;

// SHOW ... ACTIVATE: instead of opening a second instance, the form already open is activated. The client twin of
// FormActivateType - which of the two the open asked for, or null when it asked for neither
public enum GFormActivateType {
    USER,
    FIXED
}
