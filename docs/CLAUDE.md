@AGENTS.md

Documentation-specific rules for tasks under `platform/docs/`. The platform-level rules in `../AGENTS.md` (loaded via `../CLAUDE.md`) still apply — commit conventions, issue policy, risk discipline, etc. This file's `@AGENTS.md` resolves to `platform/docs/AGENTS.md`, which **complements** rather than replaces them.
