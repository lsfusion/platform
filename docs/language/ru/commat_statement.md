---
slug: "/commat_statement"
title: 'Инструкция @'
---

Инструкция `@` - использование [метакода](../paradigm/Metaprogramming.md#metacode).

### Синтаксис

```
@name(param1, ..., paramN)
[{
    statement1
    ...
    statementM
}];
```

### Описание

Инструкция `@` формирует код, получаемый из метакода с именем `name`, заменяя параметры метакода на значения собственных параметров и выполняя специальные [операции `##` и `###`](../paradigm/Metaprogramming.md#concat). 

Блок в фигурных скобках содержит сформированный код инструкции: его генерирует и поддерживает среда разработки, чтобы с полученным кодом работали навигация и анализ. При компиляции платформа игнорирует содержимое блока и всегда формирует код заново из объявления метакода, поэтому редактировать блок вручную нельзя — правки вносятся в объявление метакода или в параметры инструкции.

### Параметры 

- `name`

    Имя используемого метакода. [Составной идентификатор](IDs.md#cid).  

- `param1, ..., paramN`

    Список параметров инструкции, которые будут подставляться вместо параметров используемого метакода. В качестве параметров могут выступать [составной идентификатор](IDs.md#cid), [идентификатор класса](IDs.md#classid), [литерал](Literals.md) или *пустой параметр*, когда ничего не передается в качестве параметра.

- `statement1 ... statementM`

    Сформированный код инструкции — последовательность [инструкций](Statements.md), сгенерированная средой разработки.

### Примеры

```lsf
CLASS Book;
@objectProperties(book, INTEGER, 'Книга');

CLASS Flower;
@objectProperties(flower, BPSTRING[100], ); // если не передать параметр, то он будет пустым

CLASS Table;
@objectProperties(table, NUMERIC[14,2]);
```
