@AGENTS.md

Type-specific documentation rules for `docs/rules/` (RULES — AI task rules / coding recommendations). The common docs rules in `../AGENTS.md` (via `../GEMINI.md`) and the platform rules above still apply — this file's `@AGENTS.md` resolves to `docs/rules/AGENTS.md`, which **complements** them.
