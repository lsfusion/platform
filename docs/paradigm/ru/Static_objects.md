---
slug: "/Static_objects"
title: 'Статические объекты'
---

*Статические* (или встроенные) объекты - это объекты, которые определяются разработчиком и автоматически встраиваются в систему при старте (если на момент старта они в системе отсутствуют). Кроме того, такие объекты запрещено удалять.

При объявлении [пользовательского класса](User_classes.md) можно объявить объекты этого класса, которые будут являться статическими объектами. Если это сделать, этот пользовательский класс автоматически [наследуется](User_classes.md#inheritance) от класса `System.StaticObject`.

Каждому статическому объекту пользовательского класса необходимо задать определенное имя и заголовок, а также можно задать иконку, к которым впоследствии можно обращаться при помощи [свойств](Properties.md) `name[StaticObject]`, `caption[StaticObject]` и `image[StaticObject]` соответственно. `name[StaticObject]` возвращает *каноническое* имя объекта — его идентификатор с пространством имён модуля и классом, в виде `<пространство_имён>_<Класс>.<объект>` (для объекта `Direction.north` ниже `name[StaticObject]` даёт `<пространство_имён>_Direction.north`, а не просто `north`), тогда как `caption[StaticObject]` возвращает показываемый пользователю заголовок.

Статическими объектами [встроенных классов](Built-in_classes.md) являются конкретные числа, строки, значения дат и т.п., используемые пользователем при описании логики.

Статические объекты можно использовать для создания ограниченного множества объектов некоторого класса. Такое множество можно использовать в качестве перечисляемого типа данных, чтобы предоставлять выбор из ограниченного набора значений. Чтобы показать такое значение-перечисление на форме и дать пользователю его изменять, выводится не сама связь со статическим объектом, а её композиция с `caption[StaticObject]`: [интерактивное представление](Interactive_view.md#select) при описанных там условиях показывает такую композицию элементом выбора из объектов класса, а запись через неё уходит в связь, а не в заголовок статического объекта.

### Язык

Статические объекты пользовательских классов задаются в [инструкции `CLASS`](../language/CLASS_statement.md) в блоке, ограниченном фигурными скобками.

### Примеры

```lsf
CLASS Direction 'Направление'
{
    north 'Север',
    east 'Восток',
    south 'Юг',
    west 'Запад'
}

direction = DATA Direction ();

showDirection  {
    MESSAGE name(direction());
    MESSAGE caption(direction());
}

// создаем форму по выбору объекта класса Direction
FORM directions 'Направления'
    OBJECTS d = Direction
    PROPERTIES(d) READONLY caption

    LIST Direction OBJECT d
;

// выбор направления на форме: выводится композиция заголовка,
// запись через нее уходит в свойство direction
FORM windRose 'Роза ветров'
    PROPERTIES captionDirection 'Направление' = caption(direction())
;
```

