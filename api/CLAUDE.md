@AGENTS.md

Module-specific rules for `api/` (API-version bumping). The cross-cutting platform rules in `../AGENTS.md` (via `../CLAUDE.md`) still apply — this file's `@AGENTS.md` resolves to `api/AGENTS.md`, which **complements** them.
