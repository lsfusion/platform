package lsfusion.client.navigator.window;

import lsfusion.client.navigator.ClientNavigatorElement;

public class ClientClassWindowNavigator extends ClientWindowNavigator {

    public ClientClassWindowNavigator(String canonicalName) {
        super(canonicalName);
    }

    @Override
    public void update(ClientNavigatorElement rootElement, Object value) {
    }
}
