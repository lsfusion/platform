@AGENTS.md

Module-specific rules for `server/` (language-syntax deprecation workflow, system `.lsf` deprecation-warning policy). The cross-cutting platform rules in `../AGENTS.md` (via `../GEMINI.md`) still apply — this file's `@AGENTS.md` resolves to `server/AGENTS.md`, which **complements** them.
