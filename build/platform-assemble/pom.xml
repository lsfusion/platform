<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>lsfusion.platform.build</groupId>
        <artifactId>assemble</artifactId>
        <version>1.3.5-SNAPSHOT</version>
        <relativePath>../platform-base/pom.xml</relativePath>
    </parent>

    <artifactId>platform-assemble</artifactId>
    <packaging>pom</packaging>
    <version>1.3.5-SNAPSHOT</version>
    <name>Platform Assemble</name>

    <profiles>
        <profile>
            <id>assemble</id>
            <activation>
                <property>
                    <name>assemble.mainClass</name>
                </property>                
            </activation>
        </profile>
    </profiles>

    <!-- multiple-inheritance : platform-base -->
    <build>
        <plugins>
            <plugin>
                <!-- upload sources -->
                <artifactId>maven-source-plugin</artifactId>
                <version>2.2.1</version>
                <executions>
                    <execution>
                        <id>attach-sources</id>
                        <phase>verify</phase>
                        <goals>
                            <goal>jar-no-fork</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>
        </plugins>
    </build>

</project>