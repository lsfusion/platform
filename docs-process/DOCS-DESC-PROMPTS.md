# DOCS-DESC-PROMPTS — промпты исполнителям по блокам

Самодостаточный промпт на каждый блок, сгруппировано по трекам (внутри трека — снизу вверх). Оркестратор копирует нужный блок целиком и отдаёт исполнителю; один исполнитель ведёт один трек. Общий процесс и правила — `DOCS-DESC-PLAN.md` и `docs/AGENTS.md`.

# Трек T1 — Логическая модель — данные и выражения

## B02 — Классы  ·  [P+L] · слой 1 · трек T1 · зона: Логическая модель

_Scope:_ Классы, пользовательские/встроенные классы, статические объекты, операторы классов, классификация.

```
Ты дорабатываешь документацию lsFusion по блоку B02 «Классы» (трек T1 — Логическая модель — данные и выражения).

Перед работой прочитай: docs/AGENTS.md (релевантные разделы целиком) и docs-process/DOCS-DESC-PLAN.md.
Источник истины — КОД платформы (LsfLogics.g -> ScriptingLogicsModule.java -> LogicsModule.java -> классы;
примеры в server/src/main/lsfusion; плагин — вторичен). Не описывай по памяти.
Работай в выданной под блок ветке (docs/B02 от master) — создай её ПЕРВЫМ действием и в ОТДЕЛЬНОМ рабочем дереве (git worktree add), не в общем чекауте: иначе параллельные блоки/исполнители молча затрут несохранённое. Коммить в ветку рано (commit-first); в master сам не мерджи и master не пушь.
EN и RU — построчно синхронные переводы одного текста (одна структура и состав информации).
Tier-чистота: Paradigm — абстракция (как будто синтаксиса/расширений нет); Language — синтаксис/механика
именно этой абстракции, ссылается на Paradigm и не переизлагает её. Не смешивать tier'ы.

ДВА ПОСЛЕДОВАТЕЛЬНЫХ ПОД-ЦИКЛА: сначала ЦЕЛИКОМ Paradigm, потом ЦЕЛИКОМ Language. Каждый под-цикл =
описание + СВОЙ коммит + ЗАВЕРШЁННОЕ ревью. Language не начинать, пока под-цикл Paradigm не закрыт.

**Paradigm (доописать обе версии, en+ru построчно синхронно; если статьи ещё нет — создать оба файла):**
- `docs/paradigm/en/Classes.md` + `docs/paradigm/ru/Classes.md`
- `docs/paradigm/en/User_classes.md` + `docs/paradigm/ru/User_classes.md`
- `docs/paradigm/en/Built-in_classes.md` + `docs/paradigm/ru/Built-in_classes.md`
- `docs/paradigm/en/Static_objects.md` + `docs/paradigm/ru/Static_objects.md`
- `docs/paradigm/en/Class_operators.md` + `docs/paradigm/ru/Class_operators.md`
- `docs/paradigm/en/Classification_IS_AS.md` + `docs/paradigm/ru/Classification_IS_AS.md`

**Language (доописать обе версии, en+ru построчно синхронно; если статьи ещё нет — создать оба файла):**
- `docs/language/en/CLASS_statement.md` + `docs/language/ru/CLASS_statement.md`
- `docs/language/en/IS_AS_operators.md` + `docs/language/ru/IS_AS_operators.md`
- `docs/language/en/ISCLASS_operator.md` + `docs/language/ru/ISCLASS_operator.md`
- `docs/language/en/Property_signature_ISCLASS.md` + `docs/language/ru/Property_signature_ISCLASS.md`

ЦИКЛ СДАЧИ на КАЖДЫЙ под-цикл (commit-first внутри ветки блока):
  Под-цикл 1 — PARADIGM: коммит 'docs: B02 paradigm — Классы'.
  Под-цикл 2 — LANGUAGE (только после ПОЛНОГО закрытия Paradigm): коммит 'docs: B02 language — Классы'.
  Шаги под-цикла:
  1. Написать/создать файлы под-цикла -> коммит (subject выше).
  2. self-review (явная сверка с каждым релевантным правилом AGENTS.md; полнота по коду/дочерним;
     EN/RU parity; направление cross-link'ов; tier-чистота) + codex review с master:docs/AGENTS.md;
     правки отдельными коммитами до чистого прохода. Это твой контроль ПЕРЕД показом человеку.
  3. РЕВЬЮ ЧЕЛОВЕКОМ: отдаёшь результат человеку (исполнитель/оркестратор) — он даёт замечания.
  4. Исправляешь замечания ОТДЕЛЬНЫМИ коммитами (при необходимости снова self/codex) и возвращаешь на ревью.
  5. Повторяешь шаги 3-4, пока у человека НЕ ОСТАНЕТСЯ претензий — только тогда под-цикл закрыт.

Если конструкции блока ещё нет статьи — создать файлы en+ru в нужной type-папке (docs/paradigm/ или
docs/language/) и сам подключить их в docs/sidebars.js тем же блочным коммитом. Нашёл обобщаемое правило/проблему —
допиши его в AGENTS.md ОТДЕЛЬНЫМ коммитом (AGENTS.md дописывать может любой; это файл для ИИ).
CLAUDE.md в коммиты блока НЕ включать; docs/sidebars.js — НАОБОРОТ, включать в блочный коммит (это часть документации). Ветку блока пушишь сам (для ревью); merge ветки
в master (squash по необходимости) делает оркестратор после закрытия обоих под-циклов.
```

## B05 — Арифметика/сравнение/логика  ·  [P+L] · слой 1 · трек T1 · зона: Логическая модель

_Scope:_ Константа, арифметика, сравнение, логика.

```
Ты дорабатываешь документацию lsFusion по блоку B05 «Арифметика/сравнение/логика» (трек T1 — Логическая модель — данные и выражения).

Перед работой прочитай: docs/AGENTS.md (релевантные разделы целиком) и docs-process/DOCS-DESC-PLAN.md.
Источник истины — КОД платформы (LsfLogics.g -> ScriptingLogicsModule.java -> LogicsModule.java -> классы;
примеры в server/src/main/lsfusion; плагин — вторичен). Не описывай по памяти.
Работай в выданной под блок ветке (docs/B05 от master) — создай её ПЕРВЫМ действием и в ОТДЕЛЬНОМ рабочем дереве (git worktree add), не в общем чекауте: иначе параллельные блоки/исполнители молча затрут несохранённое. Коммить в ветку рано (commit-first); в master сам не мерджи и master не пушь.
EN и RU — построчно синхронные переводы одного текста (одна структура и состав информации).
Tier-чистота: Paradigm — абстракция (как будто синтаксиса/расширений нет); Language — синтаксис/механика
именно этой абстракции, ссылается на Paradigm и не переизлагает её. Не смешивать tier'ы.

ДВА ПОСЛЕДОВАТЕЛЬНЫХ ПОД-ЦИКЛА: сначала ЦЕЛИКОМ Paradigm, потом ЦЕЛИКОМ Language. Каждый под-цикл =
описание + СВОЙ коммит + ЗАВЕРШЁННОЕ ревью. Language не начинать, пока под-цикл Paradigm не закрыт.

**Paradigm (доописать обе версии, en+ru построчно синхронно; если статьи ещё нет — создать оба файла):**
- `docs/paradigm/en/Constant.md` + `docs/paradigm/ru/Constant.md`
- `docs/paradigm/en/Arithmetic_operators_plus_minus_etc.md` + `docs/paradigm/ru/Arithmetic_operators_plus_minus_etc.md`
- `docs/paradigm/en/Comparison_operators_=_etc.md` + `docs/paradigm/ru/Comparison_operators_=_etc.md`
- `docs/paradigm/en/Logical_operators_AND_OR_NOT_XOR.md` + `docs/paradigm/ru/Logical_operators_AND_OR_NOT_XOR.md`

**Language (доописать обе версии, en+ru построчно синхронно; если статьи ещё нет — создать оба файла):**
- `docs/language/en/Arithmetic_operators.md` + `docs/language/ru/Arithmetic_operators.md`
- `docs/language/en/Comparison_operators.md` + `docs/language/ru/Comparison_operators.md`
- `docs/language/en/AND_OR_NOT_XOR_operators.md` + `docs/language/ru/AND_OR_NOT_XOR_operators.md`

ЦИКЛ СДАЧИ на КАЖДЫЙ под-цикл (commit-first внутри ветки блока):
  Под-цикл 1 — PARADIGM: коммит 'docs: B05 paradigm — Арифметика/сравнение/логика'.
  Под-цикл 2 — LANGUAGE (только после ПОЛНОГО закрытия Paradigm): коммит 'docs: B05 language — Арифметика/сравнение/логика'.
  Шаги под-цикла:
  1. Написать/создать файлы под-цикла -> коммит (subject выше).
  2. self-review (явная сверка с каждым релевантным правилом AGENTS.md; полнота по коду/дочерним;
     EN/RU parity; направление cross-link'ов; tier-чистота) + codex review с master:docs/AGENTS.md;
     правки отдельными коммитами до чистого прохода. Это твой контроль ПЕРЕД показом человеку.
  3. РЕВЬЮ ЧЕЛОВЕКОМ: отдаёшь результат человеку (исполнитель/оркестратор) — он даёт замечания.
  4. Исправляешь замечания ОТДЕЛЬНЫМИ коммитами (при необходимости снова self/codex) и возвращаешь на ревью.
  5. Повторяешь шаги 3-4, пока у человека НЕ ОСТАНЕТСЯ претензий — только тогда под-цикл закрыт.

Если конструкции блока ещё нет статьи — создать файлы en+ru в нужной type-папке (docs/paradigm/ или
docs/language/) и сам подключить их в docs/sidebars.js тем же блочным коммитом. Нашёл обобщаемое правило/проблему —
допиши его в AGENTS.md ОТДЕЛЬНЫМ коммитом (AGENTS.md дописывать может любой; это файл для ИИ).
CLAUDE.md в коммиты блока НЕ включать; docs/sidebars.js — НАОБОРОТ, включать в блочный коммит (это часть документации). Ветку блока пушишь сам (для ревью); merge ветки
в master (squash по необходимости) делает оркестратор после закрытия обоих под-циклов.
```

## B06 — Строки/округление/экстремум/конверсия/структуры  ·  [P+L] · слой 1 · трек T1 · зона: Логическая модель

_Scope:_ Строковые операторы, округление, экстремум, конверсия типов, структуры, пользовательская формула.

```
Ты дорабатываешь документацию lsFusion по блоку B06 «Строки/округление/экстремум/конверсия/структуры» (трек T1 — Логическая модель — данные и выражения).

Перед работой прочитай: docs/AGENTS.md (релевантные разделы целиком) и docs-process/DOCS-DESC-PLAN.md.
Источник истины — КОД платформы (LsfLogics.g -> ScriptingLogicsModule.java -> LogicsModule.java -> классы;
примеры в server/src/main/lsfusion; плагин — вторичен). Не описывай по памяти.
Работай в выданной под блок ветке (docs/B06 от master) — создай её ПЕРВЫМ действием и в ОТДЕЛЬНОМ рабочем дереве (git worktree add), не в общем чекауте: иначе параллельные блоки/исполнители молча затрут несохранённое. Коммить в ветку рано (commit-first); в master сам не мерджи и master не пушь.
EN и RU — построчно синхронные переводы одного текста (одна структура и состав информации).
Tier-чистота: Paradigm — абстракция (как будто синтаксиса/расширений нет); Language — синтаксис/механика
именно этой абстракции, ссылается на Paradigm и не переизлагает её. Не смешивать tier'ы.

ДВА ПОСЛЕДОВАТЕЛЬНЫХ ПОД-ЦИКЛА: сначала ЦЕЛИКОМ Paradigm, потом ЦЕЛИКОМ Language. Каждый под-цикл =
описание + СВОЙ коммит + ЗАВЕРШЁННОЕ ревью. Language не начинать, пока под-цикл Paradigm не закрыт.

**Paradigm (доописать обе версии, en+ru построчно синхронно; если статьи ещё нет — создать оба файла):**
- `docs/paradigm/en/String_operators_plus_CONCAT_SUBSTRING.md` + `docs/paradigm/ru/String_operators_plus_CONCAT_SUBSTRING.md`
- `docs/paradigm/en/Rounding_operator_ROUND.md` + `docs/paradigm/ru/Rounding_operator_ROUND.md`
- `docs/paradigm/en/Extremum_MAX_MIN.md` + `docs/paradigm/ru/Extremum_MAX_MIN.md`
- `docs/paradigm/en/Type_conversion.md` + `docs/paradigm/ru/Type_conversion.md`
- `docs/paradigm/en/Structure_operators_STRUCT.md` + `docs/paradigm/ru/Structure_operators_STRUCT.md`
- `docs/paradigm/en/Custom_formula_FORMULA.md` + `docs/paradigm/ru/Custom_formula_FORMULA.md`

**Language (доописать обе версии, en+ru построчно синхронно; если статьи ещё нет — создать оба файла):**
- `docs/language/en/CONCAT_operator.md` + `docs/language/ru/CONCAT_operator.md`
- `docs/language/en/ROUND_operator.md` + `docs/language/ru/ROUND_operator.md`
- `docs/language/en/MAX_operator.md` + `docs/language/ru/MAX_operator.md`
- `docs/language/en/MIN_operator.md` + `docs/language/ru/MIN_operator.md`
- `docs/language/en/Type_conversion_operator.md` + `docs/language/ru/Type_conversion_operator.md`
- `docs/language/en/STRUCT_operator.md` + `docs/language/ru/STRUCT_operator.md`
- `docs/language/en/FORMULA_operator.md` + `docs/language/ru/FORMULA_operator.md`
- `docs/language/en/JSON_operator.md` + `docs/language/ru/JSON_operator.md`
- `docs/language/en/LIKE_operator.md` + `docs/language/ru/LIKE_operator.md`
- `docs/language/en/MATCH_operator.md` + `docs/language/ru/MATCH_operator.md`
- `docs/language/en/Brackets_operator.md` + `docs/language/ru/Brackets_operator.md`

ЦИКЛ СДАЧИ на КАЖДЫЙ под-цикл (commit-first внутри ветки блока):
  Под-цикл 1 — PARADIGM: коммит 'docs: B06 paradigm — Строки/округление/экстремум/конверсия/структуры'.
  Под-цикл 2 — LANGUAGE (только после ПОЛНОГО закрытия Paradigm): коммит 'docs: B06 language — Строки/округление/экстремум/конверсия/структуры'.
  Шаги под-цикла:
  1. Написать/создать файлы под-цикла -> коммит (subject выше).
  2. self-review (явная сверка с каждым релевантным правилом AGENTS.md; полнота по коду/дочерним;
     EN/RU parity; направление cross-link'ов; tier-чистота) + codex review с master:docs/AGENTS.md;
     правки отдельными коммитами до чистого прохода. Это твой контроль ПЕРЕД показом человеку.
  3. РЕВЬЮ ЧЕЛОВЕКОМ: отдаёшь результат человеку (исполнитель/оркестратор) — он даёт замечания.
  4. Исправляешь замечания ОТДЕЛЬНЫМИ коммитами (при необходимости снова self/codex) и возвращаешь на ревью.
  5. Повторяешь шаги 3-4, пока у человека НЕ ОСТАНЕТСЯ претензий — только тогда под-цикл закрыт.

Если конструкции блока ещё нет статьи — создать файлы en+ru в нужной type-папке (docs/paradigm/ или
docs/language/) и сам подключить их в docs/sidebars.js тем же блочным коммитом. Нашёл обобщаемое правило/проблему —
допиши его в AGENTS.md ОТДЕЛЬНЫМ коммитом (AGENTS.md дописывать может любой; это файл для ИИ).
CLAUDE.md в коммиты блока НЕ включать; docs/sidebars.js — НАОБОРОТ, включать в блочный коммит (это часть документации). Ветку блока пушишь сам (для ревью); merge ветки
в master (squash по необходимости) делает оркестратор после закрытия обоих под-циклов.
```

## B07 — Композиция (JOIN)  ·  [P+L] · слой 1 · трек T1 · зона: Логическая модель

_Scope:_ Композиция свойств/действий.

```
Ты дорабатываешь документацию lsFusion по блоку B07 «Композиция (JOIN)» (трек T1 — Логическая модель — данные и выражения).

Перед работой прочитай: docs/AGENTS.md (релевантные разделы целиком) и docs-process/DOCS-DESC-PLAN.md.
Источник истины — КОД платформы (LsfLogics.g -> ScriptingLogicsModule.java -> LogicsModule.java -> классы;
примеры в server/src/main/lsfusion; плагин — вторичен). Не описывай по памяти.
Работай в выданной под блок ветке (docs/B07 от master) — создай её ПЕРВЫМ действием и в ОТДЕЛЬНОМ рабочем дереве (git worktree add), не в общем чекауте: иначе параллельные блоки/исполнители молча затрут несохранённое. Коммить в ветку рано (commit-first); в master сам не мерджи и master не пушь.
EN и RU — построчно синхронные переводы одного текста (одна структура и состав информации).
Tier-чистота: Paradigm — абстракция (как будто синтаксиса/расширений нет); Language — синтаксис/механика
именно этой абстракции, ссылается на Paradigm и не переизлагает её. Не смешивать tier'ы.

ДВА ПОСЛЕДОВАТЕЛЬНЫХ ПОД-ЦИКЛА: сначала ЦЕЛИКОМ Paradigm, потом ЦЕЛИКОМ Language. Каждый под-цикл =
описание + СВОЙ коммит + ЗАВЕРШЁННОЕ ревью. Language не начинать, пока под-цикл Paradigm не закрыт.

**Paradigm (доописать обе версии, en+ru построчно синхронно; если статьи ещё нет — создать оба файла):**
- `docs/paradigm/en/Composition_JOIN.md` + `docs/paradigm/ru/Composition_JOIN.md`

**Language (доописать обе версии, en+ru построчно синхронно; если статьи ещё нет — создать оба файла):**
- `docs/language/en/JOIN_operator.md` + `docs/language/ru/JOIN_operator.md`

ЦИКЛ СДАЧИ на КАЖДЫЙ под-цикл (commit-first внутри ветки блока):
  Под-цикл 1 — PARADIGM: коммит 'docs: B07 paradigm — Композиция (JOIN)'.
  Под-цикл 2 — LANGUAGE (только после ПОЛНОГО закрытия Paradigm): коммит 'docs: B07 language — Композиция (JOIN)'.
  Шаги под-цикла:
  1. Написать/создать файлы под-цикла -> коммит (subject выше).
  2. self-review (явная сверка с каждым релевантным правилом AGENTS.md; полнота по коду/дочерним;
     EN/RU parity; направление cross-link'ов; tier-чистота) + codex review с master:docs/AGENTS.md;
     правки отдельными коммитами до чистого прохода. Это твой контроль ПЕРЕД показом человеку.
  3. РЕВЬЮ ЧЕЛОВЕКОМ: отдаёшь результат человеку (исполнитель/оркестратор) — он даёт замечания.
  4. Исправляешь замечания ОТДЕЛЬНЫМИ коммитами (при необходимости снова self/codex) и возвращаешь на ревью.
  5. Повторяешь шаги 3-4, пока у человека НЕ ОСТАНЕТСЯ претензий — только тогда под-цикл закрыт.

Если конструкции блока ещё нет статьи — создать файлы en+ru в нужной type-папке (docs/paradigm/ или
docs/language/) и сам подключить их в docs/sidebars.js тем же блочным коммитом. Нашёл обобщаемое правило/проблему —
допиши его в AGENTS.md ОТДЕЛЬНЫМ коммитом (AGENTS.md дописывать может любой; это файл для ИИ).
CLAUDE.md в коммиты блока НЕ включать; docs/sidebars.js — НАОБОРОТ, включать в блочный коммит (это часть документации). Ветку блока пушишь сам (для ревью); merge ветки
в master (squash по необходимости) делает оркестратор после закрытия обоих под-циклов.
```

## B08 — Выборка (CASE/IF/MULTI/OVERRIDE/EXCLUSIVE)  ·  [P+L] · слой 1 · трек T1 · зона: Логическая модель

_Scope:_ Операторы выборки значения.

```
Ты дорабатываешь документацию lsFusion по блоку B08 «Выборка (CASE/IF/MULTI/OVERRIDE/EXCLUSIVE)» (трек T1 — Логическая модель — данные и выражения).

Перед работой прочитай: docs/AGENTS.md (релевантные разделы целиком) и docs-process/DOCS-DESC-PLAN.md.
Источник истины — КОД платформы (LsfLogics.g -> ScriptingLogicsModule.java -> LogicsModule.java -> классы;
примеры в server/src/main/lsfusion; плагин — вторичен). Не описывай по памяти.
Работай в выданной под блок ветке (docs/B08 от master) — создай её ПЕРВЫМ действием и в ОТДЕЛЬНОМ рабочем дереве (git worktree add), не в общем чекауте: иначе параллельные блоки/исполнители молча затрут несохранённое. Коммить в ветку рано (commit-first); в master сам не мерджи и master не пушь.
EN и RU — построчно синхронные переводы одного текста (одна структура и состав информации).
Tier-чистота: Paradigm — абстракция (как будто синтаксиса/расширений нет); Language — синтаксис/механика
именно этой абстракции, ссылается на Paradigm и не переизлагает её. Не смешивать tier'ы.

ДВА ПОСЛЕДОВАТЕЛЬНЫХ ПОД-ЦИКЛА: сначала ЦЕЛИКОМ Paradigm, потом ЦЕЛИКОМ Language. Каждый под-цикл =
описание + СВОЙ коммит + ЗАВЕРШЁННОЕ ревью. Language не начинать, пока под-цикл Paradigm не закрыт.

**Paradigm (доописать обе версии, en+ru построчно синхронно; если статьи ещё нет — создать оба файла):**
- `docs/paradigm/en/Selection_CASE_IF_MULTI_OVERRIDE_EXCLUSIVE.md` + `docs/paradigm/ru/Selection_CASE_IF_MULTI_OVERRIDE_EXCLUSIVE.md`

**Language (доописать обе версии, en+ru построчно синхронно; если статьи ещё нет — создать оба файла):**
- `docs/language/en/CASE_operator.md` + `docs/language/ru/CASE_operator.md`
- `docs/language/en/IF_operator.md` + `docs/language/ru/IF_operator.md`
- `docs/language/en/IF_..._THEN_operator.md` + `docs/language/ru/IF_..._THEN_operator.md`
- `docs/language/en/MULTI_operator.md` + `docs/language/ru/MULTI_operator.md`
- `docs/language/en/OVERRIDE_operator.md` + `docs/language/ru/OVERRIDE_operator.md`
- `docs/language/en/EXCLUSIVE_operator.md` + `docs/language/ru/EXCLUSIVE_operator.md`

ЦИКЛ СДАЧИ на КАЖДЫЙ под-цикл (commit-first внутри ветки блока):
  Под-цикл 1 — PARADIGM: коммит 'docs: B08 paradigm — Выборка (CASE/IF/MULTI/OVERRIDE/EXCLUSIVE)'.
  Под-цикл 2 — LANGUAGE (только после ПОЛНОГО закрытия Paradigm): коммит 'docs: B08 language — Выборка (CASE/IF/MULTI/OVERRIDE/EXCLUSIVE)'.
  Шаги под-цикла:
  1. Написать/создать файлы под-цикла -> коммит (subject выше).
  2. self-review (явная сверка с каждым релевантным правилом AGENTS.md; полнота по коду/дочерним;
     EN/RU parity; направление cross-link'ов; tier-чистота) + codex review с master:docs/AGENTS.md;
     правки отдельными коммитами до чистого прохода. Это твой контроль ПЕРЕД показом человеку.
  3. РЕВЬЮ ЧЕЛОВЕКОМ: отдаёшь результат человеку (исполнитель/оркестратор) — он даёт замечания.
  4. Исправляешь замечания ОТДЕЛЬНЫМИ коммитами (при необходимости снова self/codex) и возвращаешь на ревью.
  5. Повторяешь шаги 3-4, пока у человека НЕ ОСТАНЕТСЯ претензий — только тогда под-цикл закрыт.

Если конструкции блока ещё нет статьи — создать файлы en+ru в нужной type-папке (docs/paradigm/ или
docs/language/) и сам подключить их в docs/sidebars.js тем же блочным коммитом. Нашёл обобщаемое правило/проблему —
допиши его в AGENTS.md ОТДЕЛЬНЫМ коммитом (AGENTS.md дописывать может любой; это файл для ИИ).
CLAUDE.md в коммиты блока НЕ включать; docs/sidebars.js — НАОБОРОТ, включать в блочный коммит (это часть документации). Ветку блока пушишь сам (для ревью); merge ветки
в master (squash по необходимости) делает оркестратор после закрытия обоих под-циклов.
```

## B09 — Фильтрация и порядок  ·  [P] · слой 1 · трек T1 · зона: Логическая модель

_Scope:_ FILTER и ORDER как абстракции (синтаксис описан внутри операторов, их использующих).

```
Ты дорабатываешь документацию lsFusion по блоку B09 «Фильтрация и порядок» (трек T1 — Логическая модель — данные и выражения).

Перед работой прочитай: docs/AGENTS.md (релевантные разделы целиком) и docs-process/DOCS-DESC-PLAN.md.
Источник истины — КОД платформы (LsfLogics.g -> ScriptingLogicsModule.java -> LogicsModule.java -> классы;
примеры в server/src/main/lsfusion; плагин — вторичен). Не описывай по памяти.
Работай в выданной под блок ветке (docs/B09 от master) — создай её ПЕРВЫМ действием и в ОТДЕЛЬНОМ рабочем дереве (git worktree add), не в общем чекауте: иначе параллельные блоки/исполнители молча затрут несохранённое. Коммить в ветку рано (commit-first); в master сам не мерджи и master не пушь.
EN и RU — построчно синхронные переводы одного текста (одна структура и состав информации).
Tier-чистота: Paradigm — абстракция (как будто синтаксиса/расширений нет); Language — синтаксис/механика
именно этой абстракции, ссылается на Paradigm и не переизлагает её. Не смешивать tier'ы.

Блок только tier'а Paradigm — один под-цикл.

**Paradigm (доописать обе версии, en+ru построчно синхронно; если статьи ещё нет — создать оба файла):**
- `docs/paradigm/en/Filter_FILTER.md` + `docs/paradigm/ru/Filter_FILTER.md`
- `docs/paradigm/en/Order_ORDER.md` + `docs/paradigm/ru/Order_ORDER.md`

ЦИКЛ СДАЧИ на КАЖДЫЙ под-цикл (commit-first внутри ветки блока):
  Один под-цикл — PARADIGM: коммит 'docs: B09 paradigm — Фильтрация и порядок'.
  Шаги под-цикла:
  1. Написать/создать файлы под-цикла -> коммит (subject выше).
  2. self-review (явная сверка с каждым релевантным правилом AGENTS.md; полнота по коду/дочерним;
     EN/RU parity; направление cross-link'ов; tier-чистота) + codex review с master:docs/AGENTS.md;
     правки отдельными коммитами до чистого прохода. Это твой контроль ПЕРЕД показом человеку.
  3. РЕВЬЮ ЧЕЛОВЕКОМ: отдаёшь результат человеку (исполнитель/оркестратор) — он даёт замечания.
  4. Исправляешь замечания ОТДЕЛЬНЫМИ коммитами (при необходимости снова self/codex) и возвращаешь на ревью.
  5. Повторяешь шаги 3-4, пока у человека НЕ ОСТАНЕТСЯ претензий — только тогда под-цикл закрыт.

Если конструкции блока ещё нет статьи — создать файлы en+ru в нужной type-папке (docs/paradigm/ или
docs/language/) и сам подключить их в docs/sidebars.js тем же блочным коммитом. Нашёл обобщаемое правило/проблему —
допиши его в AGENTS.md ОТДЕЛЬНЫМ коммитом (AGENTS.md дописывать может любой; это файл для ИИ).
CLAUDE.md в коммиты блока НЕ включать; docs/sidebars.js — НАОБОРОТ, включать в блочный коммит (это часть документации). Ветку блока пушишь сам (для ревью); merge ветки
в master (squash по необходимости) делает оркестратор после закрытия обоих под-циклов.
```

## B03 — Свойства и данные  ·  [P+L] · слой 2 · трек T1 · зона: Логическая модель

_Scope:_ Свойства, data-свойства, опции свойств.

```
Ты дорабатываешь документацию lsFusion по блоку B03 «Свойства и данные» (трек T1 — Логическая модель — данные и выражения).

Перед работой прочитай: docs/AGENTS.md (релевантные разделы целиком) и docs-process/DOCS-DESC-PLAN.md.
Источник истины — КОД платформы (LsfLogics.g -> ScriptingLogicsModule.java -> LogicsModule.java -> классы;
примеры в server/src/main/lsfusion; плагин — вторичен). Не описывай по памяти.
Работай в выданной под блок ветке (docs/B03 от master) — создай её ПЕРВЫМ действием и в ОТДЕЛЬНОМ рабочем дереве (git worktree add), не в общем чекауте: иначе параллельные блоки/исполнители молча затрут несохранённое. Коммить в ветку рано (commit-first); в master сам не мерджи и master не пушь.
ВАЖНО: это обзорный/корневой блок. Его источник истины — уже описанные ДОЧЕРНИЕ статьи (их состав
и термины) и дерево docs/sidebars.js, в большей степени чем код. Обзорная статья вводит и связывает
дочерние, а не пересказывает реализацию. Не бери этот блок, пока его дочерние блоки не описаны.
EN и RU — построчно синхронные переводы одного текста (одна структура и состав информации).
Tier-чистота: Paradigm — абстракция (как будто синтаксиса/расширений нет); Language — синтаксис/механика
именно этой абстракции, ссылается на Paradigm и не переизлагает её. Не смешивать tier'ы.

ДВА ПОСЛЕДОВАТЕЛЬНЫХ ПОД-ЦИКЛА: сначала ЦЕЛИКОМ Paradigm, потом ЦЕЛИКОМ Language. Каждый под-цикл =
описание + СВОЙ коммит + ЗАВЕРШЁННОЕ ревью. Language не начинать, пока под-цикл Paradigm не закрыт.

**Paradigm (доописать обе версии, en+ru построчно синхронно; если статьи ещё нет — создать оба файла):**
- `docs/paradigm/en/Properties.md` + `docs/paradigm/ru/Properties.md`
- `docs/paradigm/en/Data_properties_DATA.md` + `docs/paradigm/ru/Data_properties_DATA.md`

**Language (доописать обе версии, en+ru построчно синхронно; если статьи ещё нет — создать оба файла):**
- `docs/language/en/DATA_operator.md` + `docs/language/ru/DATA_operator.md`
- `docs/language/en/Property_options.md` + `docs/language/ru/Property_options.md`

ЦИКЛ СДАЧИ на КАЖДЫЙ под-цикл (commit-first внутри ветки блока):
  Под-цикл 1 — PARADIGM: коммит 'docs: B03 paradigm — Свойства и данные'.
  Под-цикл 2 — LANGUAGE (только после ПОЛНОГО закрытия Paradigm): коммит 'docs: B03 language — Свойства и данные'.
  Шаги под-цикла:
  1. Написать/создать файлы под-цикла -> коммит (subject выше).
  2. self-review (явная сверка с каждым релевантным правилом AGENTS.md; полнота по коду/дочерним;
     EN/RU parity; направление cross-link'ов; tier-чистота) + codex review с master:docs/AGENTS.md;
     правки отдельными коммитами до чистого прохода. Это твой контроль ПЕРЕД показом человеку.
  3. РЕВЬЮ ЧЕЛОВЕКОМ: отдаёшь результат человеку (исполнитель/оркестратор) — он даёт замечания.
  4. Исправляешь замечания ОТДЕЛЬНЫМИ коммитами (при необходимости снова self/codex) и возвращаешь на ревью.
  5. Повторяешь шаги 3-4, пока у человека НЕ ОСТАНЕТСЯ претензий — только тогда под-цикл закрыт.

Если конструкции блока ещё нет статьи — создать файлы en+ru в нужной type-папке (docs/paradigm/ или
docs/language/) и сам подключить их в docs/sidebars.js тем же блочным коммитом. Нашёл обобщаемое правило/проблему —
допиши его в AGENTS.md ОТДЕЛЬНЫМ коммитом (AGENTS.md дописывать может любой; это файл для ИИ).
CLAUDE.md в коммиты блока НЕ включать; docs/sidebars.js — НАОБОРОТ, включать в блочный коммит (это часть документации). Ветку блока пушишь сам (для ревью); merge ветки
в master (squash по необходимости) делает оркестратор после закрытия обоих под-циклов.
```

## B04 — Операторы-выражения: основа  ·  [P+L] · слой 2 · трек T1 · зона: Логическая модель

_Scope:_ Зонтик операторов-выражения и операций над примитивами + грамматика выражения, приоритеты, объявление через =.

```
Ты дорабатываешь документацию lsFusion по блоку B04 «Операторы-выражения: основа» (трек T1 — Логическая модель — данные и выражения).

Перед работой прочитай: docs/AGENTS.md (релевантные разделы целиком) и docs-process/DOCS-DESC-PLAN.md.
Источник истины — КОД платформы (LsfLogics.g -> ScriptingLogicsModule.java -> LogicsModule.java -> классы;
примеры в server/src/main/lsfusion; плагин — вторичен). Не описывай по памяти.
Работай в выданной под блок ветке (docs/B04 от master) — создай её ПЕРВЫМ действием и в ОТДЕЛЬНОМ рабочем дереве (git worktree add), не в общем чекауте: иначе параллельные блоки/исполнители молча затрут несохранённое. Коммить в ветку рано (commit-first); в master сам не мерджи и master не пушь.
ВАЖНО: это обзорный/корневой блок. Его источник истины — уже описанные ДОЧЕРНИЕ статьи (их состав
и термины) и дерево docs/sidebars.js, в большей степени чем код. Обзорная статья вводит и связывает
дочерние, а не пересказывает реализацию. Не бери этот блок, пока его дочерние блоки не описаны.
EN и RU — построчно синхронные переводы одного текста (одна структура и состав информации).
Tier-чистота: Paradigm — абстракция (как будто синтаксиса/расширений нет); Language — синтаксис/механика
именно этой абстракции, ссылается на Paradigm и не переизлагает её. Не смешивать tier'ы.

ДВА ПОСЛЕДОВАТЕЛЬНЫХ ПОД-ЦИКЛА: сначала ЦЕЛИКОМ Paradigm, потом ЦЕЛИКОМ Language. Каждый под-цикл =
описание + СВОЙ коммит + ЗАВЕРШЁННОЕ ревью. Language не начинать, пока под-цикл Paradigm не закрыт.

**Paradigm (доописать обе версии, en+ru построчно синхронно; если статьи ещё нет — создать оба файла):**
- `docs/paradigm/en/Property_operators_paradigm.md` + `docs/paradigm/ru/Property_operators_paradigm.md`
- `docs/paradigm/en/Operations_with_primitives.md` + `docs/paradigm/ru/Operations_with_primitives.md`

**Language (доописать обе версии, en+ru построчно синхронно; если статьи ещё нет — создать оба файла):**
- `docs/language/en/Property_operators.md` + `docs/language/ru/Property_operators.md`
- `docs/language/en/Expression.md` + `docs/language/ru/Expression.md`
- `docs/language/en/Operator_priority.md` + `docs/language/ru/Operator_priority.md`
- `docs/language/en/=_statement.md` + `docs/language/ru/=_statement.md`

ЦИКЛ СДАЧИ на КАЖДЫЙ под-цикл (commit-first внутри ветки блока):
  Под-цикл 1 — PARADIGM: коммит 'docs: B04 paradigm — Операторы-выражения: основа'.
  Под-цикл 2 — LANGUAGE (только после ПОЛНОГО закрытия Paradigm): коммит 'docs: B04 language — Операторы-выражения: основа'.
  Шаги под-цикла:
  1. Написать/создать файлы под-цикла -> коммит (subject выше).
  2. self-review (явная сверка с каждым релевантным правилом AGENTS.md; полнота по коду/дочерним;
     EN/RU parity; направление cross-link'ов; tier-чистота) + codex review с master:docs/AGENTS.md;
     правки отдельными коммитами до чистого прохода. Это твой контроль ПЕРЕД показом человеку.
  3. РЕВЬЮ ЧЕЛОВЕКОМ: отдаёшь результат человеку (исполнитель/оркестратор) — он даёт замечания.
  4. Исправляешь замечания ОТДЕЛЬНЫМИ коммитами (при необходимости снова self/codex) и возвращаешь на ревью.
  5. Повторяешь шаги 3-4, пока у человека НЕ ОСТАНЕТСЯ претензий — только тогда под-цикл закрыт.

Если конструкции блока ещё нет статьи — создать файлы en+ru в нужной type-папке (docs/paradigm/ или
docs/language/) и сам подключить их в docs/sidebars.js тем же блочным коммитом. Нашёл обобщаемое правило/проблему —
допиши его в AGENTS.md ОТДЕЛЬНЫМ коммитом (AGENTS.md дописывать может любой; это файл для ИИ).
CLAUDE.md в коммиты блока НЕ включать; docs/sidebars.js — НАОБОРОТ, включать в блочный коммит (это часть документации). Ветку блока пушишь сам (для ревью); merge ветки
в master (squash по необходимости) делает оркестратор после закрытия обоих под-циклов.
```

## B01 — Логическая модель — корень  ·  [P] · слой 3 · трек T1 · зона: Логическая модель

_Scope:_ Корневой обзор логической модели и доменной логики.

```
Ты дорабатываешь документацию lsFusion по блоку B01 «Логическая модель — корень» (трек T1 — Логическая модель — данные и выражения).

Перед работой прочитай: docs/AGENTS.md (релевантные разделы целиком) и docs-process/DOCS-DESC-PLAN.md.
Источник истины — КОД платформы (LsfLogics.g -> ScriptingLogicsModule.java -> LogicsModule.java -> классы;
примеры в server/src/main/lsfusion; плагин — вторичен). Не описывай по памяти.
Работай в выданной под блок ветке (docs/B01 от master) — создай её ПЕРВЫМ действием и в ОТДЕЛЬНОМ рабочем дереве (git worktree add), не в общем чекауте: иначе параллельные блоки/исполнители молча затрут несохранённое. Коммить в ветку рано (commit-first); в master сам не мерджи и master не пушь.
ВАЖНО: это обзорный/корневой блок. Его источник истины — уже описанные ДОЧЕРНИЕ статьи (их состав
и термины) и дерево docs/sidebars.js, в большей степени чем код. Обзорная статья вводит и связывает
дочерние, а не пересказывает реализацию. Не бери этот блок, пока его дочерние блоки не описаны.
EN и RU — построчно синхронные переводы одного текста (одна структура и состав информации).
Tier-чистота: Paradigm — абстракция (как будто синтаксиса/расширений нет); Language — синтаксис/механика
именно этой абстракции, ссылается на Paradigm и не переизлагает её. Не смешивать tier'ы.

Блок только tier'а Paradigm — один под-цикл.

**Paradigm (доописать обе версии, en+ru построчно синхронно; если статьи ещё нет — создать оба файла):**
- `docs/paradigm/en/Logical_model.md` + `docs/paradigm/ru/Logical_model.md`
- `docs/paradigm/en/Domain_logic.md` + `docs/paradigm/ru/Domain_logic.md`

ЦИКЛ СДАЧИ на КАЖДЫЙ под-цикл (commit-first внутри ветки блока):
  Один под-цикл — PARADIGM: коммит 'docs: B01 paradigm — Логическая модель — корень'.
  Шаги под-цикла:
  1. Написать/создать файлы под-цикла -> коммит (subject выше).
  2. self-review (явная сверка с каждым релевантным правилом AGENTS.md; полнота по коду/дочерним;
     EN/RU parity; направление cross-link'ов; tier-чистота) + codex review с master:docs/AGENTS.md;
     правки отдельными коммитами до чистого прохода. Это твой контроль ПЕРЕД показом человеку.
  3. РЕВЬЮ ЧЕЛОВЕКОМ: отдаёшь результат человеку (исполнитель/оркестратор) — он даёт замечания.
  4. Исправляешь замечания ОТДЕЛЬНЫМИ коммитами (при необходимости снова self/codex) и возвращаешь на ревью.
  5. Повторяешь шаги 3-4, пока у человека НЕ ОСТАНЕТСЯ претензий — только тогда под-цикл закрыт.

Если конструкции блока ещё нет статьи — создать файлы en+ru в нужной type-папке (docs/paradigm/ или
docs/language/) и сам подключить их в docs/sidebars.js тем же блочным коммитом. Нашёл обобщаемое правило/проблему —
допиши его в AGENTS.md ОТДЕЛЬНЫМ коммитом (AGENTS.md дописывать может любой; это файл для ИИ).
CLAUDE.md в коммиты блока НЕ включать; docs/sidebars.js — НАОБОРОТ, включать в блочный коммит (это часть документации). Ветку блока пушишь сам (для ревью); merge ветки
в master (squash по необходимости) делает оркестратор после закрытия обоих под-циклов.
```

# Трек T2 — Логическая модель — множества, события, ограничения

## B11 — Группировка и распределение  ·  [P+L] · слой 1 · трек T2 · зона: Логическая модель

_Scope:_ GROUP и UNGROUP.

```
Ты дорабатываешь документацию lsFusion по блоку B11 «Группировка и распределение» (трек T2 — Логическая модель — множества, события, ограничения).

Перед работой прочитай: docs/AGENTS.md (релевантные разделы целиком) и docs-process/DOCS-DESC-PLAN.md.
Источник истины — КОД платформы (LsfLogics.g -> ScriptingLogicsModule.java -> LogicsModule.java -> классы;
примеры в server/src/main/lsfusion; плагин — вторичен). Не описывай по памяти.
Работай в выданной под блок ветке (docs/B11 от master) — создай её ПЕРВЫМ действием и в ОТДЕЛЬНОМ рабочем дереве (git worktree add), не в общем чекауте: иначе параллельные блоки/исполнители молча затрут несохранённое. Коммить в ветку рано (commit-first); в master сам не мерджи и master не пушь.
EN и RU — построчно синхронные переводы одного текста (одна структура и состав информации).
Tier-чистота: Paradigm — абстракция (как будто синтаксиса/расширений нет); Language — синтаксис/механика
именно этой абстракции, ссылается на Paradigm и не переизлагает её. Не смешивать tier'ы.

ДВА ПОСЛЕДОВАТЕЛЬНЫХ ПОД-ЦИКЛА: сначала ЦЕЛИКОМ Paradigm, потом ЦЕЛИКОМ Language. Каждый под-цикл =
описание + СВОЙ коммит + ЗАВЕРШЁННОЕ ревью. Language не начинать, пока под-цикл Paradigm не закрыт.

**Paradigm (доописать обе версии, en+ru построчно синхронно; если статьи ещё нет — создать оба файла):**
- `docs/paradigm/en/Grouping_GROUP.md` + `docs/paradigm/ru/Grouping_GROUP.md`
- `docs/paradigm/en/Distribution_UNGROUP.md` + `docs/paradigm/ru/Distribution_UNGROUP.md`

**Language (доописать обе версии, en+ru построчно синхронно; если статьи ещё нет — создать оба файла):**
- `docs/language/en/GROUP_operator.md` + `docs/language/ru/GROUP_operator.md`
- `docs/language/en/UNGROUP_operator.md` + `docs/language/ru/UNGROUP_operator.md`

ЦИКЛ СДАЧИ на КАЖДЫЙ под-цикл (commit-first внутри ветки блока):
  Под-цикл 1 — PARADIGM: коммит 'docs: B11 paradigm — Группировка и распределение'.
  Под-цикл 2 — LANGUAGE (только после ПОЛНОГО закрытия Paradigm): коммит 'docs: B11 language — Группировка и распределение'.
  Шаги под-цикла:
  1. Написать/создать файлы под-цикла -> коммит (subject выше).
  2. self-review (явная сверка с каждым релевантным правилом AGENTS.md; полнота по коду/дочерним;
     EN/RU parity; направление cross-link'ов; tier-чистота) + codex review с master:docs/AGENTS.md;
     правки отдельными коммитами до чистого прохода. Это твой контроль ПЕРЕД показом человеку.
  3. РЕВЬЮ ЧЕЛОВЕКОМ: отдаёшь результат человеку (исполнитель/оркестратор) — он даёт замечания.
  4. Исправляешь замечания ОТДЕЛЬНЫМИ коммитами (при необходимости снова self/codex) и возвращаешь на ревью.
  5. Повторяешь шаги 3-4, пока у человека НЕ ОСТАНЕТСЯ претензий — только тогда под-цикл закрыт.

Если конструкции блока ещё нет статьи — создать файлы en+ru в нужной type-папке (docs/paradigm/ или
docs/language/) и сам подключить их в docs/sidebars.js тем же блочным коммитом. Нашёл обобщаемое правило/проблему —
допиши его в AGENTS.md ОТДЕЛЬНЫМ коммитом (AGENTS.md дописывать может любой; это файл для ИИ).
CLAUDE.md в коммиты блока НЕ включать; docs/sidebars.js — НАОБОРОТ, включать в блочный коммит (это часть документации). Ветку блока пушишь сам (для ревью); merge ветки
в master (squash по необходимости) делает оркестратор после закрытия обоих под-циклов.
```

## B12 — Партиционирование и рекурсия  ·  [P+L] · слой 1 · трек T2 · зона: Логическая модель

_Scope:_ PARTITION и RECURSION.

```
Ты дорабатываешь документацию lsFusion по блоку B12 «Партиционирование и рекурсия» (трек T2 — Логическая модель — множества, события, ограничения).

Перед работой прочитай: docs/AGENTS.md (релевантные разделы целиком) и docs-process/DOCS-DESC-PLAN.md.
Источник истины — КОД платформы (LsfLogics.g -> ScriptingLogicsModule.java -> LogicsModule.java -> классы;
примеры в server/src/main/lsfusion; плагин — вторичен). Не описывай по памяти.
Работай в выданной под блок ветке (docs/B12 от master) — создай её ПЕРВЫМ действием и в ОТДЕЛЬНОМ рабочем дереве (git worktree add), не в общем чекауте: иначе параллельные блоки/исполнители молча затрут несохранённое. Коммить в ветку рано (commit-first); в master сам не мерджи и master не пушь.
EN и RU — построчно синхронные переводы одного текста (одна структура и состав информации).
Tier-чистота: Paradigm — абстракция (как будто синтаксиса/расширений нет); Language — синтаксис/механика
именно этой абстракции, ссылается на Paradigm и не переизлагает её. Не смешивать tier'ы.

ДВА ПОСЛЕДОВАТЕЛЬНЫХ ПОД-ЦИКЛА: сначала ЦЕЛИКОМ Paradigm, потом ЦЕЛИКОМ Language. Каждый под-цикл =
описание + СВОЙ коммит + ЗАВЕРШЁННОЕ ревью. Language не начинать, пока под-цикл Paradigm не закрыт.

**Paradigm (доописать обе версии, en+ru построчно синхронно; если статьи ещё нет — создать оба файла):**
- `docs/paradigm/en/Partitioning_sorting_PARTITION_..._ORDER.md` + `docs/paradigm/ru/Partitioning_sorting_PARTITION_..._ORDER.md`
- `docs/paradigm/en/Recursion_RECURSION.md` + `docs/paradigm/ru/Recursion_RECURSION.md`

**Language (доописать обе версии, en+ru построчно синхронно; если статьи ещё нет — создать оба файла):**
- `docs/language/en/PARTITION_operator.md` + `docs/language/ru/PARTITION_operator.md`
- `docs/language/en/RECURSION_operator.md` + `docs/language/ru/RECURSION_operator.md`

ЦИКЛ СДАЧИ на КАЖДЫЙ под-цикл (commit-first внутри ветки блока):
  Под-цикл 1 — PARADIGM: коммит 'docs: B12 paradigm — Партиционирование и рекурсия'.
  Под-цикл 2 — LANGUAGE (только после ПОЛНОГО закрытия Paradigm): коммит 'docs: B12 language — Партиционирование и рекурсия'.
  Шаги под-цикла:
  1. Написать/создать файлы под-цикла -> коммит (subject выше).
  2. self-review (явная сверка с каждым релевантным правилом AGENTS.md; полнота по коду/дочерним;
     EN/RU parity; направление cross-link'ов; tier-чистота) + codex review с master:docs/AGENTS.md;
     правки отдельными коммитами до чистого прохода. Это твой контроль ПЕРЕД показом человеку.
  3. РЕВЬЮ ЧЕЛОВЕКОМ: отдаёшь результат человеку (исполнитель/оркестратор) — он даёт замечания.
  4. Исправляешь замечания ОТДЕЛЬНЫМИ коммитами (при необходимости снова self/codex) и возвращаешь на ревью.
  5. Повторяешь шаги 3-4, пока у человека НЕ ОСТАНЕТСЯ претензий — только тогда под-цикл закрыт.

Если конструкции блока ещё нет статьи — создать файлы en+ru в нужной type-папке (docs/paradigm/ или
docs/language/) и сам подключить их в docs/sidebars.js тем же блочным коммитом. Нашёл обобщаемое правило/проблему —
допиши его в AGENTS.md ОТДЕЛЬНЫМ коммитом (AGENTS.md дописывать может любой; это файл для ИИ).
CLAUDE.md в коммиты блока НЕ включать; docs/sidebars.js — НАОБОРОТ, включать в блочный коммит (это часть документации). Ветку блока пушишь сам (для ревью); merge ветки
в master (squash по необходимости) делает оркестратор после закрытия обоих под-циклов.
```

## B13 — Агрегации (AGGR)  ·  [P+L] · слой 1 · трек T2 · зона: Логическая модель

_Scope:_ Агрегации.

```
Ты дорабатываешь документацию lsFusion по блоку B13 «Агрегации (AGGR)» (трек T2 — Логическая модель — множества, события, ограничения).

Перед работой прочитай: docs/AGENTS.md (релевантные разделы целиком) и docs-process/DOCS-DESC-PLAN.md.
Источник истины — КОД платформы (LsfLogics.g -> ScriptingLogicsModule.java -> LogicsModule.java -> классы;
примеры в server/src/main/lsfusion; плагин — вторичен). Не описывай по памяти.
Работай в выданной под блок ветке (docs/B13 от master) — создай её ПЕРВЫМ действием и в ОТДЕЛЬНОМ рабочем дереве (git worktree add), не в общем чекауте: иначе параллельные блоки/исполнители молча затрут несохранённое. Коммить в ветку рано (commit-first); в master сам не мерджи и master не пушь.
EN и RU — построчно синхронные переводы одного текста (одна структура и состав информации).
Tier-чистота: Paradigm — абстракция (как будто синтаксиса/расширений нет); Language — синтаксис/механика
именно этой абстракции, ссылается на Paradigm и не переизлагает её. Не смешивать tier'ы.

ДВА ПОСЛЕДОВАТЕЛЬНЫХ ПОД-ЦИКЛА: сначала ЦЕЛИКОМ Paradigm, потом ЦЕЛИКОМ Language. Каждый под-цикл =
описание + СВОЙ коммит + ЗАВЕРШЁННОЕ ревью. Language не начинать, пока под-цикл Paradigm не закрыт.

**Paradigm (доописать обе версии, en+ru построчно синхронно; если статьи ещё нет — создать оба файла):**
- `docs/paradigm/en/Aggregations.md` + `docs/paradigm/ru/Aggregations.md`

**Language (доописать обе версии, en+ru построчно синхронно; если статьи ещё нет — создать оба файла):**
- `docs/language/en/AGGR_operator.md` + `docs/language/ru/AGGR_operator.md`

ЦИКЛ СДАЧИ на КАЖДЫЙ под-цикл (commit-first внутри ветки блока):
  Под-цикл 1 — PARADIGM: коммит 'docs: B13 paradigm — Агрегации (AGGR)'.
  Под-цикл 2 — LANGUAGE (только после ПОЛНОГО закрытия Paradigm): коммит 'docs: B13 language — Агрегации (AGGR)'.
  Шаги под-цикла:
  1. Написать/создать файлы под-цикла -> коммит (subject выше).
  2. self-review (явная сверка с каждым релевантным правилом AGENTS.md; полнота по коду/дочерним;
     EN/RU parity; направление cross-link'ов; tier-чистота) + codex review с master:docs/AGENTS.md;
     правки отдельными коммитами до чистого прохода. Это твой контроль ПЕРЕД показом человеку.
  3. РЕВЬЮ ЧЕЛОВЕКОМ: отдаёшь результат человеку (исполнитель/оркестратор) — он даёт замечания.
  4. Исправляешь замечания ОТДЕЛЬНЫМИ коммитами (при необходимости снова self/codex) и возвращаешь на ревью.
  5. Повторяешь шаги 3-4, пока у человека НЕ ОСТАНЕТСЯ претензий — только тогда под-цикл закрыт.

Если конструкции блока ещё нет статьи — создать файлы en+ru в нужной type-папке (docs/paradigm/ или
docs/language/) и сам подключить их в docs/sidebars.js тем же блочным коммитом. Нашёл обобщаемое правило/проблему —
допиши его в AGENTS.md ОТДЕЛЬНЫМ коммитом (AGENTS.md дописывать может любой; это файл для ИИ).
CLAUDE.md в коммиты блока НЕ включать; docs/sidebars.js — НАОБОРОТ, включать в блочный коммит (это часть документации). Ветку блока пушишь сам (для ревью); merge ветки
в master (squash по необходимости) делает оркестратор после закрытия обоих под-циклов.
```

## B14 — События  ·  [P+L] · слой 1 · трек T2 · зона: Логическая модель

_Scope:_ События, вычисляемые/простые события, блоки событий.

```
Ты дорабатываешь документацию lsFusion по блоку B14 «События» (трек T2 — Логическая модель — множества, события, ограничения).

Перед работой прочитай: docs/AGENTS.md (релевантные разделы целиком) и docs-process/DOCS-DESC-PLAN.md.
Источник истины — КОД платформы (LsfLogics.g -> ScriptingLogicsModule.java -> LogicsModule.java -> классы;
примеры в server/src/main/lsfusion; плагин — вторичен). Не описывай по памяти.
Работай в выданной под блок ветке (docs/B14 от master) — создай её ПЕРВЫМ действием и в ОТДЕЛЬНОМ рабочем дереве (git worktree add), не в общем чекауте: иначе параллельные блоки/исполнители молча затрут несохранённое. Коммить в ветку рано (commit-first); в master сам не мерджи и master не пушь.
EN и RU — построчно синхронные переводы одного текста (одна структура и состав информации).
Tier-чистота: Paradigm — абстракция (как будто синтаксиса/расширений нет); Language — синтаксис/механика
именно этой абстракции, ссылается на Paradigm и не переизлагает её. Не смешивать tier'ы.

ДВА ПОСЛЕДОВАТЕЛЬНЫХ ПОД-ЦИКЛА: сначала ЦЕЛИКОМ Paradigm, потом ЦЕЛИКОМ Language. Каждый под-цикл =
описание + СВОЙ коммит + ЗАВЕРШЁННОЕ ревью. Language не начинать, пока под-цикл Paradigm не закрыт.

**Paradigm (доописать обе версии, en+ru построчно синхронно; если статьи ещё нет — создать оба файла):**
- `docs/paradigm/en/Events.md` + `docs/paradigm/ru/Events.md`
- `docs/paradigm/en/Calculated_events.md` + `docs/paradigm/ru/Calculated_events.md`
- `docs/paradigm/en/Simple_event.md` + `docs/paradigm/ru/Simple_event.md`

**Language (доописать обе версии, en+ru построчно синхронно; если статьи ещё нет — создать оба файла):**
- `docs/language/en/WHEN_statement.md` + `docs/language/ru/WHEN_statement.md`
- `docs/language/en/ON_statement.md` + `docs/language/ru/ON_statement.md`
- `docs/language/en/AFTER_statement.md` + `docs/language/ru/AFTER_statement.md`
- `docs/language/en/BEFORE_statement.md` + `docs/language/ru/BEFORE_statement.md`
- `docs/language/en/Event_block.md` + `docs/language/ru/Event_block.md`
- `docs/language/en/lt-_WHEN_statement.md` + `docs/language/ru/lt-_WHEN_statement.md`
- `docs/language/en/Event_description_block.md` + `docs/language/ru/Event_description_block.md`

ЦИКЛ СДАЧИ на КАЖДЫЙ под-цикл (commit-first внутри ветки блока):
  Под-цикл 1 — PARADIGM: коммит 'docs: B14 paradigm — События'.
  Под-цикл 2 — LANGUAGE (только после ПОЛНОГО закрытия Paradigm): коммит 'docs: B14 language — События'.
  Шаги под-цикла:
  1. Написать/создать файлы под-цикла -> коммит (subject выше).
  2. self-review (явная сверка с каждым релевантным правилом AGENTS.md; полнота по коду/дочерним;
     EN/RU parity; направление cross-link'ов; tier-чистота) + codex review с master:docs/AGENTS.md;
     правки отдельными коммитами до чистого прохода. Это твой контроль ПЕРЕД показом человеку.
  3. РЕВЬЮ ЧЕЛОВЕКОМ: отдаёшь результат человеку (исполнитель/оркестратор) — он даёт замечания.
  4. Исправляешь замечания ОТДЕЛЬНЫМИ коммитами (при необходимости снова self/codex) и возвращаешь на ревью.
  5. Повторяешь шаги 3-4, пока у человека НЕ ОСТАНЕТСЯ претензий — только тогда под-цикл закрыт.

Если конструкции блока ещё нет статьи — создать файлы en+ru в нужной type-папке (docs/paradigm/ или
docs/language/) и сам подключить их в docs/sidebars.js тем же блочным коммитом. Нашёл обобщаемое правило/проблему —
допиши его в AGENTS.md ОТДЕЛЬНЫМ коммитом (AGENTS.md дописывать может любой; это файл для ИИ).
CLAUDE.md в коммиты блока НЕ включать; docs/sidebars.js — НАОБОРОТ, включать в блочный коммит (это часть документации). Ветку блока пушишь сам (для ревью); merge ветки
в master (squash по необходимости) делает оркестратор после закрытия обоих под-циклов.
```

## B15 — Ограничения  ·  [P+L] · слой 1 · трек T2 · зона: Логическая модель

_Scope:_ Ограничения, простые ограничения, оператор-следствие =>.

```
Ты дорабатываешь документацию lsFusion по блоку B15 «Ограничения» (трек T2 — Логическая модель — множества, события, ограничения).

Перед работой прочитай: docs/AGENTS.md (релевантные разделы целиком) и docs-process/DOCS-DESC-PLAN.md.
Источник истины — КОД платформы (LsfLogics.g -> ScriptingLogicsModule.java -> LogicsModule.java -> классы;
примеры в server/src/main/lsfusion; плагин — вторичен). Не описывай по памяти.
Работай в выданной под блок ветке (docs/B15 от master) — создай её ПЕРВЫМ действием и в ОТДЕЛЬНОМ рабочем дереве (git worktree add), не в общем чекауте: иначе параллельные блоки/исполнители молча затрут несохранённое. Коммить в ветку рано (commit-first); в master сам не мерджи и master не пушь.
EN и RU — построчно синхронные переводы одного текста (одна структура и состав информации).
Tier-чистота: Paradigm — абстракция (как будто синтаксиса/расширений нет); Language — синтаксис/механика
именно этой абстракции, ссылается на Paradigm и не переизлагает её. Не смешивать tier'ы.

ДВА ПОСЛЕДОВАТЕЛЬНЫХ ПОД-ЦИКЛА: сначала ЦЕЛИКОМ Paradigm, потом ЦЕЛИКОМ Language. Каждый под-цикл =
описание + СВОЙ коммит + ЗАВЕРШЁННОЕ ревью. Language не начинать, пока под-цикл Paradigm не закрыт.

**Paradigm (доописать обе версии, en+ru построчно синхронно; если статьи ещё нет — создать оба файла):**
- `docs/paradigm/en/Constraints.md` + `docs/paradigm/ru/Constraints.md`
- `docs/paradigm/en/Simple_constraints.md` + `docs/paradigm/ru/Simple_constraints.md`

**Language (доописать обе версии, en+ru построчно синхронно; если статьи ещё нет — создать оба файла):**
- `docs/language/en/CONSTRAINT_statement.md` + `docs/language/ru/CONSTRAINT_statement.md`
- `docs/language/en/=gt_statement.md` + `docs/language/ru/=gt_statement.md`

ЦИКЛ СДАЧИ на КАЖДЫЙ под-цикл (commit-first внутри ветки блока):
  Под-цикл 1 — PARADIGM: коммит 'docs: B15 paradigm — Ограничения'.
  Под-цикл 2 — LANGUAGE (только после ПОЛНОГО закрытия Paradigm): коммит 'docs: B15 language — Ограничения'.
  Шаги под-цикла:
  1. Написать/создать файлы под-цикла -> коммит (subject выше).
  2. self-review (явная сверка с каждым релевантным правилом AGENTS.md; полнота по коду/дочерним;
     EN/RU parity; направление cross-link'ов; tier-чистота) + codex review с master:docs/AGENTS.md;
     правки отдельными коммитами до чистого прохода. Это твой контроль ПЕРЕД показом человеку.
  3. РЕВЬЮ ЧЕЛОВЕКОМ: отдаёшь результат человеку (исполнитель/оркестратор) — он даёт замечания.
  4. Исправляешь замечания ОТДЕЛЬНЫМИ коммитами (при необходимости снова self/codex) и возвращаешь на ревью.
  5. Повторяешь шаги 3-4, пока у человека НЕ ОСТАНЕТСЯ претензий — только тогда под-цикл закрыт.

Если конструкции блока ещё нет статьи — создать файлы en+ru в нужной type-папке (docs/paradigm/ или
docs/language/) и сам подключить их в docs/sidebars.js тем же блочным коммитом. Нашёл обобщаемое правило/проблему —
допиши его в AGENTS.md ОТДЕЛЬНЫМ коммитом (AGENTS.md дописывать может любой; это файл для ИИ).
CLAUDE.md в коммиты блока НЕ включать; docs/sidebars.js — НАОБОРОТ, включать в блочный коммит (это часть документации). Ветку блока пушишь сам (для ревью); merge ветки
в master (squash по необходимости) делает оркестратор после закрытия обоих под-циклов.
```

## B16 — PREV и change-операторы  ·  [P+L] · слой 1 · трек T2 · зона: Логическая модель

_Scope:_ Предыдущее значение PREV и операторы изменения SET/CHANGED.

```
Ты дорабатываешь документацию lsFusion по блоку B16 «PREV и change-операторы» (трек T2 — Логическая модель — множества, события, ограничения).

Перед работой прочитай: docs/AGENTS.md (релевантные разделы целиком) и docs-process/DOCS-DESC-PLAN.md.
Источник истины — КОД платформы (LsfLogics.g -> ScriptingLogicsModule.java -> LogicsModule.java -> классы;
примеры в server/src/main/lsfusion; плагин — вторичен). Не описывай по памяти.
Работай в выданной под блок ветке (docs/B16 от master) — создай её ПЕРВЫМ действием и в ОТДЕЛЬНОМ рабочем дереве (git worktree add), не в общем чекауте: иначе параллельные блоки/исполнители молча затрут несохранённое. Коммить в ветку рано (commit-first); в master сам не мерджи и master не пушь.
EN и RU — построчно синхронные переводы одного текста (одна структура и состав информации).
Tier-чистота: Paradigm — абстракция (как будто синтаксиса/расширений нет); Language — синтаксис/механика
именно этой абстракции, ссылается на Paradigm и не переизлагает её. Не смешивать tier'ы.

ДВА ПОСЛЕДОВАТЕЛЬНЫХ ПОД-ЦИКЛА: сначала ЦЕЛИКОМ Paradigm, потом ЦЕЛИКОМ Language. Каждый под-цикл =
описание + СВОЙ коммит + ЗАВЕРШЁННОЕ ревью. Language не начинать, пока под-цикл Paradigm не закрыт.

**Paradigm (доописать обе версии, en+ru построчно синхронно; если статьи ещё нет — создать оба файла):**
- `docs/paradigm/en/Previous_value_PREV.md` + `docs/paradigm/ru/Previous_value_PREV.md`
- `docs/paradigm/en/Change_operators_SET_CHANGED_etc.md` + `docs/paradigm/ru/Change_operators_SET_CHANGED_etc.md`

**Language (доописать обе версии, en+ru построчно синхронно; если статьи ещё нет — создать оба файла):**
- `docs/language/en/PREV_operator.md` + `docs/language/ru/PREV_operator.md`
- `docs/language/en/Change_operators.md` + `docs/language/ru/Change_operators.md`

ЦИКЛ СДАЧИ на КАЖДЫЙ под-цикл (commit-first внутри ветки блока):
  Под-цикл 1 — PARADIGM: коммит 'docs: B16 paradigm — PREV и change-операторы'.
  Под-цикл 2 — LANGUAGE (только после ПОЛНОГО закрытия Paradigm): коммит 'docs: B16 language — PREV и change-операторы'.
  Шаги под-цикла:
  1. Написать/создать файлы под-цикла -> коммит (subject выше).
  2. self-review (явная сверка с каждым релевантным правилом AGENTS.md; полнота по коду/дочерним;
     EN/RU parity; направление cross-link'ов; tier-чистота) + codex review с master:docs/AGENTS.md;
     правки отдельными коммитами до чистого прохода. Это твой контроль ПЕРЕД показом человеку.
  3. РЕВЬЮ ЧЕЛОВЕКОМ: отдаёшь результат человеку (исполнитель/оркестратор) — он даёт замечания.
  4. Исправляешь замечания ОТДЕЛЬНЫМИ коммитами (при необходимости снова self/codex) и возвращаешь на ревью.
  5. Повторяешь шаги 3-4, пока у человека НЕ ОСТАНЕТСЯ претензий — только тогда под-цикл закрыт.

Если конструкции блока ещё нет статьи — создать файлы en+ru в нужной type-папке (docs/paradigm/ или
docs/language/) и сам подключить их в docs/sidebars.js тем же блочным коммитом. Нашёл обобщаемое правило/проблему —
допиши его в AGENTS.md ОТДЕЛЬНЫМ коммитом (AGENTS.md дописывать может любой; это файл для ИИ).
CLAUDE.md в коммиты блока НЕ включать; docs/sidebars.js — НАОБОРОТ, включать в блочный коммит (это часть документации). Ветку блока пушишь сам (для ревью); merge ветки
в master (squash по необходимости) делает оркестратор после закрытия обоих под-циклов.
```

## B10 — Set-операции — обзор  ·  [P] · слой 2 · трек T2 · зона: Логическая модель

_Scope:_ Корневой обзор set-операций.

```
Ты дорабатываешь документацию lsFusion по блоку B10 «Set-операции — обзор» (трек T2 — Логическая модель — множества, события, ограничения).

Перед работой прочитай: docs/AGENTS.md (релевантные разделы целиком) и docs-process/DOCS-DESC-PLAN.md.
Источник истины — КОД платформы (LsfLogics.g -> ScriptingLogicsModule.java -> LogicsModule.java -> классы;
примеры в server/src/main/lsfusion; плагин — вторичен). Не описывай по памяти.
Работай в выданной под блок ветке (docs/B10 от master) — создай её ПЕРВЫМ действием и в ОТДЕЛЬНОМ рабочем дереве (git worktree add), не в общем чекауте: иначе параллельные блоки/исполнители молча затрут несохранённое. Коммить в ветку рано (commit-first); в master сам не мерджи и master не пушь.
ВАЖНО: это обзорный/корневой блок. Его источник истины — уже описанные ДОЧЕРНИЕ статьи (их состав
и термины) и дерево docs/sidebars.js, в большей степени чем код. Обзорная статья вводит и связывает
дочерние, а не пересказывает реализацию. Не бери этот блок, пока его дочерние блоки не описаны.
EN и RU — построчно синхронные переводы одного текста (одна структура и состав информации).
Tier-чистота: Paradigm — абстракция (как будто синтаксиса/расширений нет); Language — синтаксис/механика
именно этой абстракции, ссылается на Paradigm и не переизлагает её. Не смешивать tier'ы.

Блок только tier'а Paradigm — один под-цикл.

**Paradigm (доописать обе версии, en+ru построчно синхронно; если статьи ещё нет — создать оба файла):**
- `docs/paradigm/en/Set_operations.md` + `docs/paradigm/ru/Set_operations.md`

ЦИКЛ СДАЧИ на КАЖДЫЙ под-цикл (commit-first внутри ветки блока):
  Один под-цикл — PARADIGM: коммит 'docs: B10 paradigm — Set-операции — обзор'.
  Шаги под-цикла:
  1. Написать/создать файлы под-цикла -> коммит (subject выше).
  2. self-review (явная сверка с каждым релевантным правилом AGENTS.md; полнота по коду/дочерним;
     EN/RU parity; направление cross-link'ов; tier-чистота) + codex review с master:docs/AGENTS.md;
     правки отдельными коммитами до чистого прохода. Это твой контроль ПЕРЕД показом человеку.
  3. РЕВЬЮ ЧЕЛОВЕКОМ: отдаёшь результат человеку (исполнитель/оркестратор) — он даёт замечания.
  4. Исправляешь замечания ОТДЕЛЬНЫМИ коммитами (при необходимости снова self/codex) и возвращаешь на ревью.
  5. Повторяешь шаги 3-4, пока у человека НЕ ОСТАНЕТСЯ претензий — только тогда под-цикл закрыт.

Если конструкции блока ещё нет статьи — создать файлы en+ru в нужной type-папке (docs/paradigm/ или
docs/language/) и сам подключить их в docs/sidebars.js тем же блочным коммитом. Нашёл обобщаемое правило/проблему —
допиши его в AGENTS.md ОТДЕЛЬНЫМ коммитом (AGENTS.md дописывать может любой; это файл для ИИ).
CLAUDE.md в коммиты блока НЕ включать; docs/sidebars.js — НАОБОРОТ, включать в блочный коммит (это часть документации). Ветку блока пушишь сам (для ревью); merge ветки
в master (squash по необходимости) делает оркестратор после закрытия обоих под-циклов.
```

# Трек T3 — Действия

## B18 — Изменение состояния  ·  [P+L] · слой 1 · трек T3 · зона: Действия

_Scope:_ Изменение состояния: CHANGE/NEW/CHANGECLASS/DELETE.

```
Ты дорабатываешь документацию lsFusion по блоку B18 «Изменение состояния» (трек T3 — Действия).

Перед работой прочитай: docs/AGENTS.md (релевантные разделы целиком) и docs-process/DOCS-DESC-PLAN.md.
Источник истины — КОД платформы (LsfLogics.g -> ScriptingLogicsModule.java -> LogicsModule.java -> классы;
примеры в server/src/main/lsfusion; плагин — вторичен). Не описывай по памяти.
Работай в выданной под блок ветке (docs/B18 от master) — создай её ПЕРВЫМ действием и в ОТДЕЛЬНОМ рабочем дереве (git worktree add), не в общем чекауте: иначе параллельные блоки/исполнители молча затрут несохранённое. Коммить в ветку рано (commit-first); в master сам не мерджи и master не пушь.
EN и RU — построчно синхронные переводы одного текста (одна структура и состав информации).
Tier-чистота: Paradigm — абстракция (как будто синтаксиса/расширений нет); Language — синтаксис/механика
именно этой абстракции, ссылается на Paradigm и не переизлагает её. Не смешивать tier'ы.

ДВА ПОСЛЕДОВАТЕЛЬНЫХ ПОД-ЦИКЛА: сначала ЦЕЛИКОМ Paradigm, потом ЦЕЛИКОМ Language. Каждый под-цикл =
описание + СВОЙ коммит + ЗАВЕРШЁННОЕ ревью. Language не начинать, пока под-цикл Paradigm не закрыт.

**Paradigm (доописать обе версии, en+ru построчно синхронно; если статьи ещё нет — создать оба файла):**
- `docs/paradigm/en/State_change.md` + `docs/paradigm/ru/State_change.md`
- `docs/paradigm/en/Property_change_CHANGE.md` + `docs/paradigm/ru/Property_change_CHANGE.md`
- `docs/paradigm/en/New_object_NEW.md` + `docs/paradigm/ru/New_object_NEW.md`
- `docs/paradigm/en/Class_change_CHANGECLASS_DELETE.md` + `docs/paradigm/ru/Class_change_CHANGECLASS_DELETE.md`

**Language (доописать обе версии, en+ru построчно синхронно; если статьи ещё нет — создать оба файла):**
- `docs/language/en/CHANGE_operator.md` + `docs/language/ru/CHANGE_operator.md`
- `docs/language/en/NEW_operator.md` + `docs/language/ru/NEW_operator.md`
- `docs/language/en/CHANGECLASS_operator.md` + `docs/language/ru/CHANGECLASS_operator.md`
- `docs/language/en/DELETE_operator.md` + `docs/language/ru/DELETE_operator.md`
- `docs/language/en/plus_equals_statement.md` + `docs/language/ru/plus_equals_statement.md`

СОЗДАТЬ новые статьи (нет в доке вообще; создать файлы en+ru И подключить их в docs/sidebars.js тем же блочным коммитом):
- `docs/language/en/ASYNCUPDATE_operator.md` + `docs/language/ru/ASYNCUPDATE_operator.md` — действие асинхронного обновления свойства; paradigm — в State_change.

ЦИКЛ СДАЧИ на КАЖДЫЙ под-цикл (commit-first внутри ветки блока):
  Под-цикл 1 — PARADIGM: коммит 'docs: B18 paradigm — Изменение состояния'.
  Под-цикл 2 — LANGUAGE (только после ПОЛНОГО закрытия Paradigm): коммит 'docs: B18 language — Изменение состояния'.
  Шаги под-цикла:
  1. Написать/создать файлы под-цикла -> коммит (subject выше).
  2. self-review (явная сверка с каждым релевантным правилом AGENTS.md; полнота по коду/дочерним;
     EN/RU parity; направление cross-link'ов; tier-чистота) + codex review с master:docs/AGENTS.md;
     правки отдельными коммитами до чистого прохода. Это твой контроль ПЕРЕД показом человеку.
  3. РЕВЬЮ ЧЕЛОВЕКОМ: отдаёшь результат человеку (исполнитель/оркестратор) — он даёт замечания.
  4. Исправляешь замечания ОТДЕЛЬНЫМИ коммитами (при необходимости снова self/codex) и возвращаешь на ревью.
  5. Повторяешь шаги 3-4, пока у человека НЕ ОСТАНЕТСЯ претензий — только тогда под-цикл закрыт.

Если конструкции блока ещё нет статьи — создать файлы en+ru в нужной type-папке (docs/paradigm/ или
docs/language/) и сам подключить их в docs/sidebars.js тем же блочным коммитом. Нашёл обобщаемое правило/проблему —
допиши его в AGENTS.md ОТДЕЛЬНЫМ коммитом (AGENTS.md дописывать может любой; это файл для ИИ).
CLAUDE.md в коммиты блока НЕ включать; docs/sidebars.js — НАОБОРОТ, включать в блочный коммит (это часть документации). Ветку блока пушишь сам (для ревью); merge ветки
в master (squash по необходимости) делает оркестратор после закрытия обоих под-циклов.
```

## B19 — Ветвление, циклы, выход  ·  [P+L] · слой 1 · трек T3 · зона: Действия

_Scope:_ Последовательность, ветвление/циклы/прерывания/выход/исключения/вызов.

```
Ты дорабатываешь документацию lsFusion по блоку B19 «Ветвление, циклы, выход» (трек T3 — Действия).

Перед работой прочитай: docs/AGENTS.md (релевантные разделы целиком) и docs-process/DOCS-DESC-PLAN.md.
Источник истины — КОД платформы (LsfLogics.g -> ScriptingLogicsModule.java -> LogicsModule.java -> классы;
примеры в server/src/main/lsfusion; плагин — вторичен). Не описывай по памяти.
Работай в выданной под блок ветке (docs/B19 от master) — создай её ПЕРВЫМ действием и в ОТДЕЛЬНОМ рабочем дереве (git worktree add), не в общем чекауте: иначе параллельные блоки/исполнители молча затрут несохранённое. Коммить в ветку рано (commit-first); в master сам не мерджи и master не пушь.
EN и RU — построчно синхронные переводы одного текста (одна структура и состав информации).
Tier-чистота: Paradigm — абстракция (как будто синтаксиса/расширений нет); Language — синтаксис/механика
именно этой абстракции, ссылается на Paradigm и не переизлагает её. Не смешивать tier'ы.

ДВА ПОСЛЕДОВАТЕЛЬНЫХ ПОД-ЦИКЛА: сначала ЦЕЛИКОМ Paradigm, потом ЦЕЛИКОМ Language. Каждый под-цикл =
описание + СВОЙ коммит + ЗАВЕРШЁННОЕ ревью. Language не начинать, пока под-цикл Paradigm не закрыт.

**Paradigm (доописать обе версии, en+ru построчно синхронно; если статьи ещё нет — создать оба файла):**
- `docs/paradigm/en/Sequence.md` + `docs/paradigm/ru/Sequence.md`
- `docs/paradigm/en/Branching_CASE_IF_MULTI.md` + `docs/paradigm/ru/Branching_CASE_IF_MULTI.md`
- `docs/paradigm/en/Loop_FOR.md` + `docs/paradigm/ru/Loop_FOR.md`
- `docs/paradigm/en/Recursive_loop_WHILE.md` + `docs/paradigm/ru/Recursive_loop_WHILE.md`
- `docs/paradigm/en/Interruption_BREAK.md` + `docs/paradigm/ru/Interruption_BREAK.md`
- `docs/paradigm/en/Next_iteration_CONTINUE.md` + `docs/paradigm/ru/Next_iteration_CONTINUE.md`
- `docs/paradigm/en/Exit_RETURN.md` + `docs/paradigm/ru/Exit_RETURN.md`
- `docs/paradigm/en/Exception_handling_TRY.md` + `docs/paradigm/ru/Exception_handling_TRY.md`
- `docs/paradigm/en/Call_EXEC.md` + `docs/paradigm/ru/Call_EXEC.md`

**Language (доописать обе версии, en+ru построчно синхронно; если статьи ещё нет — создать оба файла):**
- `docs/language/en/FOR_operator.md` + `docs/language/ru/FOR_operator.md`
- `docs/language/en/WHILE_operator.md` + `docs/language/ru/WHILE_operator.md`
- `docs/language/en/BREAK_operator.md` + `docs/language/ru/BREAK_operator.md`
- `docs/language/en/CONTINUE_operator.md` + `docs/language/ru/CONTINUE_operator.md`
- `docs/language/en/RETURN_operator.md` + `docs/language/ru/RETURN_operator.md`
- `docs/language/en/TRY_operator.md` + `docs/language/ru/TRY_operator.md`
- `docs/language/en/EXEC_operator.md` + `docs/language/ru/EXEC_operator.md`
- `docs/language/en/CASE_action_operator.md` + `docs/language/ru/CASE_action_operator.md`
- `docs/language/en/IF_..._THEN_action_operator.md` + `docs/language/ru/IF_..._THEN_action_operator.md`
- `docs/language/en/MULTI_action_operator.md` + `docs/language/ru/MULTI_action_operator.md`
- `docs/language/en/Braces_operator.md` + `docs/language/ru/Braces_operator.md`

ЦИКЛ СДАЧИ на КАЖДЫЙ под-цикл (commit-first внутри ветки блока):
  Под-цикл 1 — PARADIGM: коммит 'docs: B19 paradigm — Ветвление, циклы, выход'.
  Под-цикл 2 — LANGUAGE (только после ПОЛНОГО закрытия Paradigm): коммит 'docs: B19 language — Ветвление, циклы, выход'.
  Шаги под-цикла:
  1. Написать/создать файлы под-цикла -> коммит (subject выше).
  2. self-review (явная сверка с каждым релевантным правилом AGENTS.md; полнота по коду/дочерним;
     EN/RU parity; направление cross-link'ов; tier-чистота) + codex review с master:docs/AGENTS.md;
     правки отдельными коммитами до чистого прохода. Это твой контроль ПЕРЕД показом человеку.
  3. РЕВЬЮ ЧЕЛОВЕКОМ: отдаёшь результат человеку (исполнитель/оркестратор) — он даёт замечания.
  4. Исправляешь замечания ОТДЕЛЬНЫМИ коммитами (при необходимости снова self/codex) и возвращаешь на ревью.
  5. Повторяешь шаги 3-4, пока у человека НЕ ОСТАНЕТСЯ претензий — только тогда под-цикл закрыт.

Если конструкции блока ещё нет статьи — создать файлы en+ru в нужной type-папке (docs/paradigm/ или
docs/language/) и сам подключить их в docs/sidebars.js тем же блочным коммитом. Нашёл обобщаемое правило/проблему —
допиши его в AGENTS.md ОТДЕЛЬНЫМ коммитом (AGENTS.md дописывать может любой; это файл для ИИ).
CLAUDE.md в коммиты блока НЕ включать; docs/sidebars.js — НАОБОРОТ, включать в блочный коммит (это часть документации). Ветку блока пушишь сам (для ревью); merge ветки
в master (squash по необходимости) делает оркестратор после закрытия обоих под-циклов.
```

## B20 — Управление сессиями  ·  [P+L] · слой 1 · трек T3 · зона: Действия

_Scope:_ Сессии изменений, APPLY/CANCEL, новая/вложенная сессия.

```
Ты дорабатываешь документацию lsFusion по блоку B20 «Управление сессиями» (трек T3 — Действия).

Перед работой прочитай: docs/AGENTS.md (релевантные разделы целиком) и docs-process/DOCS-DESC-PLAN.md.
Источник истины — КОД платформы (LsfLogics.g -> ScriptingLogicsModule.java -> LogicsModule.java -> классы;
примеры в server/src/main/lsfusion; плагин — вторичен). Не описывай по памяти.
Работай в выданной под блок ветке (docs/B20 от master) — создай её ПЕРВЫМ действием и в ОТДЕЛЬНОМ рабочем дереве (git worktree add), не в общем чекауте: иначе параллельные блоки/исполнители молча затрут несохранённое. Коммить в ветку рано (commit-first); в master сам не мерджи и master не пушь.
EN и RU — построчно синхронные переводы одного текста (одна структура и состав информации).
Tier-чистота: Paradigm — абстракция (как будто синтаксиса/расширений нет); Language — синтаксис/механика
именно этой абстракции, ссылается на Paradigm и не переизлагает её. Не смешивать tier'ы.

ДВА ПОСЛЕДОВАТЕЛЬНЫХ ПОД-ЦИКЛА: сначала ЦЕЛИКОМ Paradigm, потом ЦЕЛИКОМ Language. Каждый под-цикл =
описание + СВОЙ коммит + ЗАВЕРШЁННОЕ ревью. Language не начинать, пока под-цикл Paradigm не закрыт.

**Paradigm (доописать обе версии, en+ru построчно синхронно; если статьи ещё нет — создать оба файла):**
- `docs/paradigm/en/Session_management.md` + `docs/paradigm/ru/Session_management.md`
- `docs/paradigm/en/Change_sessions.md` + `docs/paradigm/ru/Change_sessions.md`
- `docs/paradigm/en/Apply_changes_APPLY.md` + `docs/paradigm/ru/Apply_changes_APPLY.md`
- `docs/paradigm/en/Cancel_changes_CANCEL.md` + `docs/paradigm/ru/Cancel_changes_CANCEL.md`
- `docs/paradigm/en/New_session_NEWSESSION_NESTEDSESSION.md` + `docs/paradigm/ru/New_session_NEWSESSION_NESTEDSESSION.md`

**Language (доописать обе версии, en+ru построчно синхронно; если статьи ещё нет — создать оба файла):**
- `docs/language/en/APPLY_operator.md` + `docs/language/ru/APPLY_operator.md`
- `docs/language/en/CANCEL_operator.md` + `docs/language/ru/CANCEL_operator.md`
- `docs/language/en/NEWSESSION_operator.md` + `docs/language/ru/NEWSESSION_operator.md`
- `docs/language/en/NESTEDSESSION_operator.md` + `docs/language/ru/NESTEDSESSION_operator.md`

ЦИКЛ СДАЧИ на КАЖДЫЙ под-цикл (commit-first внутри ветки блока):
  Под-цикл 1 — PARADIGM: коммит 'docs: B20 paradigm — Управление сессиями'.
  Под-цикл 2 — LANGUAGE (только после ПОЛНОГО закрытия Paradigm): коммит 'docs: B20 language — Управление сессиями'.
  Шаги под-цикла:
  1. Написать/создать файлы под-цикла -> коммит (subject выше).
  2. self-review (явная сверка с каждым релевантным правилом AGENTS.md; полнота по коду/дочерним;
     EN/RU parity; направление cross-link'ов; tier-чистота) + codex review с master:docs/AGENTS.md;
     правки отдельными коммитами до чистого прохода. Это твой контроль ПЕРЕД показом человеку.
  3. РЕВЬЮ ЧЕЛОВЕКОМ: отдаёшь результат человеку (исполнитель/оркестратор) — он даёт замечания.
  4. Исправляешь замечания ОТДЕЛЬНЫМИ коммитами (при необходимости снова self/codex) и возвращаешь на ревью.
  5. Повторяешь шаги 3-4, пока у человека НЕ ОСТАНЕТСЯ претензий — только тогда под-цикл закрыт.

Если конструкции блока ещё нет статьи — создать файлы en+ru в нужной type-папке (docs/paradigm/ или
docs/language/) и сам подключить их в docs/sidebars.js тем же блочным коммитом. Нашёл обобщаемое правило/проблему —
допиши его в AGENTS.md ОТДЕЛЬНЫМ коммитом (AGENTS.md дописывать может любой; это файл для ИИ).
CLAUDE.md в коммиты блока НЕ включать; docs/sidebars.js — НАОБОРОТ, включать в блочный коммит (это часть документации). Ветку блока пушишь сам (для ревью); merge ветки
в master (squash по необходимости) делает оркестратор после закрытия обоих под-циклов.
```

## B21 — Потоки и соединения  ·  [P+L] · слой 1 · трек T3 · зона: Действия

_Scope:_ Создание потоков NEWTHREAD/NEWEXECUTOR, новое соединение.

```
Ты дорабатываешь документацию lsFusion по блоку B21 «Потоки и соединения» (трек T3 — Действия).

Перед работой прочитай: docs/AGENTS.md (релевантные разделы целиком) и docs-process/DOCS-DESC-PLAN.md.
Источник истины — КОД платформы (LsfLogics.g -> ScriptingLogicsModule.java -> LogicsModule.java -> классы;
примеры в server/src/main/lsfusion; плагин — вторичен). Не описывай по памяти.
Работай в выданной под блок ветке (docs/B21 от master) — создай её ПЕРВЫМ действием и в ОТДЕЛЬНОМ рабочем дереве (git worktree add), не в общем чекауте: иначе параллельные блоки/исполнители молча затрут несохранённое. Коммить в ветку рано (commit-first); в master сам не мерджи и master не пушь.
EN и RU — построчно синхронные переводы одного текста (одна структура и состав информации).
Tier-чистота: Paradigm — абстракция (как будто синтаксиса/расширений нет); Language — синтаксис/механика
именно этой абстракции, ссылается на Paradigm и не переизлагает её. Не смешивать tier'ы.

ДВА ПОСЛЕДОВАТЕЛЬНЫХ ПОД-ЦИКЛА: сначала ЦЕЛИКОМ Paradigm, потом ЦЕЛИКОМ Language. Каждый под-цикл =
описание + СВОЙ коммит + ЗАВЕРШЁННОЕ ревью. Language не начинать, пока под-цикл Paradigm не закрыт.

**Paradigm (доописать обе версии, en+ru построчно синхронно; если статьи ещё нет — создать оба файла):**
- `docs/paradigm/en/New_threads_NEWTHREAD_NEWEXECUTOR.md` + `docs/paradigm/ru/New_threads_NEWTHREAD_NEWEXECUTOR.md`
- `docs/paradigm/en/New_connection_NEWCONNECTION.md` + `docs/paradigm/ru/New_connection_NEWCONNECTION.md`

**Language (доописать обе версии, en+ru построчно синхронно; если статьи ещё нет — создать оба файла):**
- `docs/language/en/NEWTHREAD_operator.md` + `docs/language/ru/NEWTHREAD_operator.md`
- `docs/language/en/NEWEXECUTOR_operator.md` + `docs/language/ru/NEWEXECUTOR_operator.md`
- `docs/language/en/NEWCONNECTION_operator.md` + `docs/language/ru/NEWCONNECTION_operator.md`

ЦИКЛ СДАЧИ на КАЖДЫЙ под-цикл (commit-first внутри ветки блока):
  Под-цикл 1 — PARADIGM: коммит 'docs: B21 paradigm — Потоки и соединения'.
  Под-цикл 2 — LANGUAGE (только после ПОЛНОГО закрытия Paradigm): коммит 'docs: B21 language — Потоки и соединения'.
  Шаги под-цикла:
  1. Написать/создать файлы под-цикла -> коммит (subject выше).
  2. self-review (явная сверка с каждым релевантным правилом AGENTS.md; полнота по коду/дочерним;
     EN/RU parity; направление cross-link'ов; tier-чистота) + codex review с master:docs/AGENTS.md;
     правки отдельными коммитами до чистого прохода. Это твой контроль ПЕРЕД показом человеку.
  3. РЕВЬЮ ЧЕЛОВЕКОМ: отдаёшь результат человеку (исполнитель/оркестратор) — он даёт замечания.
  4. Исправляешь замечания ОТДЕЛЬНЫМИ коммитами (при необходимости снова self/codex) и возвращаешь на ревью.
  5. Повторяешь шаги 3-4, пока у человека НЕ ОСТАНЕТСЯ претензий — только тогда под-цикл закрыт.

Если конструкции блока ещё нет статьи — создать файлы en+ru в нужной type-папке (docs/paradigm/ или
docs/language/) и сам подключить их в docs/sidebars.js тем же блочным коммитом. Нашёл обобщаемое правило/проблему —
допиши его в AGENTS.md ОТДЕЛЬНЫМ коммитом (AGENTS.md дописывать может любой; это файл для ИИ).
CLAUDE.md в коммиты блока НЕ включать; docs/sidebars.js — НАОБОРОТ, включать в блочный коммит (это часть документации). Ветку блока пушишь сам (для ревью); merge ветки
в master (squash по необходимости) делает оркестратор после закрытия обоих под-циклов.
```

## B22 — Активация (ACTIVATE/ACTIVE)  ·  [P+L] · слой 1 · трек T3 · зона: Действия

_Scope:_ Активация и активность.

```
Ты дорабатываешь документацию lsFusion по блоку B22 «Активация (ACTIVATE/ACTIVE)» (трек T3 — Действия).

Перед работой прочитай: docs/AGENTS.md (релевантные разделы целиком) и docs-process/DOCS-DESC-PLAN.md.
Источник истины — КОД платформы (LsfLogics.g -> ScriptingLogicsModule.java -> LogicsModule.java -> классы;
примеры в server/src/main/lsfusion; плагин — вторичен). Не описывай по памяти.
Работай в выданной под блок ветке (docs/B22 от master) — создай её ПЕРВЫМ действием и в ОТДЕЛЬНОМ рабочем дереве (git worktree add), не в общем чекауте: иначе параллельные блоки/исполнители молча затрут несохранённое. Коммить в ветку рано (commit-first); в master сам не мерджи и master не пушь.
EN и RU — построчно синхронные переводы одного текста (одна структура и состав информации).
Tier-чистота: Paradigm — абстракция (как будто синтаксиса/расширений нет); Language — синтаксис/механика
именно этой абстракции, ссылается на Paradigm и не переизлагает её. Не смешивать tier'ы.

ДВА ПОСЛЕДОВАТЕЛЬНЫХ ПОД-ЦИКЛА: сначала ЦЕЛИКОМ Paradigm, потом ЦЕЛИКОМ Language. Каждый под-цикл =
описание + СВОЙ коммит + ЗАВЕРШЁННОЕ ревью. Language не начинать, пока под-цикл Paradigm не закрыт.

**Paradigm (доописать обе версии, en+ru построчно синхронно; если статьи ещё нет — создать оба файла):**
- `docs/paradigm/en/Activation_ACTIVATE.md` + `docs/paradigm/ru/Activation_ACTIVATE.md`
- `docs/paradigm/en/Activity_ACTIVE.md` + `docs/paradigm/ru/Activity_ACTIVE.md`

**Language (доописать обе версии, en+ru построчно синхронно; если статьи ещё нет — создать оба файла):**
- `docs/language/en/ACTIVATE_operator.md` + `docs/language/ru/ACTIVATE_operator.md`
- `docs/language/en/ACTIVE_operator.md` + `docs/language/ru/ACTIVE_operator.md`

ЦИКЛ СДАЧИ на КАЖДЫЙ под-цикл (commit-first внутри ветки блока):
  Под-цикл 1 — PARADIGM: коммит 'docs: B22 paradigm — Активация (ACTIVATE/ACTIVE)'.
  Под-цикл 2 — LANGUAGE (только после ПОЛНОГО закрытия Paradigm): коммит 'docs: B22 language — Активация (ACTIVATE/ACTIVE)'.
  Шаги под-цикла:
  1. Написать/создать файлы под-цикла -> коммит (subject выше).
  2. self-review (явная сверка с каждым релевантным правилом AGENTS.md; полнота по коду/дочерним;
     EN/RU parity; направление cross-link'ов; tier-чистота) + codex review с master:docs/AGENTS.md;
     правки отдельными коммитами до чистого прохода. Это твой контроль ПЕРЕД показом человеку.
  3. РЕВЬЮ ЧЕЛОВЕКОМ: отдаёшь результат человеку (исполнитель/оркестратор) — он даёт замечания.
  4. Исправляешь замечания ОТДЕЛЬНЫМИ коммитами (при необходимости снова self/codex) и возвращаешь на ревью.
  5. Повторяешь шаги 3-4, пока у человека НЕ ОСТАНЕТСЯ претензий — только тогда под-цикл закрыт.

Если конструкции блока ещё нет статьи — создать файлы en+ru в нужной type-папке (docs/paradigm/ или
docs/language/) и сам подключить их в docs/sidebars.js тем же блочным коммитом. Нашёл обобщаемое правило/проблему —
допиши его в AGENTS.md ОТДЕЛЬНЫМ коммитом (AGENTS.md дописывать может любой; это файл для ИИ).
CLAUDE.md в коммиты блока НЕ включать; docs/sidebars.js — НАОБОРОТ, включать в блочный коммит (это часть документации). Ветку блока пушишь сам (для ревью); merge ветки
в master (squash по необходимости) делает оркестратор после закрытия обоих под-циклов.
```

## B23 — Сообщения и ввод значений  ·  [P+L] · слой 1 · трек T3 · зона: Взаимодействие

_Scope:_ Сообщения MESSAGE/ASK, ввод значений, примитивный ввод, запрос значения.

```
Ты дорабатываешь документацию lsFusion по блоку B23 «Сообщения и ввод значений» (трек T3 — Действия).

Перед работой прочитай: docs/AGENTS.md (релевантные разделы целиком) и docs-process/DOCS-DESC-PLAN.md.
Источник истины — КОД платформы (LsfLogics.g -> ScriptingLogicsModule.java -> LogicsModule.java -> классы;
примеры в server/src/main/lsfusion; плагин — вторичен). Не описывай по памяти.
Работай в выданной под блок ветке (docs/B23 от master) — создай её ПЕРВЫМ действием и в ОТДЕЛЬНОМ рабочем дереве (git worktree add), не в общем чекауте: иначе параллельные блоки/исполнители молча затрут несохранённое. Коммить в ветку рано (commit-first); в master сам не мерджи и master не пушь.
EN и RU — построчно синхронные переводы одного текста (одна структура и состав информации).
Tier-чистота: Paradigm — абстракция (как будто синтаксиса/расширений нет); Language — синтаксис/механика
именно этой абстракции, ссылается на Paradigm и не переизлагает её. Не смешивать tier'ы.

ДВА ПОСЛЕДОВАТЕЛЬНЫХ ПОД-ЦИКЛА: сначала ЦЕЛИКОМ Paradigm, потом ЦЕЛИКОМ Language. Каждый под-цикл =
описание + СВОЙ коммит + ЗАВЕРШЁННОЕ ревью. Language не начинать, пока под-цикл Paradigm не закрыт.

**Paradigm (доописать обе версии, en+ru построчно синхронно; если статьи ещё нет — создать оба файла):**
- `docs/paradigm/en/Show_message_MESSAGE_ASK.md` + `docs/paradigm/ru/Show_message_MESSAGE_ASK.md`
- `docs/paradigm/en/Value_input.md` + `docs/paradigm/ru/Value_input.md`
- `docs/paradigm/en/Primitive_input_INPUT.md` + `docs/paradigm/ru/Primitive_input_INPUT.md`
- `docs/paradigm/en/Value_request_REQUEST.md` + `docs/paradigm/ru/Value_request_REQUEST.md`

**Language (доописать обе версии, en+ru построчно синхронно; если статьи ещё нет — создать оба файла):**
- `docs/language/en/MESSAGE_operator.md` + `docs/language/ru/MESSAGE_operator.md`
- `docs/language/en/ASK_operator.md` + `docs/language/ru/ASK_operator.md`
- `docs/language/en/INPUT_operator.md` + `docs/language/ru/INPUT_operator.md`
- `docs/language/en/REQUEST_operator.md` + `docs/language/ru/REQUEST_operator.md`

ЦИКЛ СДАЧИ на КАЖДЫЙ под-цикл (commit-first внутри ветки блока):
  Под-цикл 1 — PARADIGM: коммит 'docs: B23 paradigm — Сообщения и ввод значений'.
  Под-цикл 2 — LANGUAGE (только после ПОЛНОГО закрытия Paradigm): коммит 'docs: B23 language — Сообщения и ввод значений'.
  Шаги под-цикла:
  1. Написать/создать файлы под-цикла -> коммит (subject выше).
  2. self-review (явная сверка с каждым релевантным правилом AGENTS.md; полнота по коду/дочерним;
     EN/RU parity; направление cross-link'ов; tier-чистота) + codex review с master:docs/AGENTS.md;
     правки отдельными коммитами до чистого прохода. Это твой контроль ПЕРЕД показом человеку.
  3. РЕВЬЮ ЧЕЛОВЕКОМ: отдаёшь результат человеку (исполнитель/оркестратор) — он даёт замечания.
  4. Исправляешь замечания ОТДЕЛЬНЫМИ коммитами (при необходимости снова self/codex) и возвращаешь на ревью.
  5. Повторяешь шаги 3-4, пока у человека НЕ ОСТАНЕТСЯ претензий — только тогда под-цикл закрыт.

Если конструкции блока ещё нет статьи — создать файлы en+ru в нужной type-папке (docs/paradigm/ или
docs/language/) и сам подключить их в docs/sidebars.js тем же блочным коммитом. Нашёл обобщаемое правило/проблему —
допиши его в AGENTS.md ОТДЕЛЬНЫМ коммитом (AGENTS.md дописывать может любой; это файл для ИИ).
CLAUDE.md в коммиты блока НЕ включать; docs/sidebars.js — НАОБОРОТ, включать в блочный коммит (это часть документации). Ветку блока пушишь сам (для ревью); merge ветки
в master (squash по необходимости) делает оркестратор после закрытия обоих под-циклов.
```

## B17 — Действия — обзор, обёртка ACTION, порядок выполнения  ·  [P+L] · слой 2 · трек T3 · зона: Действия

_Scope:_ Корень действий, операторы-действия, объявление ACTION, опции; обзор порядка выполнения.

```
Ты дорабатываешь документацию lsFusion по блоку B17 «Действия — обзор, обёртка ACTION, порядок выполнения» (трек T3 — Действия).

Перед работой прочитай: docs/AGENTS.md (релевантные разделы целиком) и docs-process/DOCS-DESC-PLAN.md.
Источник истины — КОД платформы (LsfLogics.g -> ScriptingLogicsModule.java -> LogicsModule.java -> классы;
примеры в server/src/main/lsfusion; плагин — вторичен). Не описывай по памяти.
Работай в выданной под блок ветке (docs/B17 от master) — создай её ПЕРВЫМ действием и в ОТДЕЛЬНОМ рабочем дереве (git worktree add), не в общем чекауте: иначе параллельные блоки/исполнители молча затрут несохранённое. Коммить в ветку рано (commit-first); в master сам не мерджи и master не пушь.
ВАЖНО: это обзорный/корневой блок. Его источник истины — уже описанные ДОЧЕРНИЕ статьи (их состав
и термины) и дерево docs/sidebars.js, в большей степени чем код. Обзорная статья вводит и связывает
дочерние, а не пересказывает реализацию. Не бери этот блок, пока его дочерние блоки не описаны.
EN и RU — построчно синхронные переводы одного текста (одна структура и состав информации).
Tier-чистота: Paradigm — абстракция (как будто синтаксиса/расширений нет); Language — синтаксис/механика
именно этой абстракции, ссылается на Paradigm и не переизлагает её. Не смешивать tier'ы.

ДВА ПОСЛЕДОВАТЕЛЬНЫХ ПОД-ЦИКЛА: сначала ЦЕЛИКОМ Paradigm, потом ЦЕЛИКОМ Language. Каждый под-цикл =
описание + СВОЙ коммит + ЗАВЕРШЁННОЕ ревью. Language не начинать, пока под-цикл Paradigm не закрыт.

**Paradigm (доописать обе версии, en+ru построчно синхронно; если статьи ещё нет — создать оба файла):**
- `docs/paradigm/en/Actions.md` + `docs/paradigm/ru/Actions.md`
- `docs/paradigm/en/Action_operators_paradigm.md` + `docs/paradigm/ru/Action_operators_paradigm.md`
- `docs/paradigm/en/Execution_order.md` + `docs/paradigm/ru/Execution_order.md`

**Language (доописать обе версии, en+ru построчно синхронно; если статьи ещё нет — создать оба файла):**
- `docs/language/en/Action_operators.md` + `docs/language/ru/Action_operators.md`
- `docs/language/en/ACTION_statement.md` + `docs/language/ru/ACTION_statement.md`
- `docs/language/en/ACTION_plus_statement.md` + `docs/language/ru/ACTION_plus_statement.md`
- `docs/language/en/Action_options.md` + `docs/language/ru/Action_options.md`
- `docs/language/en/Empty_statement.md` + `docs/language/ru/Empty_statement.md`

ЦИКЛ СДАЧИ на КАЖДЫЙ под-цикл (commit-first внутри ветки блока):
  Под-цикл 1 — PARADIGM: коммит 'docs: B17 paradigm — Действия — обзор, обёртка ACTION, порядок выполнения'.
  Под-цикл 2 — LANGUAGE (только после ПОЛНОГО закрытия Paradigm): коммит 'docs: B17 language — Действия — обзор, обёртка ACTION, порядок выполнения'.
  Шаги под-цикла:
  1. Написать/создать файлы под-цикла -> коммит (subject выше).
  2. self-review (явная сверка с каждым релевантным правилом AGENTS.md; полнота по коду/дочерним;
     EN/RU parity; направление cross-link'ов; tier-чистота) + codex review с master:docs/AGENTS.md;
     правки отдельными коммитами до чистого прохода. Это твой контроль ПЕРЕД показом человеку.
  3. РЕВЬЮ ЧЕЛОВЕКОМ: отдаёшь результат человеку (исполнитель/оркестратор) — он даёт замечания.
  4. Исправляешь замечания ОТДЕЛЬНЫМИ коммитами (при необходимости снова self/codex) и возвращаешь на ревью.
  5. Повторяешь шаги 3-4, пока у человека НЕ ОСТАНЕТСЯ претензий — только тогда под-цикл закрыт.

Если конструкции блока ещё нет статьи — создать файлы en+ru в нужной type-папке (docs/paradigm/ или
docs/language/) и сам подключить их в docs/sidebars.js тем же блочным коммитом. Нашёл обобщаемое правило/проблему —
допиши его в AGENTS.md ОТДЕЛЬНЫМ коммитом (AGENTS.md дописывать может любой; это файл для ИИ).
CLAUDE.md в коммиты блока НЕ включать; docs/sidebars.js — НАОБОРОТ, включать в блочный коммит (это часть документации). Ветку блока пушишь сам (для ревью); merge ветки
в master (squash по необходимости) делает оркестратор после закрытия обоих под-циклов.
```

# Трек T4 — Формы и взаимодействие

## B24 — Взаимодействие с пользователем и открытие форм  ·  [P+L] · слой 1 · трек T4 · зона: Взаимодействие

_Scope:_ Взаимодействие через IS, открытие формы, интерактивный показ.

```
Ты дорабатываешь документацию lsFusion по блоку B24 «Взаимодействие с пользователем и открытие форм» (трек T4 — Формы и взаимодействие).

Перед работой прочитай: docs/AGENTS.md (релевантные разделы целиком) и docs-process/DOCS-DESC-PLAN.md.
Источник истины — КОД платформы (LsfLogics.g -> ScriptingLogicsModule.java -> LogicsModule.java -> классы;
примеры в server/src/main/lsfusion; плагин — вторичен). Не описывай по памяти.
Работай в выданной под блок ветке (docs/B24 от master) — создай её ПЕРВЫМ действием и в ОТДЕЛЬНОМ рабочем дереве (git worktree add), не в общем чекауте: иначе параллельные блоки/исполнители молча затрут несохранённое. Коммить в ветку рано (commit-first); в master сам не мерджи и master не пушь.
EN и RU — построчно синхронные переводы одного текста (одна структура и состав информации).
Tier-чистота: Paradigm — абстракция (как будто синтаксиса/расширений нет); Language — синтаксис/механика
именно этой абстракции, ссылается на Paradigm и не переизлагает её. Не смешивать tier'ы.

ДВА ПОСЛЕДОВАТЕЛЬНЫХ ПОД-ЦИКЛА: сначала ЦЕЛИКОМ Paradigm, потом ЦЕЛИКОМ Language. Каждый под-цикл =
описание + СВОЙ коммит + ЗАВЕРШЁННОЕ ревью. Language не начинать, пока под-цикл Paradigm не закрыт.

**Paradigm (доописать обе версии, en+ru построчно синхронно; если статьи ещё нет — создать оба файла):**
- `docs/paradigm/en/User_IS_interaction.md` + `docs/paradigm/ru/User_IS_interaction.md`
- `docs/paradigm/en/Open_form.md` + `docs/paradigm/ru/Open_form.md`

**Language (доописать обе версии, en+ru построчно синхронно; если статьи ещё нет — создать оба файла):**
- `docs/language/en/SHOW_operator.md` + `docs/language/ru/SHOW_operator.md`
- `docs/language/en/DIALOG_operator.md` + `docs/language/ru/DIALOG_operator.md`

ЦИКЛ СДАЧИ на КАЖДЫЙ под-цикл (commit-first внутри ветки блока):
  Под-цикл 1 — PARADIGM: коммит 'docs: B24 paradigm — Взаимодействие с пользователем и открытие форм'.
  Под-цикл 2 — LANGUAGE (только после ПОЛНОГО закрытия Paradigm): коммит 'docs: B24 language — Взаимодействие с пользователем и открытие форм'.
  Шаги под-цикла:
  1. Написать/создать файлы под-цикла -> коммит (subject выше).
  2. self-review (явная сверка с каждым релевантным правилом AGENTS.md; полнота по коду/дочерним;
     EN/RU parity; направление cross-link'ов; tier-чистота) + codex review с master:docs/AGENTS.md;
     правки отдельными коммитами до чистого прохода. Это твой контроль ПЕРЕД показом человеку.
  3. РЕВЬЮ ЧЕЛОВЕКОМ: отдаёшь результат человеку (исполнитель/оркестратор) — он даёт замечания.
  4. Исправляешь замечания ОТДЕЛЬНЫМИ коммитами (при необходимости снова self/codex) и возвращаешь на ревью.
  5. Повторяешь шаги 3-4, пока у человека НЕ ОСТАНЕТСЯ претензий — только тогда под-цикл закрыт.

Если конструкции блока ещё нет статьи — создать файлы en+ru в нужной type-папке (docs/paradigm/ или
docs/language/) и сам подключить их в docs/sidebars.js тем же блочным коммитом. Нашёл обобщаемое правило/проблему —
допиши его в AGENTS.md ОТДЕЛЬНЫМ коммитом (AGENTS.md дописывать может любой; это файл для ИИ).
CLAUDE.md в коммиты блока НЕ включать; docs/sidebars.js — НАОБОРОТ, включать в блочный коммит (это часть документации). Ветку блока пушишь сам (для ревью); merge ветки
в master (squash по необходимости) делает оркестратор после закрытия обоих под-циклов.
```

## B25 — Печать и структурированный вид  ·  [P+L] · слой 1 · трек T4 · зона: Взаимодействие

_Scope:_ Печатное и структурированное представления формы.

```
Ты дорабатываешь документацию lsFusion по блоку B25 «Печать и структурированный вид» (трек T4 — Формы и взаимодействие).

Перед работой прочитай: docs/AGENTS.md (релевантные разделы целиком) и docs-process/DOCS-DESC-PLAN.md.
Источник истины — КОД платформы (LsfLogics.g -> ScriptingLogicsModule.java -> LogicsModule.java -> классы;
примеры в server/src/main/lsfusion; плагин — вторичен). Не описывай по памяти.
Работай в выданной под блок ветке (docs/B25 от master) — создай её ПЕРВЫМ действием и в ОТДЕЛЬНОМ рабочем дереве (git worktree add), не в общем чекауте: иначе параллельные блоки/исполнители молча затрут несохранённое. Коммить в ветку рано (commit-first); в master сам не мерджи и master не пушь.
EN и RU — построчно синхронные переводы одного текста (одна структура и состав информации).
Tier-чистота: Paradigm — абстракция (как будто синтаксиса/расширений нет); Language — синтаксис/механика
именно этой абстракции, ссылается на Paradigm и не переизлагает её. Не смешивать tier'ы.

ДВА ПОСЛЕДОВАТЕЛЬНЫХ ПОД-ЦИКЛА: сначала ЦЕЛИКОМ Paradigm, потом ЦЕЛИКОМ Language. Каждый под-цикл =
описание + СВОЙ коммит + ЗАВЕРШЁННОЕ ревью. Language не начинать, пока под-цикл Paradigm не закрыт.

**Paradigm (доописать обе версии, en+ru построчно синхронно; если статьи ещё нет — создать оба файла):**
- `docs/paradigm/en/Print_view.md` + `docs/paradigm/ru/Print_view.md`
- `docs/paradigm/en/In_a_print_view_PRINT.md` + `docs/paradigm/ru/In_a_print_view_PRINT.md`
- `docs/paradigm/en/Structured_view.md` + `docs/paradigm/ru/Structured_view.md`
- `docs/paradigm/en/In_a_structured_view_EXPORT_IMPORT.md` + `docs/paradigm/ru/In_a_structured_view_EXPORT_IMPORT.md`

**Language (доописать обе версии, en+ru построчно синхронно; если статьи ещё нет — создать оба файла):**
- `docs/language/en/PRINT_operator.md` + `docs/language/ru/PRINT_operator.md`

ЦИКЛ СДАЧИ на КАЖДЫЙ под-цикл (commit-first внутри ветки блока):
  Под-цикл 1 — PARADIGM: коммит 'docs: B25 paradigm — Печать и структурированный вид'.
  Под-цикл 2 — LANGUAGE (только после ПОЛНОГО закрытия Paradigm): коммит 'docs: B25 language — Печать и структурированный вид'.
  Шаги под-цикла:
  1. Написать/создать файлы под-цикла -> коммит (subject выше).
  2. self-review (явная сверка с каждым релевантным правилом AGENTS.md; полнота по коду/дочерним;
     EN/RU parity; направление cross-link'ов; tier-чистота) + codex review с master:docs/AGENTS.md;
     правки отдельными коммитами до чистого прохода. Это твой контроль ПЕРЕД показом человеку.
  3. РЕВЬЮ ЧЕЛОВЕКОМ: отдаёшь результат человеку (исполнитель/оркестратор) — он даёт замечания.
  4. Исправляешь замечания ОТДЕЛЬНЫМИ коммитами (при необходимости снова self/codex) и возвращаешь на ревью.
  5. Повторяешь шаги 3-4, пока у человека НЕ ОСТАНЕТСЯ претензий — только тогда под-цикл закрыт.

Если конструкции блока ещё нет статьи — создать файлы en+ru в нужной type-папке (docs/paradigm/ или
docs/language/) и сам подключить их в docs/sidebars.js тем же блочным коммитом. Нашёл обобщаемое правило/проблему —
допиши его в AGENTS.md ОТДЕЛЬНЫМ коммитом (AGENTS.md дописывать может любой; это файл для ИИ).
CLAUDE.md в коммиты блока НЕ включать; docs/sidebars.js — НАОБОРОТ, включать в блочный коммит (это часть документации). Ветку блока пушишь сам (для ревью); merge ветки
в master (squash по необходимости) делает оркестратор после закрытия обоих под-циклов.
```

## B26 — Фокус/дерево объектов/скриншот  ·  [P+L] · слой 1 · трек T4 · зона: Взаимодействие

_Scope:_ Операторы фокуса, видимость дерева объектов, операторы групп объектов, скриншот.

```
Ты дорабатываешь документацию lsFusion по блоку B26 «Фокус/дерево объектов/скриншот» (трек T4 — Формы и взаимодействие).

Перед работой прочитай: docs/AGENTS.md (релевантные разделы целиком) и docs-process/DOCS-DESC-PLAN.md.
Источник истины — КОД платформы (LsfLogics.g -> ScriptingLogicsModule.java -> LogicsModule.java -> классы;
примеры в server/src/main/lsfusion; плагин — вторичен). Не описывай по памяти.
Работай в выданной под блок ветке (docs/B26 от master) — создай её ПЕРВЫМ действием и в ОТДЕЛЬНОМ рабочем дереве (git worktree add), не в общем чекауте: иначе параллельные блоки/исполнители молча затрут несохранённое. Коммить в ветку рано (commit-first); в master сам не мерджи и master не пушь.
EN и RU — построчно синхронные переводы одного текста (одна структура и состав информации).
Tier-чистота: Paradigm — абстракция (как будто синтаксиса/расширений нет); Language — синтаксис/механика
именно этой абстракции, ссылается на Paradigm и не переизлагает её. Не смешивать tier'ы.

ДВА ПОСЛЕДОВАТЕЛЬНЫХ ПОД-ЦИКЛА: сначала ЦЕЛИКОМ Paradigm, потом ЦЕЛИКОМ Language. Каждый под-цикл =
описание + СВОЙ коммит + ЗАВЕРШЁННОЕ ревью. Language не начинать, пока под-цикл Paradigm не закрыт.

**Paradigm (доописать обе версии, en+ru построчно синхронно; если статьи ещё нет — создать оба файла):**
- `docs/paradigm/en/Focus_operators.md` + `docs/paradigm/ru/Focus_operators.md`
- `docs/paradigm/en/Object_tree_visibility_EXPAND_COLLAPSE.md` + `docs/paradigm/ru/Object_tree_visibility_EXPAND_COLLAPSE.md`
- `docs/paradigm/en/Object_group_operators.md` + `docs/paradigm/ru/Object_group_operators.md`
- `docs/paradigm/en/Capture_SCREENSHOT.md` + `docs/paradigm/ru/Capture_SCREENSHOT.md`

**Language (доописать обе версии, en+ru построчно синхронно; если статьи ещё нет — создать оба файла):**
- `docs/language/en/SCREENSHOT_operator.md` + `docs/language/ru/SCREENSHOT_operator.md`
- `docs/language/en/EXPAND_operator.md` + `docs/language/ru/EXPAND_operator.md`
- `docs/language/en/COLLAPSE_operator.md` + `docs/language/ru/COLLAPSE_operator.md`
- `docs/language/en/Object_group_operator.md` + `docs/language/ru/Object_group_operator.md`

ЦИКЛ СДАЧИ на КАЖДЫЙ под-цикл (commit-first внутри ветки блока):
  Под-цикл 1 — PARADIGM: коммит 'docs: B26 paradigm — Фокус/дерево объектов/скриншот'.
  Под-цикл 2 — LANGUAGE (только после ПОЛНОГО закрытия Paradigm): коммит 'docs: B26 language — Фокус/дерево объектов/скриншот'.
  Шаги под-цикла:
  1. Написать/создать файлы под-цикла -> коммит (subject выше).
  2. self-review (явная сверка с каждым релевантным правилом AGENTS.md; полнота по коду/дочерним;
     EN/RU parity; направление cross-link'ов; tier-чистота) + codex review с master:docs/AGENTS.md;
     правки отдельными коммитами до чистого прохода. Это твой контроль ПЕРЕД показом человеку.
  3. РЕВЬЮ ЧЕЛОВЕКОМ: отдаёшь результат человеку (исполнитель/оркестратор) — он даёт замечания.
  4. Исправляешь замечания ОТДЕЛЬНЫМИ коммитами (при необходимости снова self/codex) и возвращаешь на ревью.
  5. Повторяешь шаги 3-4, пока у человека НЕ ОСТАНЕТСЯ претензий — только тогда под-цикл закрыт.

Если конструкции блока ещё нет статьи — создать файлы en+ru в нужной type-папке (docs/paradigm/ или
docs/language/) и сам подключить их в docs/sidebars.js тем же блочным коммитом. Нашёл обобщаемое правило/проблему —
допиши его в AGENTS.md ОТДЕЛЬНЫМ коммитом (AGENTS.md дописывать может любой; это файл для ИИ).
CLAUDE.md в коммиты блока НЕ включать; docs/sidebars.js — НАОБОРОТ, включать в блочный коммит (это часть документации). Ветку блока пушишь сам (для ревью); merge ветки
в master (squash по необходимости) делает оркестратор после закрытия обоих под-циклов.
```

## B28 — Дизайн формы и представления  ·  [P+L] · слой 1 · трек T4 · зона: Формы

_Scope:_ Дизайн формы, представления, отчётный дизайн, pivot.

```
Ты дорабатываешь документацию lsFusion по блоку B28 «Дизайн формы и представления» (трек T4 — Формы и взаимодействие).

Перед работой прочитай: docs/AGENTS.md (релевантные разделы целиком) и docs-process/DOCS-DESC-PLAN.md.
Источник истины — КОД платформы (LsfLogics.g -> ScriptingLogicsModule.java -> LogicsModule.java -> классы;
примеры в server/src/main/lsfusion; плагин — вторичен). Не описывай по памяти.
Работай в выданной под блок ветке (docs/B28 от master) — создай её ПЕРВЫМ действием и в ОТДЕЛЬНОМ рабочем дереве (git worktree add), не в общем чекауте: иначе параллельные блоки/исполнители молча затрут несохранённое. Коммить в ветку рано (commit-first); в master сам не мерджи и master не пушь.
EN и RU — построчно синхронные переводы одного текста (одна структура и состав информации).
Tier-чистота: Paradigm — абстракция (как будто синтаксиса/расширений нет); Language — синтаксис/механика
именно этой абстракции, ссылается на Paradigm и не переизлагает её. Не смешивать tier'ы.

ДВА ПОСЛЕДОВАТЕЛЬНЫХ ПОД-ЦИКЛА: сначала ЦЕЛИКОМ Paradigm, потом ЦЕЛИКОМ Language. Каждый под-цикл =
описание + СВОЙ коммит + ЗАВЕРШЁННОЕ ревью. Language не начинать, пока под-цикл Paradigm не закрыт.

**Paradigm (доописать обе версии, en+ru построчно синхронно; если статьи ещё нет — создать оба файла):**
- `docs/paradigm/en/Form_design.md` + `docs/paradigm/ru/Form_design.md`
- `docs/paradigm/en/Form_views.md` + `docs/paradigm/ru/Form_views.md`
- `docs/paradigm/en/Report_design.md` + `docs/paradigm/ru/Report_design.md`

**Language (доописать обе версии, en+ru построчно синхронно; если статьи ещё нет — создать оба файла):**
- `docs/language/en/DESIGN_statement.md` + `docs/language/ru/DESIGN_statement.md`
- `docs/language/en/Pivot_block.md` + `docs/language/ru/Pivot_block.md`

ЦИКЛ СДАЧИ на КАЖДЫЙ под-цикл (commit-first внутри ветки блока):
  Под-цикл 1 — PARADIGM: коммит 'docs: B28 paradigm — Дизайн формы и представления'.
  Под-цикл 2 — LANGUAGE (только после ПОЛНОГО закрытия Paradigm): коммит 'docs: B28 language — Дизайн формы и представления'.
  Шаги под-цикла:
  1. Написать/создать файлы под-цикла -> коммит (subject выше).
  2. self-review (явная сверка с каждым релевантным правилом AGENTS.md; полнота по коду/дочерним;
     EN/RU parity; направление cross-link'ов; tier-чистота) + codex review с master:docs/AGENTS.md;
     правки отдельными коммитами до чистого прохода. Это твой контроль ПЕРЕД показом человеку.
  3. РЕВЬЮ ЧЕЛОВЕКОМ: отдаёшь результат человеку (исполнитель/оркестратор) — он даёт замечания.
  4. Исправляешь замечания ОТДЕЛЬНЫМИ коммитами (при необходимости снова self/codex) и возвращаешь на ревью.
  5. Повторяешь шаги 3-4, пока у человека НЕ ОСТАНЕТСЯ претензий — только тогда под-цикл закрыт.

Если конструкции блока ещё нет статьи — создать файлы en+ru в нужной type-папке (docs/paradigm/ или
docs/language/) и сам подключить их в docs/sidebars.js тем же блочным коммитом. Нашёл обобщаемое правило/проблему —
допиши его в AGENTS.md ОТДЕЛЬНЫМ коммитом (AGENTS.md дописывать может любой; это файл для ИИ).
CLAUDE.md в коммиты блока НЕ включать; docs/sidebars.js — НАОБОРОТ, включать в блочный коммит (это часть документации). Ветку блока пушишь сам (для ревью); merge ветки
в master (squash по необходимости) делает оркестратор после закрытия обоих под-циклов.
```

## B29 — Интерактивный вид  ·  [P] · слой 1 · трек T4 · зона: Формы

_Scope:_ Интерактивное представление и SHOW/DIALOG-семантика.

```
Ты дорабатываешь документацию lsFusion по блоку B29 «Интерактивный вид» (трек T4 — Формы и взаимодействие).

Перед работой прочитай: docs/AGENTS.md (релевантные разделы целиком) и docs-process/DOCS-DESC-PLAN.md.
Источник истины — КОД платформы (LsfLogics.g -> ScriptingLogicsModule.java -> LogicsModule.java -> классы;
примеры в server/src/main/lsfusion; плагин — вторичен). Не описывай по памяти.
Работай в выданной под блок ветке (docs/B29 от master) — создай её ПЕРВЫМ действием и в ОТДЕЛЬНОМ рабочем дереве (git worktree add), не в общем чекауте: иначе параллельные блоки/исполнители молча затрут несохранённое. Коммить в ветку рано (commit-first); в master сам не мерджи и master не пушь.
EN и RU — построчно синхронные переводы одного текста (одна структура и состав информации).
Tier-чистота: Paradigm — абстракция (как будто синтаксиса/расширений нет); Language — синтаксис/механика
именно этой абстракции, ссылается на Paradigm и не переизлагает её. Не смешивать tier'ы.

Блок только tier'а Paradigm — один под-цикл.

**Paradigm (доописать обе версии, en+ru построчно синхронно; если статьи ещё нет — создать оба файла):**
- `docs/paradigm/en/Interactive_view.md` + `docs/paradigm/ru/Interactive_view.md`
- `docs/paradigm/en/In_an_interactive_view_SHOW_DIALOG.md` + `docs/paradigm/ru/In_an_interactive_view_SHOW_DIALOG.md`

ЦИКЛ СДАЧИ на КАЖДЫЙ под-цикл (commit-first внутри ветки блока):
  Один под-цикл — PARADIGM: коммит 'docs: B29 paradigm — Интерактивный вид'.
  Шаги под-цикла:
  1. Написать/создать файлы под-цикла -> коммит (subject выше).
  2. self-review (явная сверка с каждым релевантным правилом AGENTS.md; полнота по коду/дочерним;
     EN/RU parity; направление cross-link'ов; tier-чистота) + codex review с master:docs/AGENTS.md;
     правки отдельными коммитами до чистого прохода. Это твой контроль ПЕРЕД показом человеку.
  3. РЕВЬЮ ЧЕЛОВЕКОМ: отдаёшь результат человеку (исполнитель/оркестратор) — он даёт замечания.
  4. Исправляешь замечания ОТДЕЛЬНЫМИ коммитами (при необходимости снова self/codex) и возвращаешь на ревью.
  5. Повторяешь шаги 3-4, пока у человека НЕ ОСТАНЕТСЯ претензий — только тогда под-цикл закрыт.

Если конструкции блока ещё нет статьи — создать файлы en+ru в нужной type-папке (docs/paradigm/ или
docs/language/) и сам подключить их в docs/sidebars.js тем же блочным коммитом. Нашёл обобщаемое правило/проблему —
допиши его в AGENTS.md ОТДЕЛЬНЫМ коммитом (AGENTS.md дописывать может любой; это файл для ИИ).
CLAUDE.md в коммиты блока НЕ включать; docs/sidebars.js — НАОБОРОТ, включать в блочный коммит (это часть документации). Ветку блока пушишь сам (для ревью); merge ветки
в master (squash по необходимости) делает оркестратор после закрытия обоих под-циклов.
```

## B30 — Статический вид (VIEW)  ·  [P] · слой 1 · трек T4 · зона: Формы

_Scope:_ Статическое представление и оператор VIEW.

```
Ты дорабатываешь документацию lsFusion по блоку B30 «Статический вид (VIEW)» (трек T4 — Формы и взаимодействие).

Перед работой прочитай: docs/AGENTS.md (релевантные разделы целиком) и docs-process/DOCS-DESC-PLAN.md.
Источник истины — КОД платформы (LsfLogics.g -> ScriptingLogicsModule.java -> LogicsModule.java -> классы;
примеры в server/src/main/lsfusion; плагин — вторичен). Не описывай по памяти.
Работай в выданной под блок ветке (docs/B30 от master) — создай её ПЕРВЫМ действием и в ОТДЕЛЬНОМ рабочем дереве (git worktree add), не в общем чекауте: иначе параллельные блоки/исполнители молча затрут несохранённое. Коммить в ветку рано (commit-first); в master сам не мерджи и master не пушь.
EN и RU — построчно синхронные переводы одного текста (одна структура и состав информации).
Tier-чистота: Paradigm — абстракция (как будто синтаксиса/расширений нет); Language — синтаксис/механика
именно этой абстракции, ссылается на Paradigm и не переизлагает её. Не смешивать tier'ы.

Блок только tier'а Paradigm — один под-цикл.

**Paradigm (доописать обе версии, en+ru построчно синхронно; если статьи ещё нет — создать оба файла):**
- `docs/paradigm/en/Static_view.md` + `docs/paradigm/ru/Static_view.md`
- `docs/paradigm/en/View_VIEW.md` + `docs/paradigm/ru/View_VIEW.md`

ЦИКЛ СДАЧИ на КАЖДЫЙ под-цикл (commit-first внутри ветки блока):
  Один под-цикл — PARADIGM: коммит 'docs: B30 paradigm — Статический вид (VIEW)'.
  Шаги под-цикла:
  1. Написать/создать файлы под-цикла -> коммит (subject выше).
  2. self-review (явная сверка с каждым релевантным правилом AGENTS.md; полнота по коду/дочерним;
     EN/RU parity; направление cross-link'ов; tier-чистота) + codex review с master:docs/AGENTS.md;
     правки отдельными коммитами до чистого прохода. Это твой контроль ПЕРЕД показом человеку.
  3. РЕВЬЮ ЧЕЛОВЕКОМ: отдаёшь результат человеку (исполнитель/оркестратор) — он даёт замечания.
  4. Исправляешь замечания ОТДЕЛЬНЫМИ коммитами (при необходимости снова self/codex) и возвращаешь на ревью.
  5. Повторяешь шаги 3-4, пока у человека НЕ ОСТАНЕТСЯ претензий — только тогда под-цикл закрыт.

Если конструкции блока ещё нет статьи — создать файлы en+ru в нужной type-папке (docs/paradigm/ или
docs/language/) и сам подключить их в docs/sidebars.js тем же блочным коммитом. Нашёл обобщаемое правило/проблему —
допиши его в AGENTS.md ОТДЕЛЬНЫМ коммитом (AGENTS.md дописывать может любой; это файл для ИИ).
CLAUDE.md в коммиты блока НЕ включать; docs/sidebars.js — НАОБОРОТ, включать в блочный коммит (это часть документации). Ветку блока пушишь сам (для ревью); merge ветки
в master (squash по необходимости) делает оркестратор после закрытия обоих под-циклов.
```

## B31 — События формы  ·  [P] · слой 1 · трек T4 · зона: Формы

_Scope:_ События формы.

```
Ты дорабатываешь документацию lsFusion по блоку B31 «События формы» (трек T4 — Формы и взаимодействие).

Перед работой прочитай: docs/AGENTS.md (релевантные разделы целиком) и docs-process/DOCS-DESC-PLAN.md.
Источник истины — КОД платформы (LsfLogics.g -> ScriptingLogicsModule.java -> LogicsModule.java -> классы;
примеры в server/src/main/lsfusion; плагин — вторичен). Не описывай по памяти.
Работай в выданной под блок ветке (docs/B31 от master) — создай её ПЕРВЫМ действием и в ОТДЕЛЬНОМ рабочем дереве (git worktree add), не в общем чекауте: иначе параллельные блоки/исполнители молча затрут несохранённое. Коммить в ветку рано (commit-first); в master сам не мерджи и master не пушь.
EN и RU — построчно синхронные переводы одного текста (одна структура и состав информации).
Tier-чистота: Paradigm — абстракция (как будто синтаксиса/расширений нет); Language — синтаксис/механика
именно этой абстракции, ссылается на Paradigm и не переизлагает её. Не смешивать tier'ы.

Блок только tier'а Paradigm — один под-цикл.

**Paradigm (доописать обе версии, en+ru построчно синхронно; если статьи ещё нет — создать оба файла):**
- `docs/paradigm/en/Form_events.md` + `docs/paradigm/ru/Form_events.md`

ЦИКЛ СДАЧИ на КАЖДЫЙ под-цикл (commit-first внутри ветки блока):
  Один под-цикл — PARADIGM: коммит 'docs: B31 paradigm — События формы'.
  Шаги под-цикла:
  1. Написать/создать файлы под-цикла -> коммит (subject выше).
  2. self-review (явная сверка с каждым релевантным правилом AGENTS.md; полнота по коду/дочерним;
     EN/RU parity; направление cross-link'ов; tier-чистота) + codex review с master:docs/AGENTS.md;
     правки отдельными коммитами до чистого прохода. Это твой контроль ПЕРЕД показом человеку.
  3. РЕВЬЮ ЧЕЛОВЕКОМ: отдаёшь результат человеку (исполнитель/оркестратор) — он даёт замечания.
  4. Исправляешь замечания ОТДЕЛЬНЫМИ коммитами (при необходимости снова self/codex) и возвращаешь на ревью.
  5. Повторяешь шаги 3-4, пока у человека НЕ ОСТАНЕТСЯ претензий — только тогда под-цикл закрыт.

Если конструкции блока ещё нет статьи — создать файлы en+ru в нужной type-папке (docs/paradigm/ или
docs/language/) и сам подключить их в docs/sidebars.js тем же блочным коммитом. Нашёл обобщаемое правило/проблему —
допиши его в AGENTS.md ОТДЕЛЬНЫМ коммитом (AGENTS.md дописывать может любой; это файл для ИИ).
CLAUDE.md в коммиты блока НЕ включать; docs/sidebars.js — НАОБОРОТ, включать в блочный коммит (это часть документации). Ветку блока пушишь сам (для ревью); merge ветки
в master (squash по необходимости) делает оркестратор после закрытия обоих под-циклов.
```

## B32 — Навигатор и UI  ·  [P+L] · слой 1 · трек T4 · зона: Формы

_Scope:_ Навигатор, дизайн навигатора, пользовательский интерфейс, иконки.

```
Ты дорабатываешь документацию lsFusion по блоку B32 «Навигатор и UI» (трек T4 — Формы и взаимодействие).

Перед работой прочитай: docs/AGENTS.md (релевантные разделы целиком) и docs-process/DOCS-DESC-PLAN.md.
Источник истины — КОД платформы (LsfLogics.g -> ScriptingLogicsModule.java -> LogicsModule.java -> классы;
примеры в server/src/main/lsfusion; плагин — вторичен). Не описывай по памяти.
Работай в выданной под блок ветке (docs/B32 от master) — создай её ПЕРВЫМ действием и в ОТДЕЛЬНОМ рабочем дереве (git worktree add), не в общем чекауте: иначе параллельные блоки/исполнители молча затрут несохранённое. Коммить в ветку рано (commit-first); в master сам не мерджи и master не пушь.
EN и RU — построчно синхронные переводы одного текста (одна структура и состав информации).
Tier-чистота: Paradigm — абстракция (как будто синтаксиса/расширений нет); Language — синтаксис/механика
именно этой абстракции, ссылается на Paradigm и не переизлагает её. Не смешивать tier'ы.

ДВА ПОСЛЕДОВАТЕЛЬНЫХ ПОД-ЦИКЛА: сначала ЦЕЛИКОМ Paradigm, потом ЦЕЛИКОМ Language. Каждый под-цикл =
описание + СВОЙ коммит + ЗАВЕРШЁННОЕ ревью. Language не начинать, пока под-цикл Paradigm не закрыт.

**Paradigm (доописать обе версии, en+ru построчно синхронно; если статьи ещё нет — создать оба файла):**
- `docs/paradigm/en/Navigator.md` + `docs/paradigm/ru/Navigator.md`
- `docs/paradigm/en/Navigator_design.md` + `docs/paradigm/ru/Navigator_design.md`
- `docs/paradigm/en/User_interface.md` + `docs/paradigm/ru/User_interface.md`
- `docs/paradigm/en/Icons.md` + `docs/paradigm/ru/Icons.md`

**Language (доописать обе версии, en+ru построчно синхронно; если статьи ещё нет — создать оба файла):**
- `docs/language/en/NAVIGATOR_statement.md` + `docs/language/ru/NAVIGATOR_statement.md`
- `docs/language/en/WINDOW_statement.md` + `docs/language/ru/WINDOW_statement.md`

ЦИКЛ СДАЧИ на КАЖДЫЙ под-цикл (commit-first внутри ветки блока):
  Под-цикл 1 — PARADIGM: коммит 'docs: B32 paradigm — Навигатор и UI'.
  Под-цикл 2 — LANGUAGE (только после ПОЛНОГО закрытия Paradigm): коммит 'docs: B32 language — Навигатор и UI'.
  Шаги под-цикла:
  1. Написать/создать файлы под-цикла -> коммит (subject выше).
  2. self-review (явная сверка с каждым релевантным правилом AGENTS.md; полнота по коду/дочерним;
     EN/RU parity; направление cross-link'ов; tier-чистота) + codex review с master:docs/AGENTS.md;
     правки отдельными коммитами до чистого прохода. Это твой контроль ПЕРЕД показом человеку.
  3. РЕВЬЮ ЧЕЛОВЕКОМ: отдаёшь результат человеку (исполнитель/оркестратор) — он даёт замечания.
  4. Исправляешь замечания ОТДЕЛЬНЫМИ коммитами (при необходимости снова self/codex) и возвращаешь на ревью.
  5. Повторяешь шаги 3-4, пока у человека НЕ ОСТАНЕТСЯ претензий — только тогда под-цикл закрыт.

Если конструкции блока ещё нет статьи — создать файлы en+ru в нужной type-папке (docs/paradigm/ или
docs/language/) и сам подключить их в docs/sidebars.js тем же блочным коммитом. Нашёл обобщаемое правило/проблему —
допиши его в AGENTS.md ОТДЕЛЬНЫМ коммитом (AGENTS.md дописывать может любой; это файл для ИИ).
CLAUDE.md в коммиты блока НЕ включать; docs/sidebars.js — НАОБОРОТ, включать в блочный коммит (это часть документации). Ветку блока пушишь сам (для ревью); merge ветки
в master (squash по необходимости) делает оркестратор после закрытия обоих под-циклов.
```

## B33 — Расширение формы  ·  [P+L] · слой 1 · трек T4 · зона: Формы

_Scope:_ Расширение формы.

```
Ты дорабатываешь документацию lsFusion по блоку B33 «Расширение формы» (трек T4 — Формы и взаимодействие).

Перед работой прочитай: docs/AGENTS.md (релевантные разделы целиком) и docs-process/DOCS-DESC-PLAN.md.
Источник истины — КОД платформы (LsfLogics.g -> ScriptingLogicsModule.java -> LogicsModule.java -> классы;
примеры в server/src/main/lsfusion; плагин — вторичен). Не описывай по памяти.
Работай в выданной под блок ветке (docs/B33 от master) — создай её ПЕРВЫМ действием и в ОТДЕЛЬНОМ рабочем дереве (git worktree add), не в общем чекауте: иначе параллельные блоки/исполнители молча затрут несохранённое. Коммить в ветку рано (commit-first); в master сам не мерджи и master не пушь.
EN и RU — построчно синхронные переводы одного текста (одна структура и состав информации).
Tier-чистота: Paradigm — абстракция (как будто синтаксиса/расширений нет); Language — синтаксис/механика
именно этой абстракции, ссылается на Paradigm и не переизлагает её. Не смешивать tier'ы.

ДВА ПОСЛЕДОВАТЕЛЬНЫХ ПОД-ЦИКЛА: сначала ЦЕЛИКОМ Paradigm, потом ЦЕЛИКОМ Language. Каждый под-цикл =
описание + СВОЙ коммит + ЗАВЕРШЁННОЕ ревью. Language не начинать, пока под-цикл Paradigm не закрыт.

**Paradigm (доописать обе версии, en+ru построчно синхронно; если статьи ещё нет — создать оба файла):**
- `docs/paradigm/en/Form_extension.md` + `docs/paradigm/ru/Form_extension.md`

**Language (доописать обе версии, en+ru построчно синхронно; если статьи ещё нет — создать оба файла):**
- `docs/language/en/EXTEND_FORM_statement.md` + `docs/language/ru/EXTEND_FORM_statement.md`

ЦИКЛ СДАЧИ на КАЖДЫЙ под-цикл (commit-first внутри ветки блока):
  Под-цикл 1 — PARADIGM: коммит 'docs: B33 paradigm — Расширение формы'.
  Под-цикл 2 — LANGUAGE (только после ПОЛНОГО закрытия Paradigm): коммит 'docs: B33 language — Расширение формы'.
  Шаги под-цикла:
  1. Написать/создать файлы под-цикла -> коммит (subject выше).
  2. self-review (явная сверка с каждым релевантным правилом AGENTS.md; полнота по коду/дочерним;
     EN/RU parity; направление cross-link'ов; tier-чистота) + codex review с master:docs/AGENTS.md;
     правки отдельными коммитами до чистого прохода. Это твой контроль ПЕРЕД показом человеку.
  3. РЕВЬЮ ЧЕЛОВЕКОМ: отдаёшь результат человеку (исполнитель/оркестратор) — он даёт замечания.
  4. Исправляешь замечания ОТДЕЛЬНЫМИ коммитами (при необходимости снова self/codex) и возвращаешь на ревью.
  5. Повторяешь шаги 3-4, пока у человека НЕ ОСТАНЕТСЯ претензий — только тогда под-цикл закрыт.

Если конструкции блока ещё нет статьи — создать файлы en+ru в нужной type-папке (docs/paradigm/ или
docs/language/) и сам подключить их в docs/sidebars.js тем же блочным коммитом. Нашёл обобщаемое правило/проблему —
допиши его в AGENTS.md ОТДЕЛЬНЫМ коммитом (AGENTS.md дописывать может любой; это файл для ИИ).
CLAUDE.md в коммиты блока НЕ включать; docs/sidebars.js — НАОБОРОТ, включать в блочный коммит (это часть документации). Ветку блока пушишь сам (для ревью); merge ветки
в master (squash по необходимости) делает оркестратор после закрытия обоих под-циклов.
```

## B27 — Формы — структура и операторы  ·  [P+L] · слой 2 · трек T4 · зона: Формы

_Scope:_ Корень форм, структура формы, операторы формы.

```
Ты дорабатываешь документацию lsFusion по блоку B27 «Формы — структура и операторы» (трек T4 — Формы и взаимодействие).

Перед работой прочитай: docs/AGENTS.md (релевантные разделы целиком) и docs-process/DOCS-DESC-PLAN.md.
Источник истины — КОД платформы (LsfLogics.g -> ScriptingLogicsModule.java -> LogicsModule.java -> классы;
примеры в server/src/main/lsfusion; плагин — вторичен). Не описывай по памяти.
Работай в выданной под блок ветке (docs/B27 от master) — создай её ПЕРВЫМ действием и в ОТДЕЛЬНОМ рабочем дереве (git worktree add), не в общем чекауте: иначе параллельные блоки/исполнители молча затрут несохранённое. Коммить в ветку рано (commit-first); в master сам не мерджи и master не пушь.
ВАЖНО: это обзорный/корневой блок. Его источник истины — уже описанные ДОЧЕРНИЕ статьи (их состав
и термины) и дерево docs/sidebars.js, в большей степени чем код. Обзорная статья вводит и связывает
дочерние, а не пересказывает реализацию. Не бери этот блок, пока его дочерние блоки не описаны.
EN и RU — построчно синхронные переводы одного текста (одна структура и состав информации).
Tier-чистота: Paradigm — абстракция (как будто синтаксиса/расширений нет); Language — синтаксис/механика
именно этой абстракции, ссылается на Paradigm и не переизлагает её. Не смешивать tier'ы.

ДВА ПОСЛЕДОВАТЕЛЬНЫХ ПОД-ЦИКЛА: сначала ЦЕЛИКОМ Paradigm, потом ЦЕЛИКОМ Language. Каждый под-цикл =
описание + СВОЙ коммит + ЗАВЕРШЁННОЕ ревью. Language не начинать, пока под-цикл Paradigm не закрыт.

**Paradigm (доописать обе версии, en+ru построчно синхронно; если статьи ещё нет — создать оба файла):**
- `docs/paradigm/en/Forms.md` + `docs/paradigm/ru/Forms.md`
- `docs/paradigm/en/Form_structure.md` + `docs/paradigm/ru/Form_structure.md`
- `docs/paradigm/en/Form_operators.md` + `docs/paradigm/ru/Form_operators.md`

**Language (доописать обе версии, en+ru построчно синхронно; если статьи ещё нет — создать оба файла):**
- `docs/language/en/FORM_statement.md` + `docs/language/ru/FORM_statement.md`
- `docs/language/en/Object_blocks.md` + `docs/language/ru/Object_blocks.md`
- `docs/language/en/Properties_and_actions_block.md` + `docs/language/ru/Properties_and_actions_block.md`
- `docs/language/en/Filters_and_sortings_block.md` + `docs/language/ru/Filters_and_sortings_block.md`

ЦИКЛ СДАЧИ на КАЖДЫЙ под-цикл (commit-first внутри ветки блока):
  Под-цикл 1 — PARADIGM: коммит 'docs: B27 paradigm — Формы — структура и операторы'.
  Под-цикл 2 — LANGUAGE (только после ПОЛНОГО закрытия Paradigm): коммит 'docs: B27 language — Формы — структура и операторы'.
  Шаги под-цикла:
  1. Написать/создать файлы под-цикла -> коммит (subject выше).
  2. self-review (явная сверка с каждым релевантным правилом AGENTS.md; полнота по коду/дочерним;
     EN/RU parity; направление cross-link'ов; tier-чистота) + codex review с master:docs/AGENTS.md;
     правки отдельными коммитами до чистого прохода. Это твой контроль ПЕРЕД показом человеку.
  3. РЕВЬЮ ЧЕЛОВЕКОМ: отдаёшь результат человеку (исполнитель/оркестратор) — он даёт замечания.
  4. Исправляешь замечания ОТДЕЛЬНЫМИ коммитами (при необходимости снова self/codex) и возвращаешь на ревью.
  5. Повторяешь шаги 3-4, пока у человека НЕ ОСТАНЕТСЯ претензий — только тогда под-цикл закрыт.

Если конструкции блока ещё нет статьи — создать файлы en+ru в нужной type-папке (docs/paradigm/ или
docs/language/) и сам подключить их в docs/sidebars.js тем же блочным коммитом. Нашёл обобщаемое правило/проблему —
допиши его в AGENTS.md ОТДЕЛЬНЫМ коммитом (AGENTS.md дописывать может любой; это файл для ИИ).
CLAUDE.md в коммиты блока НЕ включать; docs/sidebars.js — НАОБОРОТ, включать в блочный коммит (это часть документации). Ветку блока пушишь сам (для ревью); merge ветки
в master (squash по необходимости) делает оркестратор после закрытия обоих под-циклов.
```

# Трек T5 — Физическая модель, развитие, язык, корни

## B34 — Таблицы, индексы, материализации  ·  [P+L] · слой 1 · трек T5 · зона: Физическая модель

_Scope:_ Таблицы, индексы, материализации.

```
Ты дорабатываешь документацию lsFusion по блоку B34 «Таблицы, индексы, материализации» (трек T5 — Физическая модель, развитие, язык, корни).

Перед работой прочитай: docs/AGENTS.md (релевантные разделы целиком) и docs-process/DOCS-DESC-PLAN.md.
Источник истины — КОД платформы (LsfLogics.g -> ScriptingLogicsModule.java -> LogicsModule.java -> классы;
примеры в server/src/main/lsfusion; плагин — вторичен). Не описывай по памяти.
Работай в выданной под блок ветке (docs/B34 от master) — создай её ПЕРВЫМ действием и в ОТДЕЛЬНОМ рабочем дереве (git worktree add), не в общем чекауте: иначе параллельные блоки/исполнители молча затрут несохранённое. Коммить в ветку рано (commit-first); в master сам не мерджи и master не пушь.
EN и RU — построчно синхронные переводы одного текста (одна структура и состав информации).
Tier-чистота: Paradigm — абстракция (как будто синтаксиса/расширений нет); Language — синтаксис/механика
именно этой абстракции, ссылается на Paradigm и не переизлагает её. Не смешивать tier'ы.

ДВА ПОСЛЕДОВАТЕЛЬНЫХ ПОД-ЦИКЛА: сначала ЦЕЛИКОМ Paradigm, потом ЦЕЛИКОМ Language. Каждый под-цикл =
описание + СВОЙ коммит + ЗАВЕРШЁННОЕ ревью. Language не начинать, пока под-цикл Paradigm не закрыт.

**Paradigm (доописать обе версии, en+ru построчно синхронно; если статьи ещё нет — создать оба файла):**
- `docs/paradigm/en/Tables.md` + `docs/paradigm/ru/Tables.md`
- `docs/paradigm/en/Indexes.md` + `docs/paradigm/ru/Indexes.md`
- `docs/paradigm/en/Materializations.md` + `docs/paradigm/ru/Materializations.md`

**Language (доописать обе версии, en+ru построчно синхронно; если статьи ещё нет — создать оба файла):**
- `docs/language/en/TABLE_statement.md` + `docs/language/ru/TABLE_statement.md`
- `docs/language/en/INDEX_statement.md` + `docs/language/ru/INDEX_statement.md`

СОЗДАТЬ новые статьи (нет в доке вообще; создать файлы en+ru И подключить их в docs/sidebars.js тем же блочным коммитом):
- `docs/language/en/RECALCULATE_operator.md` + `docs/language/ru/RECALCULATE_operator.md` — действие пересчёта хранимого/материализованного свойства (опция NOCLASSES); paradigm — в Materializations.

ЦИКЛ СДАЧИ на КАЖДЫЙ под-цикл (commit-first внутри ветки блока):
  Под-цикл 1 — PARADIGM: коммит 'docs: B34 paradigm — Таблицы, индексы, материализации'.
  Под-цикл 2 — LANGUAGE (только после ПОЛНОГО закрытия Paradigm): коммит 'docs: B34 language — Таблицы, индексы, материализации'.
  Шаги под-цикла:
  1. Написать/создать файлы под-цикла -> коммит (subject выше).
  2. self-review (явная сверка с каждым релевантным правилом AGENTS.md; полнота по коду/дочерним;
     EN/RU parity; направление cross-link'ов; tier-чистота) + codex review с master:docs/AGENTS.md;
     правки отдельными коммитами до чистого прохода. Это твой контроль ПЕРЕД показом человеку.
  3. РЕВЬЮ ЧЕЛОВЕКОМ: отдаёшь результат человеку (исполнитель/оркестратор) — он даёт замечания.
  4. Исправляешь замечания ОТДЕЛЬНЫМИ коммитами (при необходимости снова self/codex) и возвращаешь на ревью.
  5. Повторяешь шаги 3-4, пока у человека НЕ ОСТАНЕТСЯ претензий — только тогда под-цикл закрыт.

Если конструкции блока ещё нет статьи — создать файлы en+ru в нужной type-папке (docs/paradigm/ или
docs/language/) и сам подключить их в docs/sidebars.js тем же блочным коммитом. Нашёл обобщаемое правило/проблему —
допиши его в AGENTS.md ОТДЕЛЬНЫМ коммитом (AGENTS.md дописывать может любой; это файл для ИИ).
CLAUDE.md в коммиты блока НЕ включать; docs/sidebars.js — НАОБОРОТ, включать в блочный коммит (это часть документации). Ветку блока пушишь сам (для ревью); merge ветки
в master (squash по необходимости) делает оркестратор после закрытия обоих под-циклов.
```

## B37 — Метапрограммирование  ·  [P+L] · слой 1 · трек T5 · зона: Развитие

_Scope:_ Метапрограммирование и META/@.

```
Ты дорабатываешь документацию lsFusion по блоку B37 «Метапрограммирование» (трек T5 — Физическая модель, развитие, язык, корни).

Перед работой прочитай: docs/AGENTS.md (релевантные разделы целиком) и docs-process/DOCS-DESC-PLAN.md.
Источник истины — КОД платформы (LsfLogics.g -> ScriptingLogicsModule.java -> LogicsModule.java -> классы;
примеры в server/src/main/lsfusion; плагин — вторичен). Не описывай по памяти.
Работай в выданной под блок ветке (docs/B37 от master) — создай её ПЕРВЫМ действием и в ОТДЕЛЬНОМ рабочем дереве (git worktree add), не в общем чекауте: иначе параллельные блоки/исполнители молча затрут несохранённое. Коммить в ветку рано (commit-first); в master сам не мерджи и master не пушь.
EN и RU — построчно синхронные переводы одного текста (одна структура и состав информации).
Tier-чистота: Paradigm — абстракция (как будто синтаксиса/расширений нет); Language — синтаксис/механика
именно этой абстракции, ссылается на Paradigm и не переизлагает её. Не смешивать tier'ы.

ДВА ПОСЛЕДОВАТЕЛЬНЫХ ПОД-ЦИКЛА: сначала ЦЕЛИКОМ Paradigm, потом ЦЕЛИКОМ Language. Каждый под-цикл =
описание + СВОЙ коммит + ЗАВЕРШЁННОЕ ревью. Language не начинать, пока под-цикл Paradigm не закрыт.

**Paradigm (доописать обе версии, en+ru построчно синхронно; если статьи ещё нет — создать оба файла):**
- `docs/paradigm/en/Metaprogramming.md` + `docs/paradigm/ru/Metaprogramming.md`

**Language (доописать обе версии, en+ru построчно синхронно; если статьи ещё нет — создать оба файла):**
- `docs/language/en/META_statement.md` + `docs/language/ru/META_statement.md`
- `docs/language/en/commat_statement.md` + `docs/language/ru/commat_statement.md`

СОЗДАТЬ новые статьи (нет в доке вообще; создать файлы en+ru И подключить их в docs/sidebars.js тем же блочным коммитом):
- `docs/language/en/REFLECTION_operator.md` + `docs/language/ru/REFLECTION_operator.md` — свойство-рефлексия (метаданные, напр. CANONICALNAME); paradigm — в Metaprogramming.

ЦИКЛ СДАЧИ на КАЖДЫЙ под-цикл (commit-first внутри ветки блока):
  Под-цикл 1 — PARADIGM: коммит 'docs: B37 paradigm — Метапрограммирование'.
  Под-цикл 2 — LANGUAGE (только после ПОЛНОГО закрытия Paradigm): коммит 'docs: B37 language — Метапрограммирование'.
  Шаги под-цикла:
  1. Написать/создать файлы под-цикла -> коммит (subject выше).
  2. self-review (явная сверка с каждым релевантным правилом AGENTS.md; полнота по коду/дочерним;
     EN/RU parity; направление cross-link'ов; tier-чистота) + codex review с master:docs/AGENTS.md;
     правки отдельными коммитами до чистого прохода. Это твой контроль ПЕРЕД показом человеку.
  3. РЕВЬЮ ЧЕЛОВЕКОМ: отдаёшь результат человеку (исполнитель/оркестратор) — он даёт замечания.
  4. Исправляешь замечания ОТДЕЛЬНЫМИ коммитами (при необходимости снова self/codex) и возвращаешь на ревью.
  5. Повторяешь шаги 3-4, пока у человека НЕ ОСТАНЕТСЯ претензий — только тогда под-цикл закрыт.

Если конструкции блока ещё нет статьи — создать файлы en+ru в нужной type-папке (docs/paradigm/ или
docs/language/) и сам подключить их в docs/sidebars.js тем же блочным коммитом. Нашёл обобщаемое правило/проблему —
допиши его в AGENTS.md ОТДЕЛЬНЫМ коммитом (AGENTS.md дописывать может любой; это файл для ИИ).
CLAUDE.md в коммиты блока НЕ включать; docs/sidebars.js — НАОБОРОТ, включать в блочный коммит (это часть документации). Ветку блока пушишь сам (для ревью); merge ветки
в master (squash по необходимости) делает оркестратор после закрытия обоих под-циклов.
```

## B38 — Миграция/локализация/поиск  ·  [P] · слой 1 · трек T5 · зона: Развитие

_Scope:_ Миграция, интернационализация, поиск.

```
Ты дорабатываешь документацию lsFusion по блоку B38 «Миграция/локализация/поиск» (трек T5 — Физическая модель, развитие, язык, корни).

Перед работой прочитай: docs/AGENTS.md (релевантные разделы целиком) и docs-process/DOCS-DESC-PLAN.md.
Источник истины — КОД платформы (LsfLogics.g -> ScriptingLogicsModule.java -> LogicsModule.java -> классы;
примеры в server/src/main/lsfusion; плагин — вторичен). Не описывай по памяти.
Работай в выданной под блок ветке (docs/B38 от master) — создай её ПЕРВЫМ действием и в ОТДЕЛЬНОМ рабочем дереве (git worktree add), не в общем чекауте: иначе параллельные блоки/исполнители молча затрут несохранённое. Коммить в ветку рано (commit-first); в master сам не мерджи и master не пушь.
EN и RU — построчно синхронные переводы одного текста (одна структура и состав информации).
Tier-чистота: Paradigm — абстракция (как будто синтаксиса/расширений нет); Language — синтаксис/механика
именно этой абстракции, ссылается на Paradigm и не переизлагает её. Не смешивать tier'ы.

Блок только tier'а Paradigm — один под-цикл.

**Paradigm (доописать обе версии, en+ru построчно синхронно; если статьи ещё нет — создать оба файла):**
- `docs/paradigm/en/Migration.md` + `docs/paradigm/ru/Migration.md`
- `docs/paradigm/en/Internationalization.md` + `docs/paradigm/ru/Internationalization.md`
- `docs/paradigm/en/Search_.md` + `docs/paradigm/ru/Search_.md`

ЦИКЛ СДАЧИ на КАЖДЫЙ под-цикл (commit-first внутри ветки блока):
  Один под-цикл — PARADIGM: коммит 'docs: B38 paradigm — Миграция/локализация/поиск'.
  Шаги под-цикла:
  1. Написать/создать файлы под-цикла -> коммит (subject выше).
  2. self-review (явная сверка с каждым релевантным правилом AGENTS.md; полнота по коду/дочерним;
     EN/RU parity; направление cross-link'ов; tier-чистота) + codex review с master:docs/AGENTS.md;
     правки отдельными коммитами до чистого прохода. Это твой контроль ПЕРЕД показом человеку.
  3. РЕВЬЮ ЧЕЛОВЕКОМ: отдаёшь результат человеку (исполнитель/оркестратор) — он даёт замечания.
  4. Исправляешь замечания ОТДЕЛЬНЫМИ коммитами (при необходимости снова self/codex) и возвращаешь на ревью.
  5. Повторяешь шаги 3-4, пока у человека НЕ ОСТАНЕТСЯ претензий — только тогда под-цикл закрыт.

Если конструкции блока ещё нет статьи — создать файлы en+ru в нужной type-папке (docs/paradigm/ или
docs/language/) и сам подключить их в docs/sidebars.js тем же блочным коммитом. Нашёл обобщаемое правило/проблему —
допиши его в AGENTS.md ОТДЕЛЬНЫМ коммитом (AGENTS.md дописывать может любой; это файл для ИИ).
CLAUDE.md в коммиты блока НЕ включать; docs/sidebars.js — НАОБОРОТ, включать в блочный коммит (это часть документации). Ветку блока пушишь сам (для ревью); merge ветки
в master (squash по необходимости) делает оркестратор после закрытия обоих под-циклов.
```

## B39 — Основы языка  ·  [L] · слой 1 · трек T5 · зона: Развитие

_Scope:_ Базовые языковые статьи: токены, литералы, комментарии, соглашения.

```
Ты дорабатываешь документацию lsFusion по блоку B39 «Основы языка» (трек T5 — Физическая модель, развитие, язык, корни).

Перед работой прочитай: docs/AGENTS.md (релевантные разделы целиком) и docs-process/DOCS-DESC-PLAN.md.
Источник истины — КОД платформы (LsfLogics.g -> ScriptingLogicsModule.java -> LogicsModule.java -> классы;
примеры в server/src/main/lsfusion; плагин — вторичен). Не описывай по памяти.
Работай в выданной под блок ветке (docs/B39 от master) — создай её ПЕРВЫМ действием и в ОТДЕЛЬНОМ рабочем дереве (git worktree add), не в общем чекауте: иначе параллельные блоки/исполнители молча затрут несохранённое. Коммить в ветку рано (commit-first); в master сам не мерджи и master не пушь.
EN и RU — построчно синхронные переводы одного текста (одна структура и состав информации).
Tier-чистота: Paradigm — абстракция (как будто синтаксиса/расширений нет); Language — синтаксис/механика
именно этой абстракции, ссылается на Paradigm и не переизлагает её. Не смешивать tier'ы.

Блок только tier'а Language — один под-цикл.

**Language (доописать обе версии, en+ru построчно синхронно; если статьи ещё нет — создать оба файла):**
- `docs/language/en/Tokens.md` + `docs/language/ru/Tokens.md`
- `docs/language/en/Literals.md` + `docs/language/ru/Literals.md`
- `docs/language/en/Comments.md` + `docs/language/ru/Comments.md`
- `docs/language/en/Coding_conventions.md` + `docs/language/ru/Coding_conventions.md`

ЦИКЛ СДАЧИ на КАЖДЫЙ под-цикл (commit-first внутри ветки блока):
  Один под-цикл — LANGUAGE: коммит 'docs: B39 language — Основы языка'.
  Шаги под-цикла:
  1. Написать/создать файлы под-цикла -> коммит (subject выше).
  2. self-review (явная сверка с каждым релевантным правилом AGENTS.md; полнота по коду/дочерним;
     EN/RU parity; направление cross-link'ов; tier-чистота) + codex review с master:docs/AGENTS.md;
     правки отдельными коммитами до чистого прохода. Это твой контроль ПЕРЕД показом человеку.
  3. РЕВЬЮ ЧЕЛОВЕКОМ: отдаёшь результат человеку (исполнитель/оркестратор) — он даёт замечания.
  4. Исправляешь замечания ОТДЕЛЬНЫМИ коммитами (при необходимости снова self/codex) и возвращаешь на ревью.
  5. Повторяешь шаги 3-4, пока у человека НЕ ОСТАНЕТСЯ претензий — только тогда под-цикл закрыт.

Если конструкции блока ещё нет статьи — создать файлы en+ru в нужной type-папке (docs/paradigm/ или
docs/language/) и сам подключить их в docs/sidebars.js тем же блочным коммитом. Нашёл обобщаемое правило/проблему —
допиши его в AGENTS.md ОТДЕЛЬНЫМ коммитом (AGENTS.md дописывать может любой; это файл для ИИ).
CLAUDE.md в коммиты блока НЕ включать; docs/sidebars.js — НАОБОРОТ, включать в блочный коммит (это часть документации). Ветку блока пушишь сам (для ревью); merge ветки
в master (squash по необходимости) делает оркестратор после закрытия обоих под-циклов.
```

## B35 — Модульность и идентификация  ·  [P+L] · слой 2 · трек T5 · зона: Развитие

_Scope:_ Модульность, модули, группы свойств/действий, именование, идентификация элементов.

```
Ты дорабатываешь документацию lsFusion по блоку B35 «Модульность и идентификация» (трек T5 — Физическая модель, развитие, язык, корни).

Перед работой прочитай: docs/AGENTS.md (релевантные разделы целиком) и docs-process/DOCS-DESC-PLAN.md.
Источник истины — КОД платформы (LsfLogics.g -> ScriptingLogicsModule.java -> LogicsModule.java -> классы;
примеры в server/src/main/lsfusion; плагин — вторичен). Не описывай по памяти.
Работай в выданной под блок ветке (docs/B35 от master) — создай её ПЕРВЫМ действием и в ОТДЕЛЬНОМ рабочем дереве (git worktree add), не в общем чекауте: иначе параллельные блоки/исполнители молча затрут несохранённое. Коммить в ветку рано (commit-first); в master сам не мерджи и master не пушь.
ВАЖНО: это обзорный/корневой блок. Его источник истины — уже описанные ДОЧЕРНИЕ статьи (их состав
и термины) и дерево docs/sidebars.js, в большей степени чем код. Обзорная статья вводит и связывает
дочерние, а не пересказывает реализацию. Не бери этот блок, пока его дочерние блоки не описаны.
EN и RU — построчно синхронные переводы одного текста (одна структура и состав информации).
Tier-чистота: Paradigm — абстракция (как будто синтаксиса/расширений нет); Language — синтаксис/механика
именно этой абстракции, ссылается на Paradigm и не переизлагает её. Не смешивать tier'ы.

ДВА ПОСЛЕДОВАТЕЛЬНЫХ ПОД-ЦИКЛА: сначала ЦЕЛИКОМ Paradigm, потом ЦЕЛИКОМ Language. Каждый под-цикл =
описание + СВОЙ коммит + ЗАВЕРШЁННОЕ ревью. Language не начинать, пока под-цикл Paradigm не закрыт.

**Paradigm (доописать обе версии, en+ru построчно синхронно; если статьи ещё нет — создать оба файла):**
- `docs/paradigm/en/Modularity.md` + `docs/paradigm/ru/Modularity.md`
- `docs/paradigm/en/Modules.md` + `docs/paradigm/ru/Modules.md`
- `docs/paradigm/en/Groups_of_properties_and_actions.md` + `docs/paradigm/ru/Groups_of_properties_and_actions.md`
- `docs/paradigm/en/Naming.md` + `docs/paradigm/ru/Naming.md`
- `docs/paradigm/en/Element_identification.md` + `docs/paradigm/ru/Element_identification.md`

**Language (доописать обе версии, en+ru построчно синхронно; если статьи ещё нет — создать оба файла):**
- `docs/language/en/Module_header.md` + `docs/language/ru/Module_header.md`
- `docs/language/en/GROUP_statement.md` + `docs/language/ru/GROUP_statement.md`
- `docs/language/en/IDs.md` + `docs/language/ru/IDs.md`

ЦИКЛ СДАЧИ на КАЖДЫЙ под-цикл (commit-first внутри ветки блока):
  Под-цикл 1 — PARADIGM: коммит 'docs: B35 paradigm — Модульность и идентификация'.
  Под-цикл 2 — LANGUAGE (только после ПОЛНОГО закрытия Paradigm): коммит 'docs: B35 language — Модульность и идентификация'.
  Шаги под-цикла:
  1. Написать/создать файлы под-цикла -> коммит (subject выше).
  2. self-review (явная сверка с каждым релевантным правилом AGENTS.md; полнота по коду/дочерним;
     EN/RU parity; направление cross-link'ов; tier-чистота) + codex review с master:docs/AGENTS.md;
     правки отдельными коммитами до чистого прохода. Это твой контроль ПЕРЕД показом человеку.
  3. РЕВЬЮ ЧЕЛОВЕКОМ: отдаёшь результат человеку (исполнитель/оркестратор) — он даёт замечания.
  4. Исправляешь замечания ОТДЕЛЬНЫМИ коммитами (при необходимости снова self/codex) и возвращаешь на ревью.
  5. Повторяешь шаги 3-4, пока у человека НЕ ОСТАНЕТСЯ претензий — только тогда под-цикл закрыт.

Если конструкции блока ещё нет статьи — создать файлы en+ru в нужной type-папке (docs/paradigm/ или
docs/language/) и сам подключить их в docs/sidebars.js тем же блочным коммитом. Нашёл обобщаемое правило/проблему —
допиши его в AGENTS.md ОТДЕЛЬНЫМ коммитом (AGENTS.md дописывать может любой; это файл для ИИ).
CLAUDE.md в коммиты блока НЕ включать; docs/sidebars.js — НАОБОРОТ, включать в блочный коммит (это часть документации). Ветку блока пушишь сам (для ревью); merge ветки
в master (squash по необходимости) делает оркестратор после закрытия обоих под-циклов.
```

## B36 — Расширения  ·  [P+L] · слой 2 · трек T5 · зона: Развитие

_Scope:_ Расширения класса/свойства/действия + ABSTRACT-полиморфизм.

```
Ты дорабатываешь документацию lsFusion по блоку B36 «Расширения» (трек T5 — Физическая модель, развитие, язык, корни).

Перед работой прочитай: docs/AGENTS.md (релевантные разделы целиком) и docs-process/DOCS-DESC-PLAN.md.
Источник истины — КОД платформы (LsfLogics.g -> ScriptingLogicsModule.java -> LogicsModule.java -> классы;
примеры в server/src/main/lsfusion; плагин — вторичен). Не описывай по памяти.
Работай в выданной под блок ветке (docs/B36 от master) — создай её ПЕРВЫМ действием и в ОТДЕЛЬНОМ рабочем дереве (git worktree add), не в общем чекауте: иначе параллельные блоки/исполнители молча затрут несохранённое. Коммить в ветку рано (commit-first); в master сам не мерджи и master не пушь.
ВАЖНО: это обзорный/корневой блок. Его источник истины — уже описанные ДОЧЕРНИЕ статьи (их состав
и термины) и дерево docs/sidebars.js, в большей степени чем код. Обзорная статья вводит и связывает
дочерние, а не пересказывает реализацию. Не бери этот блок, пока его дочерние блоки не описаны.
EN и RU — построчно синхронные переводы одного текста (одна структура и состав информации).
Tier-чистота: Paradigm — абстракция (как будто синтаксиса/расширений нет); Language — синтаксис/механика
именно этой абстракции, ссылается на Paradigm и не переизлагает её. Не смешивать tier'ы.

ДВА ПОСЛЕДОВАТЕЛЬНЫХ ПОД-ЦИКЛА: сначала ЦЕЛИКОМ Paradigm, потом ЦЕЛИКОМ Language. Каждый под-цикл =
описание + СВОЙ коммит + ЗАВЕРШЁННОЕ ревью. Language не начинать, пока под-цикл Paradigm не закрыт.

**Paradigm (доописать обе версии, en+ru построчно синхронно; если статьи ещё нет — создать оба файла):**
- `docs/paradigm/en/Extensions.md` + `docs/paradigm/ru/Extensions.md`
- `docs/paradigm/en/Class_extension.md` + `docs/paradigm/ru/Class_extension.md`
- `docs/paradigm/en/Property_extension.md` + `docs/paradigm/ru/Property_extension.md`
- `docs/paradigm/en/Action_extension.md` + `docs/paradigm/ru/Action_extension.md`

**Language (доописать обе версии, en+ru построчно синхронно; если статьи ещё нет — создать оба файла):**
- `docs/language/en/EXTEND_CLASS_statement.md` + `docs/language/ru/EXTEND_CLASS_statement.md`
- `docs/language/en/ABSTRACT_operator.md` + `docs/language/ru/ABSTRACT_operator.md`
- `docs/language/en/ABSTRACT_action_operator.md` + `docs/language/ru/ABSTRACT_action_operator.md`

ЦИКЛ СДАЧИ на КАЖДЫЙ под-цикл (commit-first внутри ветки блока):
  Под-цикл 1 — PARADIGM: коммит 'docs: B36 paradigm — Расширения'.
  Под-цикл 2 — LANGUAGE (только после ПОЛНОГО закрытия Paradigm): коммит 'docs: B36 language — Расширения'.
  Шаги под-цикла:
  1. Написать/создать файлы под-цикла -> коммит (subject выше).
  2. self-review (явная сверка с каждым релевантным правилом AGENTS.md; полнота по коду/дочерним;
     EN/RU parity; направление cross-link'ов; tier-чистота) + codex review с master:docs/AGENTS.md;
     правки отдельными коммитами до чистого прохода. Это твой контроль ПЕРЕД показом человеку.
  3. РЕВЬЮ ЧЕЛОВЕКОМ: отдаёшь результат человеку (исполнитель/оркестратор) — он даёт замечания.
  4. Исправляешь замечания ОТДЕЛЬНЫМИ коммитами (при необходимости снова self/codex) и возвращаешь на ревью.
  5. Повторяешь шаги 3-4, пока у человека НЕ ОСТАНЕТСЯ претензий — только тогда под-цикл закрыт.

Если конструкции блока ещё нет статьи — создать файлы en+ru в нужной type-папке (docs/paradigm/ или
docs/language/) и сам подключить их в docs/sidebars.js тем же блочным коммитом. Нашёл обобщаемое правило/проблему —
допиши его в AGENTS.md ОТДЕЛЬНЫМ коммитом (AGENTS.md дописывать может любой; это файл для ИИ).
CLAUDE.md в коммиты блока НЕ включать; docs/sidebars.js — НАОБОРОТ, включать в блочный коммит (это часть документации). Ветку блока пушишь сам (для ревью); merge ветки
в master (squash по необходимости) делает оркестратор после закрытия обоих под-циклов.
```

## B00 — Корневые обзоры Paradigm/Language  ·  [P+L] · слой 3 · трек T5 · зона: Корни

_Scope:_ Корни tier'ов и моделей: Paradigm, View_logic, Physical_model; Language и обзор statement'ов.

```
Ты дорабатываешь документацию lsFusion по блоку B00 «Корневые обзоры Paradigm/Language» (трек T5 — Физическая модель, развитие, язык, корни).

Перед работой прочитай: docs/AGENTS.md (релевантные разделы целиком) и docs-process/DOCS-DESC-PLAN.md.
Источник истины — КОД платформы (LsfLogics.g -> ScriptingLogicsModule.java -> LogicsModule.java -> классы;
примеры в server/src/main/lsfusion; плагин — вторичен). Не описывай по памяти.
Работай в выданной под блок ветке (docs/B00 от master) — создай её ПЕРВЫМ действием и в ОТДЕЛЬНОМ рабочем дереве (git worktree add), не в общем чекауте: иначе параллельные блоки/исполнители молча затрут несохранённое. Коммить в ветку рано (commit-first); в master сам не мерджи и master не пушь.
ВАЖНО: это обзорный/корневой блок. Его источник истины — уже описанные ДОЧЕРНИЕ статьи (их состав
и термины) и дерево docs/sidebars.js, в большей степени чем код. Обзорная статья вводит и связывает
дочерние, а не пересказывает реализацию. Не бери этот блок, пока его дочерние блоки не описаны.
EN и RU — построчно синхронные переводы одного текста (одна структура и состав информации).
Tier-чистота: Paradigm — абстракция (как будто синтаксиса/расширений нет); Language — синтаксис/механика
именно этой абстракции, ссылается на Paradigm и не переизлагает её. Не смешивать tier'ы.

ДВА ПОСЛЕДОВАТЕЛЬНЫХ ПОД-ЦИКЛА: сначала ЦЕЛИКОМ Paradigm, потом ЦЕЛИКОМ Language. Каждый под-цикл =
описание + СВОЙ коммит + ЗАВЕРШЁННОЕ ревью. Language не начинать, пока под-цикл Paradigm не закрыт.

**Paradigm (доописать обе версии, en+ru построчно синхронно; если статьи ещё нет — создать оба файла):**
- `docs/paradigm/en/Paradigm.md` + `docs/paradigm/ru/Paradigm.md`
- `docs/paradigm/en/View_logic.md` + `docs/paradigm/ru/View_logic.md`
- `docs/paradigm/en/Physical_model.md` + `docs/paradigm/ru/Physical_model.md`

**Language (доописать обе версии, en+ru построчно синхронно; если статьи ещё нет — создать оба файла):**
- `docs/language/en/Language.md` + `docs/language/ru/Language.md`
- `docs/language/en/Statements.md` + `docs/language/ru/Statements.md`

ЦИКЛ СДАЧИ на КАЖДЫЙ под-цикл (commit-first внутри ветки блока):
  Под-цикл 1 — PARADIGM: коммит 'docs: B00 paradigm — Корневые обзоры Paradigm/Language'.
  Под-цикл 2 — LANGUAGE (только после ПОЛНОГО закрытия Paradigm): коммит 'docs: B00 language — Корневые обзоры Paradigm/Language'.
  Шаги под-цикла:
  1. Написать/создать файлы под-цикла -> коммит (subject выше).
  2. self-review (явная сверка с каждым релевантным правилом AGENTS.md; полнота по коду/дочерним;
     EN/RU parity; направление cross-link'ов; tier-чистота) + codex review с master:docs/AGENTS.md;
     правки отдельными коммитами до чистого прохода. Это твой контроль ПЕРЕД показом человеку.
  3. РЕВЬЮ ЧЕЛОВЕКОМ: отдаёшь результат человеку (исполнитель/оркестратор) — он даёт замечания.
  4. Исправляешь замечания ОТДЕЛЬНЫМИ коммитами (при необходимости снова self/codex) и возвращаешь на ревью.
  5. Повторяешь шаги 3-4, пока у человека НЕ ОСТАНЕТСЯ претензий — только тогда под-цикл закрыт.

Если конструкции блока ещё нет статьи — создать файлы en+ru в нужной type-папке (docs/paradigm/ или
docs/language/) и сам подключить их в docs/sidebars.js тем же блочным коммитом. Нашёл обобщаемое правило/проблему —
допиши его в AGENTS.md ОТДЕЛЬНЫМ коммитом (AGENTS.md дописывать может любой; это файл для ИИ).
CLAUDE.md в коммиты блока НЕ включать; docs/sidebars.js — НАОБОРОТ, включать в блочный коммит (это часть документации). Ветку блока пушишь сам (для ревью); merge ветки
в master (squash по необходимости) делает оркестратор после закрытия обоих под-циклов.
```

# Трек T6 — Интеграция, исполнение, управление, обучение

## B41 — EVAL / Java / Spring  ·  [P+L] · слой 1 · трек T6 · зона: Интеграция

_Scope:_ EVAL, Java integration API, кастомный Spring-bean.

```
Ты дорабатываешь документацию lsFusion по блоку B41 «EVAL / Java / Spring» (трек T6 — Интеграция, исполнение, управление, обучение).

Перед работой прочитай: docs/AGENTS.md (релевантные разделы целиком) и docs-process/DOCS-DESC-PLAN.md.
Источник истины — КОД платформы (LsfLogics.g -> ScriptingLogicsModule.java -> LogicsModule.java -> классы;
примеры в server/src/main/lsfusion; плагин — вторичен). Не описывай по памяти.
Работай в выданной под блок ветке (docs/B41 от master) — создай её ПЕРВЫМ действием и в ОТДЕЛЬНОМ рабочем дереве (git worktree add), не в общем чекауте: иначе параллельные блоки/исполнители молча затрут несохранённое. Коммить в ветку рано (commit-first); в master сам не мерджи и master не пушь.
EN и RU — построчно синхронные переводы одного текста (одна структура и состав информации).
Tier-чистота: Paradigm — абстракция (как будто синтаксиса/расширений нет); Language — синтаксис/механика
именно этой абстракции, ссылается на Paradigm и не переизлагает её. Не смешивать tier'ы.

ДВА ПОСЛЕДОВАТЕЛЬНЫХ ПОД-ЦИКЛА: сначала ЦЕЛИКОМ Paradigm, потом ЦЕЛИКОМ Language. Каждый под-цикл =
описание + СВОЙ коммит + ЗАВЕРШЁННОЕ ревью. Language не начинать, пока под-цикл Paradigm не закрыт.

**Paradigm (доописать обе версии, en+ru построчно синхронно; если статьи ещё нет — создать оба файла):**
- `docs/paradigm/en/Eval_EVAL.md` + `docs/paradigm/ru/Eval_EVAL.md`
- `docs/paradigm/en/Java_integration_API.md` + `docs/paradigm/ru/Java_integration_API.md`
- `docs/paradigm/en/Custom_Spring_bean_EventServer.md` + `docs/paradigm/ru/Custom_Spring_bean_EventServer.md`

**Language (доописать обе версии, en+ru построчно синхронно; если статьи ещё нет — создать оба файла):**
- `docs/language/en/EVAL_operator.md` + `docs/language/ru/EVAL_operator.md`

ЦИКЛ СДАЧИ на КАЖДЫЙ под-цикл (commit-first внутри ветки блока):
  Под-цикл 1 — PARADIGM: коммит 'docs: B41 paradigm — EVAL / Java / Spring'.
  Под-цикл 2 — LANGUAGE (только после ПОЛНОГО закрытия Paradigm): коммит 'docs: B41 language — EVAL / Java / Spring'.
  Шаги под-цикла:
  1. Написать/создать файлы под-цикла -> коммит (subject выше).
  2. self-review (явная сверка с каждым релевантным правилом AGENTS.md; полнота по коду/дочерним;
     EN/RU parity; направление cross-link'ов; tier-чистота) + codex review с master:docs/AGENTS.md;
     правки отдельными коммитами до чистого прохода. Это твой контроль ПЕРЕД показом человеку.
  3. РЕВЬЮ ЧЕЛОВЕКОМ: отдаёшь результат человеку (исполнитель/оркестратор) — он даёт замечания.
  4. Исправляешь замечания ОТДЕЛЬНЫМИ коммитами (при необходимости снова self/codex) и возвращаешь на ревью.
  5. Повторяешь шаги 3-4, пока у человека НЕ ОСТАНЕТСЯ претензий — только тогда под-цикл закрыт.

Если конструкции блока ещё нет статьи — создать файлы en+ru в нужной type-папке (docs/paradigm/ или
docs/language/) и сам подключить их в docs/sidebars.js тем же блочным коммитом. Нашёл обобщаемое правило/проблему —
допиши его в AGENTS.md ОТДЕЛЬНЫМ коммитом (AGENTS.md дописывать может любой; это файл для ИИ).
CLAUDE.md в коммиты блока НЕ включать; docs/sidebars.js — НАОБОРОТ, включать в блочный коммит (это часть документации). Ветку блока пушишь сам (для ревью); merge ветки
в master (squash по необходимости) делает оркестратор после закрытия обоих под-циклов.
```

## B42 — Файлы и обмен данными  ·  [P+L] · слой 1 · трек T6 · зона: Интеграция

_Scope:_ Файловые операторы, READ/WRITE, EXPORT/IMPORT данных, EMAIL.

```
Ты дорабатываешь документацию lsFusion по блоку B42 «Файлы и обмен данными» (трек T6 — Интеграция, исполнение, управление, обучение).

Перед работой прочитай: docs/AGENTS.md (релевантные разделы целиком) и docs-process/DOCS-DESC-PLAN.md.
Источник истины — КОД платформы (LsfLogics.g -> ScriptingLogicsModule.java -> LogicsModule.java -> классы;
примеры в server/src/main/lsfusion; плагин — вторичен). Не описывай по памяти.
Работай в выданной под блок ветке (docs/B42 от master) — создай её ПЕРВЫМ действием и в ОТДЕЛЬНОМ рабочем дереве (git worktree add), не в общем чекауте: иначе параллельные блоки/исполнители молча затрут несохранённое. Коммить в ветку рано (commit-first); в master сам не мерджи и master не пушь.
EN и RU — построчно синхронные переводы одного текста (одна структура и состав информации).
Tier-чистота: Paradigm — абстракция (как будто синтаксиса/расширений нет); Language — синтаксис/механика
именно этой абстракции, ссылается на Paradigm и не переизлагает её. Не смешивать tier'ы.

ДВА ПОСЛЕДОВАТЕЛЬНЫХ ПОД-ЦИКЛА: сначала ЦЕЛИКОМ Paradigm, потом ЦЕЛИКОМ Language. Каждый под-цикл =
описание + СВОЙ коммит + ЗАВЕРШЁННОЕ ревью. Language не начинать, пока под-цикл Paradigm не закрыт.

**Paradigm (доописать обе версии, en+ru построчно синхронно; если статьи ещё нет — создать оба файла):**
- `docs/paradigm/en/File_operators.md` + `docs/paradigm/ru/File_operators.md`
- `docs/paradigm/en/Read_file_READ.md` + `docs/paradigm/ru/Read_file_READ.md`
- `docs/paradigm/en/Write_file_WRITE.md` + `docs/paradigm/ru/Write_file_WRITE.md`
- `docs/paradigm/en/Data_export_EXPORT.md` + `docs/paradigm/ru/Data_export_EXPORT.md`
- `docs/paradigm/en/Data_import_IMPORT.md` + `docs/paradigm/ru/Data_import_IMPORT.md`
- `docs/paradigm/en/Send_mail_EMAIL.md` + `docs/paradigm/ru/Send_mail_EMAIL.md`

**Language (доописать обе версии, en+ru построчно синхронно; если статьи ещё нет — создать оба файла):**
- `docs/language/en/READ_operator.md` + `docs/language/ru/READ_operator.md`
- `docs/language/en/WRITE_operator.md` + `docs/language/ru/WRITE_operator.md`
- `docs/language/en/EXPORT_operator.md` + `docs/language/ru/EXPORT_operator.md`
- `docs/language/en/IMPORT_operator.md` + `docs/language/ru/IMPORT_operator.md`
- `docs/language/en/EMAIL_operator.md` + `docs/language/ru/EMAIL_operator.md`

ЦИКЛ СДАЧИ на КАЖДЫЙ под-цикл (commit-first внутри ветки блока):
  Под-цикл 1 — PARADIGM: коммит 'docs: B42 paradigm — Файлы и обмен данными'.
  Под-цикл 2 — LANGUAGE (только после ПОЛНОГО закрытия Paradigm): коммит 'docs: B42 language — Файлы и обмен данными'.
  Шаги под-цикла:
  1. Написать/создать файлы под-цикла -> коммит (subject выше).
  2. self-review (явная сверка с каждым релевантным правилом AGENTS.md; полнота по коду/дочерним;
     EN/RU parity; направление cross-link'ов; tier-чистота) + codex review с master:docs/AGENTS.md;
     правки отдельными коммитами до чистого прохода. Это твой контроль ПЕРЕД показом человеку.
  3. РЕВЬЮ ЧЕЛОВЕКОМ: отдаёшь результат человеку (исполнитель/оркестратор) — он даёт замечания.
  4. Исправляешь замечания ОТДЕЛЬНЫМИ коммитами (при необходимости снова self/codex) и возвращаешь на ревью.
  5. Повторяешь шаги 3-4, пока у человека НЕ ОСТАНЕТСЯ претензий — только тогда под-цикл закрыт.

Если конструкции блока ещё нет статьи — создать файлы en+ru в нужной type-папке (docs/paradigm/ или
docs/language/) и сам подключить их в docs/sidebars.js тем же блочным коммитом. Нашёл обобщаемое правило/проблему —
допиши его в AGENTS.md ОТДЕЛЬНЫМ коммитом (AGENTS.md дописывать может любой; это файл для ИИ).
CLAUDE.md в коммиты блока НЕ включать; docs/sidebars.js — НАОБОРОТ, включать в блочный коммит (это часть документации). Ветку блока пушишь сам (для ревью); merge ветки
в master (squash по необходимости) делает оркестратор после закрытия обоих под-циклов.
```

## B45 — Мониторинг и сервисы  ·  [P] · слой 1 · трек T6 · зона: Управление

_Scope:_ Монитор процессов, профайлер, журналы, MCP-сервер, чат.

```
Ты дорабатываешь документацию lsFusion по блоку B45 «Мониторинг и сервисы» (трек T6 — Интеграция, исполнение, управление, обучение).

Перед работой прочитай: docs/AGENTS.md (релевантные разделы целиком) и docs-process/DOCS-DESC-PLAN.md.
Источник истины — КОД платформы (LsfLogics.g -> ScriptingLogicsModule.java -> LogicsModule.java -> классы;
примеры в server/src/main/lsfusion; плагин — вторичен). Не описывай по памяти.
Работай в выданной под блок ветке (docs/B45 от master) — создай её ПЕРВЫМ действием и в ОТДЕЛЬНОМ рабочем дереве (git worktree add), не в общем чекауте: иначе параллельные блоки/исполнители молча затрут несохранённое. Коммить в ветку рано (commit-first); в master сам не мерджи и master не пушь.
EN и RU — построчно синхронные переводы одного текста (одна структура и состав информации).
Tier-чистота: Paradigm — абстракция (как будто синтаксиса/расширений нет); Language — синтаксис/механика
именно этой абстракции, ссылается на Paradigm и не переизлагает её. Не смешивать tier'ы.

Блок только tier'а Paradigm — один под-цикл.

**Paradigm (доописать обе версии, en+ru построчно синхронно; если статьи ещё нет — создать оба файла):**
- `docs/paradigm/en/Process_monitor.md` + `docs/paradigm/ru/Process_monitor.md`
- `docs/paradigm/en/Profiler.md` + `docs/paradigm/ru/Profiler.md`
- `docs/paradigm/en/Journals_and_logs.md` + `docs/paradigm/ru/Journals_and_logs.md`
- `docs/paradigm/en/MCP_server.md` + `docs/paradigm/ru/MCP_server.md`
- `docs/paradigm/en/Chat.md` + `docs/paradigm/ru/Chat.md`

СОЗДАТЬ новые статьи (нет в доке вообще; создать файлы en+ru И подключить их в docs/sidebars.js тем же блочным коммитом):
- `docs/language/en/SHOWDEP_operator.md` + `docs/language/ru/SHOWDEP_operator.md` — диагностическое действие (зависимости/рекурсия свойств; покрывает SHOWREC); paradigm — в обзоре управления.

ЦИКЛ СДАЧИ на КАЖДЫЙ под-цикл (commit-first внутри ветки блока):
  Один под-цикл — PARADIGM: коммит 'docs: B45 paradigm — Мониторинг и сервисы'.
  Шаги под-цикла:
  1. Написать/создать файлы под-цикла -> коммит (subject выше).
  2. self-review (явная сверка с каждым релевантным правилом AGENTS.md; полнота по коду/дочерним;
     EN/RU parity; направление cross-link'ов; tier-чистота) + codex review с master:docs/AGENTS.md;
     правки отдельными коммитами до чистого прохода. Это твой контроль ПЕРЕД показом человеку.
  3. РЕВЬЮ ЧЕЛОВЕКОМ: отдаёшь результат человеку (исполнитель/оркестратор) — он даёт замечания.
  4. Исправляешь замечания ОТДЕЛЬНЫМИ коммитами (при необходимости снова self/codex) и возвращаешь на ревью.
  5. Повторяешь шаги 3-4, пока у человека НЕ ОСТАНЕТСЯ претензий — только тогда под-цикл закрыт.

Если конструкции блока ещё нет статьи — создать файлы en+ru в нужной type-папке (docs/paradigm/ или
docs/language/) и сам подключить их в docs/sidebars.js тем же блочным коммитом. Нашёл обобщаемое правило/проблему —
допиши его в AGENTS.md ОТДЕЛЬНЫМ коммитом (AGENTS.md дописывать может любой; это файл для ИИ).
CLAUDE.md в коммиты блока НЕ включать; docs/sidebars.js — НАОБОРОТ, включать в блочный коммит (это часть документации). Ветку блока пушишь сам (для ревью); merge ветки
в master (squash по необходимости) делает оркестратор после закрытия обоих под-циклов.
```

## B46 — Установка и среда разработки  ·  [P] · слой 1 · трек T6 · зона: Установка

_Scope:_ Установка (авто/ручная), Docker, разработка (auto/manual), IDE, проекты.

```
Ты дорабатываешь документацию lsFusion по блоку B46 «Установка и среда разработки» (трек T6 — Интеграция, исполнение, управление, обучение).

Перед работой прочитай: docs/AGENTS.md (релевантные разделы целиком) и docs-process/DOCS-DESC-PLAN.md.
Источник истины — КОД платформы (LsfLogics.g -> ScriptingLogicsModule.java -> LogicsModule.java -> классы;
примеры в server/src/main/lsfusion; плагин — вторичен). Не описывай по памяти.
Работай в выданной под блок ветке (docs/B46 от master) — создай её ПЕРВЫМ действием и в ОТДЕЛЬНОМ рабочем дереве (git worktree add), не в общем чекауте: иначе параллельные блоки/исполнители молча затрут несохранённое. Коммить в ветку рано (commit-first); в master сам не мерджи и master не пушь.
EN и RU — построчно синхронные переводы одного текста (одна структура и состав информации).
Tier-чистота: Paradigm — абстракция (как будто синтаксиса/расширений нет); Language — синтаксис/механика
именно этой абстракции, ссылается на Paradigm и не переизлагает её. Не смешивать tier'ы.

Блок только tier'а Paradigm — один под-цикл.

**Paradigm (доописать обе версии, en+ru построчно синхронно; если статьи ещё нет — создать оба файла):**
- `docs/paradigm/en/Install.md` + `docs/paradigm/ru/Install.md`
- `docs/paradigm/en/Automatic_installation.md` + `docs/paradigm/ru/Automatic_installation.md`
- `docs/paradigm/en/Manual_installation.md` + `docs/paradigm/ru/Manual_installation.md`
- `docs/paradigm/en/Docker.md` + `docs/paradigm/ru/Docker.md`
- `docs/paradigm/en/Development.md` + `docs/paradigm/ru/Development.md`
- `docs/paradigm/en/Development_auto.md` + `docs/paradigm/ru/Development_auto.md`
- `docs/paradigm/en/Development_manual.md` + `docs/paradigm/ru/Development_manual.md`
- `docs/paradigm/en/IDE.md` + `docs/paradigm/ru/IDE.md`
- `docs/paradigm/en/Projects.md` + `docs/paradigm/ru/Projects.md`

ЦИКЛ СДАЧИ на КАЖДЫЙ под-цикл (commit-first внутри ветки блока):
  Один под-цикл — PARADIGM: коммит 'docs: B46 paradigm — Установка и среда разработки'.
  Шаги под-цикла:
  1. Написать/создать файлы под-цикла -> коммит (subject выше).
  2. self-review (явная сверка с каждым релевантным правилом AGENTS.md; полнота по коду/дочерним;
     EN/RU parity; направление cross-link'ов; tier-чистота) + codex review с master:docs/AGENTS.md;
     правки отдельными коммитами до чистого прохода. Это твой контроль ПЕРЕД показом человеку.
  3. РЕВЬЮ ЧЕЛОВЕКОМ: отдаёшь результат человеку (исполнитель/оркестратор) — он даёт замечания.
  4. Исправляешь замечания ОТДЕЛЬНЫМИ коммитами (при необходимости снова self/codex) и возвращаешь на ревью.
  5. Повторяешь шаги 3-4, пока у человека НЕ ОСТАНЕТСЯ претензий — только тогда под-цикл закрыт.

Если конструкции блока ещё нет статьи — создать файлы en+ru в нужной type-папке (docs/paradigm/ или
docs/language/) и сам подключить их в docs/sidebars.js тем же блочным коммитом. Нашёл обобщаемое правило/проблему —
допиши его в AGENTS.md ОТДЕЛЬНЫМ коммитом (AGENTS.md дописывать может любой; это файл для ИИ).
CLAUDE.md в коммиты блока НЕ включать; docs/sidebars.js — НАОБОРОТ, включать в блочный коммит (это часть документации). Ветку блока пушишь сам (для ревью); merge ветки
в master (squash по необходимости) делает оркестратор после закрытия обоих под-циклов.
```

## B48 — Учебные приложения  ·  [P] · слой 1 · трек T6 · зона: Обучение

_Scope:_ Учебные примеры приложений.

```
Ты дорабатываешь документацию lsFusion по блоку B48 «Учебные приложения» (трек T6 — Интеграция, исполнение, управление, обучение).

Перед работой прочитай: docs/AGENTS.md (релевантные разделы целиком) и docs-process/DOCS-DESC-PLAN.md.
Источник истины — КОД платформы (LsfLogics.g -> ScriptingLogicsModule.java -> LogicsModule.java -> классы;
примеры в server/src/main/lsfusion; плагин — вторичен). Не описывай по памяти.
Работай в выданной под блок ветке (docs/B48 от master) — создай её ПЕРВЫМ действием и в ОТДЕЛЬНОМ рабочем дереве (git worktree add), не в общем чекауте: иначе параллельные блоки/исполнители молча затрут несохранённое. Коммить в ветку рано (commit-first); в master сам не мерджи и master не пушь.
EN и RU — построчно синхронные переводы одного текста (одна структура и состав информации).
Tier-чистота: Paradigm — абстракция (как будто синтаксиса/расширений нет); Language — синтаксис/механика
именно этой абстракции, ссылается на Paradigm и не переизлагает её. Не смешивать tier'ы.

Блок только tier'а Paradigm — один под-цикл.

**Paradigm (доописать обе версии, en+ru построчно синхронно; если статьи ещё нет — создать оба файла):**
- `docs/paradigm/en/Materials_management.md` + `docs/paradigm/ru/Materials_management.md`
- `docs/paradigm/en/Score_table.md` + `docs/paradigm/ru/Score_table.md`

ЦИКЛ СДАЧИ на КАЖДЫЙ под-цикл (commit-first внутри ветки блока):
  Один под-цикл — PARADIGM: коммит 'docs: B48 paradigm — Учебные приложения'.
  Шаги под-цикла:
  1. Написать/создать файлы под-цикла -> коммит (subject выше).
  2. self-review (явная сверка с каждым релевантным правилом AGENTS.md; полнота по коду/дочерним;
     EN/RU parity; направление cross-link'ов; tier-чистота) + codex review с master:docs/AGENTS.md;
     правки отдельными коммитами до чистого прохода. Это твой контроль ПЕРЕД показом человеку.
  3. РЕВЬЮ ЧЕЛОВЕКОМ: отдаёшь результат человеку (исполнитель/оркестратор) — он даёт замечания.
  4. Исправляешь замечания ОТДЕЛЬНЫМИ коммитами (при необходимости снова self/codex) и возвращаешь на ревью.
  5. Повторяешь шаги 3-4, пока у человека НЕ ОСТАНЕТСЯ претензий — только тогда под-цикл закрыт.

Если конструкции блока ещё нет статьи — создать файлы en+ru в нужной type-папке (docs/paradigm/ или
docs/language/) и сам подключить их в docs/sidebars.js тем же блочным коммитом. Нашёл обобщаемое правило/проблему —
допиши его в AGENTS.md ОТДЕЛЬНЫМ коммитом (AGENTS.md дописывать может любой; это файл для ИИ).
CLAUDE.md в коммиты блока НЕ включать; docs/sidebars.js — НАОБОРОТ, включать в блочный коммит (это часть документации). Ветку блока пушишь сам (для ревью); merge ветки
в master (squash по необходимости) делает оркестратор после закрытия обоих под-циклов.
```

## B40 — Доступ к внешним/внутренним системам  ·  [P+L] · слой 2 · трек T6 · зона: Интеграция

_Scope:_ Интеграция (обзор), EXTERNAL/INTERNAL доступ в обе стороны, внутренний вызов.

```
Ты дорабатываешь документацию lsFusion по блоку B40 «Доступ к внешним/внутренним системам» (трек T6 — Интеграция, исполнение, управление, обучение).

Перед работой прочитай: docs/AGENTS.md (релевантные разделы целиком) и docs-process/DOCS-DESC-PLAN.md.
Источник истины — КОД платформы (LsfLogics.g -> ScriptingLogicsModule.java -> LogicsModule.java -> классы;
примеры в server/src/main/lsfusion; плагин — вторичен). Не описывай по памяти.
Работай в выданной под блок ветке (docs/B40 от master) — создай её ПЕРВЫМ действием и в ОТДЕЛЬНОМ рабочем дереве (git worktree add), не в общем чекауте: иначе параллельные блоки/исполнители молча затрут несохранённое. Коммить в ветку рано (commit-first); в master сам не мерджи и master не пушь.
ВАЖНО: это обзорный/корневой блок. Его источник истины — уже описанные ДОЧЕРНИЕ статьи (их состав
и термины) и дерево docs/sidebars.js, в большей степени чем код. Обзорная статья вводит и связывает
дочерние, а не пересказывает реализацию. Не бери этот блок, пока его дочерние блоки не описаны.
EN и RU — построчно синхронные переводы одного текста (одна структура и состав информации).
Tier-чистота: Paradigm — абстракция (как будто синтаксиса/расширений нет); Language — синтаксис/механика
именно этой абстракции, ссылается на Paradigm и не переизлагает её. Не смешивать tier'ы.

ДВА ПОСЛЕДОВАТЕЛЬНЫХ ПОД-ЦИКЛА: сначала ЦЕЛИКОМ Paradigm, потом ЦЕЛИКОМ Language. Каждый под-цикл =
описание + СВОЙ коммит + ЗАВЕРШЁННОЕ ревью. Language не начинать, пока под-цикл Paradigm не закрыт.

**Paradigm (доописать обе версии, en+ru построчно синхронно; если статьи ещё нет — создать оба файла):**
- `docs/paradigm/en/Integration.md` + `docs/paradigm/ru/Integration.md`
- `docs/paradigm/en/Access_to_an_external_system_EXTERNAL.md` + `docs/paradigm/ru/Access_to_an_external_system_EXTERNAL.md`
- `docs/paradigm/en/Access_from_an_external_system.md` + `docs/paradigm/ru/Access_from_an_external_system.md`
- `docs/paradigm/en/Access_to_an_internal_system_INTERNAL_FORMULA.md` + `docs/paradigm/ru/Access_to_an_internal_system_INTERNAL_FORMULA.md`
- `docs/paradigm/en/Access_from_an_internal_system.md` + `docs/paradigm/ru/Access_from_an_internal_system.md`
- `docs/paradigm/en/Internal_call_INTERNAL.md` + `docs/paradigm/ru/Internal_call_INTERNAL.md`

**Language (доописать обе версии, en+ru построчно синхронно; если статьи ещё нет — создать оба файла):**
- `docs/language/en/EXTERNAL_operator.md` + `docs/language/ru/EXTERNAL_operator.md`
- `docs/language/en/INTERNAL_operator.md` + `docs/language/ru/INTERNAL_operator.md`

ЦИКЛ СДАЧИ на КАЖДЫЙ под-цикл (commit-first внутри ветки блока):
  Под-цикл 1 — PARADIGM: коммит 'docs: B40 paradigm — Доступ к внешним/внутренним системам'.
  Под-цикл 2 — LANGUAGE (только после ПОЛНОГО закрытия Paradigm): коммит 'docs: B40 language — Доступ к внешним/внутренним системам'.
  Шаги под-цикла:
  1. Написать/создать файлы под-цикла -> коммит (subject выше).
  2. self-review (явная сверка с каждым релевантным правилом AGENTS.md; полнота по коду/дочерним;
     EN/RU parity; направление cross-link'ов; tier-чистота) + codex review с master:docs/AGENTS.md;
     правки отдельными коммитами до чистого прохода. Это твой контроль ПЕРЕД показом человеку.
  3. РЕВЬЮ ЧЕЛОВЕКОМ: отдаёшь результат человеку (исполнитель/оркестратор) — он даёт замечания.
  4. Исправляешь замечания ОТДЕЛЬНЫМИ коммитами (при необходимости снова self/codex) и возвращаешь на ревью.
  5. Повторяешь шаги 3-4, пока у человека НЕ ОСТАНЕТСЯ претензий — только тогда под-цикл закрыт.

Если конструкции блока ещё нет статьи — создать файлы en+ru в нужной type-папке (docs/paradigm/ или
docs/language/) и сам подключить их в docs/sidebars.js тем же блочным коммитом. Нашёл обобщаемое правило/проблему —
допиши его в AGENTS.md ОТДЕЛЬНЫМ коммитом (AGENTS.md дописывать может любой; это файл для ИИ).
CLAUDE.md в коммиты блока НЕ включать; docs/sidebars.js — НАОБОРОТ, включать в блочный коммит (это часть документации). Ветку блока пушишь сам (для ревью); merge ветки
в master (squash по необходимости) делает оркестратор после закрытия обоих под-циклов.
```

## B43 — Исполнение и планировщик  ·  [P] · слой 2 · трек T6 · зона: Исполнение

_Scope:_ Исполнение (обзор/auto/manual), планировщик, события и параметры запуска.

```
Ты дорабатываешь документацию lsFusion по блоку B43 «Исполнение и планировщик» (трек T6 — Интеграция, исполнение, управление, обучение).

Перед работой прочитай: docs/AGENTS.md (релевантные разделы целиком) и docs-process/DOCS-DESC-PLAN.md.
Источник истины — КОД платформы (LsfLogics.g -> ScriptingLogicsModule.java -> LogicsModule.java -> классы;
примеры в server/src/main/lsfusion; плагин — вторичен). Не описывай по памяти.
Работай в выданной под блок ветке (docs/B43 от master) — создай её ПЕРВЫМ действием и в ОТДЕЛЬНОМ рабочем дереве (git worktree add), не в общем чекауте: иначе параллельные блоки/исполнители молча затрут несохранённое. Коммить в ветку рано (commit-first); в master сам не мерджи и master не пушь.
ВАЖНО: это обзорный/корневой блок. Его источник истины — уже описанные ДОЧЕРНИЕ статьи (их состав
и термины) и дерево docs/sidebars.js, в большей степени чем код. Обзорная статья вводит и связывает
дочерние, а не пересказывает реализацию. Не бери этот блок, пока его дочерние блоки не описаны.
EN и RU — построчно синхронные переводы одного текста (одна структура и состав информации).
Tier-чистота: Paradigm — абстракция (как будто синтаксиса/расширений нет); Language — синтаксис/механика
именно этой абстракции, ссылается на Paradigm и не переизлагает её. Не смешивать tier'ы.

Блок только tier'а Paradigm — один под-цикл.

**Paradigm (доописать обе версии, en+ru построчно синхронно; если статьи ещё нет — создать оба файла):**
- `docs/paradigm/en/Execution.md` + `docs/paradigm/ru/Execution.md`
- `docs/paradigm/en/Execution_auto.md` + `docs/paradigm/ru/Execution_auto.md`
- `docs/paradigm/en/Execution_manual.md` + `docs/paradigm/ru/Execution_manual.md`
- `docs/paradigm/en/Scheduler.md` + `docs/paradigm/ru/Scheduler.md`
- `docs/paradigm/en/Launch_events.md` + `docs/paradigm/ru/Launch_events.md`
- `docs/paradigm/en/Launch_parameters.md` + `docs/paradigm/ru/Launch_parameters.md`

ЦИКЛ СДАЧИ на КАЖДЫЙ под-цикл (commit-first внутри ветки блока):
  Один под-цикл — PARADIGM: коммит 'docs: B43 paradigm — Исполнение и планировщик'.
  Шаги под-цикла:
  1. Написать/создать файлы под-цикла -> коммит (subject выше).
  2. self-review (явная сверка с каждым релевантным правилом AGENTS.md; полнота по коду/дочерним;
     EN/RU parity; направление cross-link'ов; tier-чистота) + codex review с master:docs/AGENTS.md;
     правки отдельными коммитами до чистого прохода. Это твой контроль ПЕРЕД показом человеку.
  3. РЕВЬЮ ЧЕЛОВЕКОМ: отдаёшь результат человеку (исполнитель/оркестратор) — он даёт замечания.
  4. Исправляешь замечания ОТДЕЛЬНЫМИ коммитами (при необходимости снова self/codex) и возвращаешь на ревью.
  5. Повторяешь шаги 3-4, пока у человека НЕ ОСТАНЕТСЯ претензий — только тогда под-цикл закрыт.

Если конструкции блока ещё нет статьи — создать файлы en+ru в нужной type-папке (docs/paradigm/ или
docs/language/) и сам подключить их в docs/sidebars.js тем же блочным коммитом. Нашёл обобщаемое правило/проблему —
допиши его в AGENTS.md ОТДЕЛЬНЫМ коммитом (AGENTS.md дописывать может любой; это файл для ИИ).
CLAUDE.md в коммиты блока НЕ включать; docs/sidebars.js — НАОБОРОТ, включать в блочный коммит (это часть документации). Ветку блока пушишь сам (для ревью); merge ветки
в master (squash по необходимости) делает оркестратор после закрытия обоих под-циклов.
```

## B44 — Параметры и безопасность  ·  [P] · слой 2 · трек T6 · зона: Управление

_Scope:_ Управление (обзор), системные/рабочие параметры, политика безопасности, интерпретатор.

```
Ты дорабатываешь документацию lsFusion по блоку B44 «Параметры и безопасность» (трек T6 — Интеграция, исполнение, управление, обучение).

Перед работой прочитай: docs/AGENTS.md (релевантные разделы целиком) и docs-process/DOCS-DESC-PLAN.md.
Источник истины — КОД платформы (LsfLogics.g -> ScriptingLogicsModule.java -> LogicsModule.java -> классы;
примеры в server/src/main/lsfusion; плагин — вторичен). Не описывай по памяти.
Работай в выданной под блок ветке (docs/B44 от master) — создай её ПЕРВЫМ действием и в ОТДЕЛЬНОМ рабочем дереве (git worktree add), не в общем чекауте: иначе параллельные блоки/исполнители молча затрут несохранённое. Коммить в ветку рано (commit-first); в master сам не мерджи и master не пушь.
ВАЖНО: это обзорный/корневой блок. Его источник истины — уже описанные ДОЧЕРНИЕ статьи (их состав
и термины) и дерево docs/sidebars.js, в большей степени чем код. Обзорная статья вводит и связывает
дочерние, а не пересказывает реализацию. Не бери этот блок, пока его дочерние блоки не описаны.
EN и RU — построчно синхронные переводы одного текста (одна структура и состав информации).
Tier-чистота: Paradigm — абстракция (как будто синтаксиса/расширений нет); Language — синтаксис/механика
именно этой абстракции, ссылается на Paradigm и не переизлагает её. Не смешивать tier'ы.

Блок только tier'а Paradigm — один под-цикл.

**Paradigm (доописать обе версии, en+ru построчно синхронно; если статьи ещё нет — создать оба файла):**
- `docs/paradigm/en/Management.md` + `docs/paradigm/ru/Management.md`
- `docs/paradigm/en/System_parameters.md` + `docs/paradigm/ru/System_parameters.md`
- `docs/paradigm/en/Working_parameters.md` + `docs/paradigm/ru/Working_parameters.md`
- `docs/paradigm/en/Security_policy.md` + `docs/paradigm/ru/Security_policy.md`
- `docs/paradigm/en/Interpreter.md` + `docs/paradigm/ru/Interpreter.md`

ЦИКЛ СДАЧИ на КАЖДЫЙ под-цикл (commit-first внутри ветки блока):
  Один под-цикл — PARADIGM: коммит 'docs: B44 paradigm — Параметры и безопасность'.
  Шаги под-цикла:
  1. Написать/создать файлы под-цикла -> коммит (subject выше).
  2. self-review (явная сверка с каждым релевантным правилом AGENTS.md; полнота по коду/дочерним;
     EN/RU parity; направление cross-link'ов; tier-чистота) + codex review с master:docs/AGENTS.md;
     правки отдельными коммитами до чистого прохода. Это твой контроль ПЕРЕД показом человеку.
  3. РЕВЬЮ ЧЕЛОВЕКОМ: отдаёшь результат человеку (исполнитель/оркестратор) — он даёт замечания.
  4. Исправляешь замечания ОТДЕЛЬНЫМИ коммитами (при необходимости снова self/codex) и возвращаешь на ревью.
  5. Повторяешь шаги 3-4, пока у человека НЕ ОСТАНЕТСЯ претензий — только тогда под-цикл закрыт.

Если конструкции блока ещё нет статьи — создать файлы en+ru в нужной type-папке (docs/paradigm/ или
docs/language/) и сам подключить их в docs/sidebars.js тем же блочным коммитом. Нашёл обобщаемое правило/проблему —
допиши его в AGENTS.md ОТДЕЛЬНЫМ коммитом (AGENTS.md дописывать может любой; это файл для ИИ).
CLAUDE.md в коммиты блока НЕ включать; docs/sidebars.js — НАОБОРОТ, включать в блочный коммит (это часть документации). Ветку блока пушишь сам (для ревью); merge ветки
в master (squash по необходимости) делает оркестратор после закрытия обоих под-циклов.
```

## B47 — Обучающие материалы  ·  [P] · слой 2 · трек T6 · зона: Обучение

_Scope:_ Learn, обучающие материалы, примеры, онлайн-демо.

```
Ты дорабатываешь документацию lsFusion по блоку B47 «Обучающие материалы» (трек T6 — Интеграция, исполнение, управление, обучение).

Перед работой прочитай: docs/AGENTS.md (релевантные разделы целиком) и docs-process/DOCS-DESC-PLAN.md.
Источник истины — КОД платформы (LsfLogics.g -> ScriptingLogicsModule.java -> LogicsModule.java -> классы;
примеры в server/src/main/lsfusion; плагин — вторичен). Не описывай по памяти.
Работай в выданной под блок ветке (docs/B47 от master) — создай её ПЕРВЫМ действием и в ОТДЕЛЬНОМ рабочем дереве (git worktree add), не в общем чекауте: иначе параллельные блоки/исполнители молча затрут несохранённое. Коммить в ветку рано (commit-first); в master сам не мерджи и master не пушь.
ВАЖНО: это обзорный/корневой блок. Его источник истины — уже описанные ДОЧЕРНИЕ статьи (их состав
и термины) и дерево docs/sidebars.js, в большей степени чем код. Обзорная статья вводит и связывает
дочерние, а не пересказывает реализацию. Не бери этот блок, пока его дочерние блоки не описаны.
EN и RU — построчно синхронные переводы одного текста (одна структура и состав информации).
Tier-чистота: Paradigm — абстракция (как будто синтаксиса/расширений нет); Language — синтаксис/механика
именно этой абстракции, ссылается на Paradigm и не переизлагает её. Не смешивать tier'ы.

Блок только tier'а Paradigm — один под-цикл.

**Paradigm (доописать обе версии, en+ru построчно синхронно; если статьи ещё нет — создать оба файла):**
- `docs/paradigm/en/Learn.md` + `docs/paradigm/ru/Learn.md`
- `docs/paradigm/en/Learning_materials.md` + `docs/paradigm/ru/Learning_materials.md`
- `docs/paradigm/en/Examples.md` + `docs/paradigm/ru/Examples.md`
- `docs/paradigm/en/Online_demo.md` + `docs/paradigm/ru/Online_demo.md`

ЦИКЛ СДАЧИ на КАЖДЫЙ под-цикл (commit-first внутри ветки блока):
  Один под-цикл — PARADIGM: коммит 'docs: B47 paradigm — Обучающие материалы'.
  Шаги под-цикла:
  1. Написать/создать файлы под-цикла -> коммит (subject выше).
  2. self-review (явная сверка с каждым релевантным правилом AGENTS.md; полнота по коду/дочерним;
     EN/RU parity; направление cross-link'ов; tier-чистота) + codex review с master:docs/AGENTS.md;
     правки отдельными коммитами до чистого прохода. Это твой контроль ПЕРЕД показом человеку.
  3. РЕВЬЮ ЧЕЛОВЕКОМ: отдаёшь результат человеку (исполнитель/оркестратор) — он даёт замечания.
  4. Исправляешь замечания ОТДЕЛЬНЫМИ коммитами (при необходимости снова self/codex) и возвращаешь на ревью.
  5. Повторяешь шаги 3-4, пока у человека НЕ ОСТАНЕТСЯ претензий — только тогда под-цикл закрыт.

Если конструкции блока ещё нет статьи — создать файлы en+ru в нужной type-папке (docs/paradigm/ или
docs/language/) и сам подключить их в docs/sidebars.js тем же блочным коммитом. Нашёл обобщаемое правило/проблему —
допиши его в AGENTS.md ОТДЕЛЬНЫМ коммитом (AGENTS.md дописывать может любой; это файл для ИИ).
CLAUDE.md в коммиты блока НЕ включать; docs/sidebars.js — НАОБОРОТ, включать в блочный коммит (это часть документации). Ветку блока пушишь сам (для ревью); merge ветки
в master (squash по необходимости) делает оркестратор после закрытия обоих под-циклов.
```
